/**
 * Debug Settings Category
 *
 * Production debugging tools for tracking and inspecting issues.
 */

import DebugPanel from '../../DebugPanel';

export default function DebugSettings() {
  return (
    <div className="settings-category">
      <DebugPanel />
    </div>
  );
}
