export interface TooltipData {
  agentId: string;
  name: string;
  status: string;
  workingOn?: string;
  screenX: number;
  screenY: number;
}

export interface ActionMenuData {
  agentId: string;
  screenX: number;
  screenY: number;
}
