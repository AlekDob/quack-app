# Log — session-usage-panel

- (Builder (display: Scott)) Builder: implementati SessionUsageCircle (ProgressCircle 16px) + SessionUsageDrawer (slide-over) + polling claude_usage_limits ogni 30s + cumulative token tracking in AIChatPanel. Build OK.
