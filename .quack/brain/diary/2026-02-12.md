---
type: diary
project: quack-app
date: 2026-02-12
---

## Fixed TeammateWidget drill-down bugs + diagnosed team persistence race condition

### Bug Fixes
1. **TeammateWidget not clickable after completion**: Fixed isClickable logic (removed isStarting dependency)
2. **SessionId lost on stop**: Fixed App.tsx to preserve sessionId when teammate completes

### Investigation
Used code-explorer agent to trace full team persistence flow:
- Rust side: Teams saved correctly to .quack/teams/{id}.json
- React side: Race condition - loadActiveTeam called before agents ready
- Result: Team loads but without avatar/color enrichment

### Next Steps
- Fix race condition by making loadActiveTeam reactive to agent availability
- Add error surfacing (toast notifications instead of console.error)
- Clean up orphan team files
- Consider localStorage caching as fallback

## Fixed Quack Store agent-bundle installation

Removed broken project picker flow and implemented direct global install for agent-bundles.

### Changes
- **useMarketplace.ts**: Added `installBundleSkills()` function for direct installation to `~/.claude/`
- **QuackStoreDrawer.tsx**: Updated routing logic - clicking "Get" on agent-bundles (Agent Jack, Agent Sophie, etc.) now installs skills and rules directly without project selection
- **User feedback**: Toast notification shows install counts like "Agent Jack installed (3 skills, 2 rules)"

### Impact
Agent bundles now install seamlessly to global Claude directory, making them immediately available for all projects.

## Fixed agent-bundle installation flow (CORRECT VERSION)

Project picker now preserved and working. Root issue was UnifiedAgent in Tauri store not connecting to sidebar terminals.

### Root Cause
When installing agent-bundles from the store, the agent would be created in the Tauri store but wouldn't appear in the sidebar with a working terminal. The project picker modal was showing but the installed agent lacked proper terminal connection.

### Solution
Added `onAgentBundleInstalled` callback chain:
1. **QuackStoreDrawer.tsx**: Passes callback to parent (App.tsx)
2. **App.tsx**: Handler that creates proper terminal in sidebar:
   - Calls `invoke('create_terminal')` to create Rust-side terminal
   - Updates React state with `setTerminals`
   - Injects agent personality into terminal

### Flow
1. User clicks "Get" on agent-bundle in store
2. Picks project from modal
3. Skills/rules installed globally to `~/.claude/`
4. Agent appears in sidebar with working terminal, ready to use

### Bonus Fix
Fixed StoreProjectPickerModal scroll overflow with max-height constraints.

### Files Changed
- **useMarketplace.ts**: Return arrays for proper callback handling
- **QuackStoreDrawer.tsx**: Added `onAgentBundleInstalled` callback prop
- **App.tsx**: Implemented callback handler with terminal creation
- **StoreProjectPickerModal.tsx**: Fixed scroll overflow
