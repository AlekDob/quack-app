---
type: diary
project: quack-app
date: 2026-04-01
---

- [10:30] (Alek) **fix(sidebar): branch not propagated from session to terminal** — La sidebar (RepositoryGroup) mostrava "main" per agenti che lavoravano su branch diversi (es. `001-mvp-youtube-wrapper`). Root cause: `AgentSession.branch` non veniva copiato su `TerminalInfo.branch`, e il git watcher partiva DOPO il checkout quindi non emetteva eventi. Fix doppio: (1) Rust watcher ora emette il branch corrente all'avvio, (2) Frontend propaga `session.branch` → `terminal.branch` quando la sessione viene attivata. 12 test unitari aggiunti. Files: `git_watcher.rs`, `App.tsx`, `branchTerminalSync.test.ts`.
- [10:52] (Auto) Sessione hook: 1 file letti (~152.8k tok), 1 letture ripetute
- [11:00] (Alek) **feat(brain-hooks): 4 hook automatici per Quack Brain** — Ispirato da OpenWolf, implementati 4 hook Claude Code (session-start, pre-read, pre-write, stop) che rendono il Brain proattivo. Pre-read mostra AST.md + gotcha prima di leggere file, pre-write verifica Brain breadcrumbs e Do-Not-Repeat, stop auto-genera diary entry. Zero dipendenze, puro Node.js. Creato anche pacchetto marketplace `brain-hooks` v1.0.0. Files: `~/.quack/hooks/brain/*.js`, `quack-marketplace/plugins/brain-hooks/`, `.claude/settings.json`.
- [11:15] (Auto) Sessione hook: 1 file letti (~0 tok), 1 letture ripetute
- [12:10] (Alek) **feat(brain-ui): BrainContextBanner in chat** — Banner collapsibile sopra la chat che mostra stats Brain (40G 52B 68P 20D) all'inizio di ogni sessione. Si espande con griglia stats colorate, ultimo diary, warning entry stale, e bottone "Apri Brain". Visibile solo quando messages.length === 0. Pattern: AgentRulesBanner. Files: `BrainContextBanner.tsx`, `BrainContextBanner.css`, `useBrainStats.ts`, `ChatView.tsx`.
- [12:40] (Alek) **feat(brain-diary): auto-diary su Mark as Done** — Quando l'utente chiude una sessione con "Mark as Done", Quack auto-appende al diary Brain un summary (titolo, messaggi, durata). Nuovo service `brainSessionService.ts` chiamato dal `sessionStore.markDone()`. Risolve il gap: gli hook CLI girano solo nel terminale, ma "Mark as Done" è un'azione UI. Files: `brainSessionService.ts`, `sessionStore.ts`.
