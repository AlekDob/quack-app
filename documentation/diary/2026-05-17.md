---
type: diary
project: quack-app
date: 2026-05-17
---

- [15:30] (Alek) Codex M1.5 — UX parity skills/comandi/personalità su sessione Codex. Domanda d'origine: "uno che usa Codex deve avere la stessa UX di Claude — come gestiamo skills/comandi/CLAUDE.md?". Prima della scrittura codice: **verifica doc Codex live + 2 spike empirici** su `codex exec` 0.42.0 (la modalità che spawniamo). Matrice verificata (→ `documentation/research/codex-exec-capability-matrix.md`): AGENTS.md ✅ confermato in exec; slash command ❌ rotti in exec (openai/codex#3641); skills ❌ nessuna discovery nativa (lo spike ha mostrato Codex che fa solo `ls`/`cat` del SKILL.md come file qualunque, zero eventi skill); subagent ❌ `NO_SUBAGENT_CAPABILITY` anche con `features.multi_agent=true` + `[agents.*]`. Lo spike subagent ha **chiuso** quel ramo (zero codice) e ha **confermato empiricamente** la riga "subagent assente" della `decision-quack-abstraction-agent-level-not-model-level` (era inferita; aggiunto addendum datato). Implementato (composizione prompt/file Quack-owned, NON reimplementazione harness, coerente con la decisione agent-level): (1) Rust — estratto `build_agent_header()` puro CONDIVISO tra CLAUDE.md e nuovo `inject_personality_to_agents_md` → persona byte-identica cross-backend, registrato in lib.rs, 2 golden test (parity + idempotency) verdi che blindano zero-regressione Claude; (2) TS — `injectAgentPersonalityAgentsMd` gemello best-effort + nuovo `codexPromptComposer.ts` (espansione `/comando` in `<command-context>` + indice `<available-skills>` per le skill selezionate dell'agente), entrambi cablati SOLO nel branch `backend === 'codex'` di `sendMessageForAgent` (path Claude byte-identico per costruzione, non importa nemmeno i moduli nuovi); 6 unit test sulla logica pura `expandSlashCommands`. Gate: tsc pulito, vitest miei 20/20 (composer 6 + adapter 14), cargo 40p/2f (i 2 fail sono i preesistenti documentati `claude_usage`/`telegram_obfuscation`; baseline 38p/2f → +2 miei test verdi, zero nuovi fail). Gli 88 fail vitest del full-suite **provati preesistenti**: stash delle modifiche + rimozione file nuovo → Tooltip/useMaxPlanTracking falliscono identici sul baseline `eb82f2b` (working tree sporco/ambiente, non mia regressione). KEY INSIGHT: la parità UX vera si ottiene riconoscendo che 2 dei 3 "primitivi harness" (comandi, personalità) sono in realtà **composizione di prompt/file** — portabili a costo quasi zero — mentre skill-autonome e subagent sono primitive vere dell'harness Claude → gate trasparente, mai fake silenzioso. Bonus emerso dai fatti: espandendo i comandi client-side Quack **bypassa il bug #3641**, dando alle sessioni Codex slash command funzionanti che il `codex exec` nativo NON ha (parità superiore, non inferiore). Effetto collaterale da sapere: una sessione Codex ora crea/aggiorna `AGENTS.md` nella working dir (stesso meccanismo del CLAUDE.md injection esistente). Follow-up tracciati: smoke GUI dal vivo (comando + skill su sessione Codex reale); valutare in M2 se iniettare il corpo completo SKILL.md vs solo l'indice in base ai token reali; il gate UI subagent/team Claude-only resta invariato (già coperto da pattern-backend-capability-gated-ui).
- [15:32] (Auto) Sessione: 7 file letti (~205.1k tok), 2 letture ripetute, ~217.1k tok risparmiati | Brain: 2 AST hits, 36 gotcha warnings, 42 breadcrumbs
