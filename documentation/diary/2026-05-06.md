---
type: diary
project: quack-app
date: 2026-05-06
---

- [16:07] (Auto) Sessione: 2 file letti (~3.1k tok)
- [16:11] (Auto) Sessione: 2 file letti (~21.2k tok)
- [16:30] (Alek) Fix lentezza switch tra progetti diversi (~2-3s): `AgentTokenStatsPanel` era sempre montato sotto l'accordion (children di `AccordionSection` renderizzati regardless di `isExpanded`), quindi ad ogni cambio progetto partiva `get_project_agent_stats` su SQLite + contesa col mutex di `recordUsage`. Aggiunto prop `enabled` gated su `focusedSection === "token-stats"` → la query parte solo quando il pannello è realmente aperto. Brain: `bugs/fix-token-stats-panel-blocks-project-switch.md`
- [16:16] (Auto) Sessione: 2 file letti (~14.3k tok) | Brain: 5 gotcha warnings, 1 breadcrumbs
- [16:29] (Auto) Sessione: 1 file letti (~27.0k tok) | Brain: 5 gotcha warnings
- [16:45] (Alek) Cambiata classificazione active/dormant in `RepositoryGroup.tsx`: ora un agente con **zero sessioni** è attivo (renderizzato come card piena), non più dormiente. Dormant rimane solo per agenti che hanno sessioni MA tutte `done` (archiviati). Risolve il flusso "creo nuovo agente → finisce nascosto come chip dormiente". Aggiornata feature doc `043-agent-sidebar.md`.
- [16:34] (Auto) Sessione: 7 file letti (~213.0k tok), 1 letture ripetute, ~668.1k tok risparmiati | Brain: 5 AST hits, 32 gotcha warnings, 17 breadcrumbs
- [17:00] (Alek) Coalescenza del doppio `loadAgents` al cambio sessione cross-progetto. L'effect a `App.tsx:7195` aveva `loadAgents` come dep; visto che `useCallback(loadAgents)` si ricrea su ogni change di `activeTerminal?.cwd` o `explorerPath`, il click sessione (che cambia entrambi in due step cascading: `setActiveId` poi `loadDirectory`) faceva partire l'effect due volte → 2× `check_agents_directory` + 2× `list_agents`. Rimosso `loadAgents` dalle deps con `eslint-disable` (stesso pattern del `loadSkills` effect a linea 7210). Restano da indagare: 3× `loadAvailableSkills` (probabilmente `basePath` di ChatInput multi-mount) e 3× `Saved 47 agents` di `unifiedAgentStorage`.
- [17:30] (Alek) TaskHub: progetto più visibile per ogni task. Il vecchio testo grigio 8px nascosto a destra dell'avatar è stato sostituito da un **chip colorato a sinistra del titolo** (subito dopo lo status dot), che usa il colore del progetto da `.quack-repo-order.dat` via `useProjectColor(projectPath)` — stessa fonte di Kanban e RepositoryGroup, niente nuovo storage. Per i progetti senza colore custom ho aggiunto `projectFallbackIndex` (hash 32-bit del path → indice nella `DEFAULT_PROJECT_COLORS` palette), così non finiscono tutti arancioni. Stile chip: `9px/600` lowercase, bg `${color}1F`, border `${color}55`, max-width 90px. Modifiche: `TaskHubItem.tsx` (nuovo prop `projectPath`, import hook, riposizionamento chip), `TaskHubView.tsx` (passa `item.session.projectPath`). Feature `054-task-hub-view.md` aggiornata. Scelta architetturale: scartato il toggle "Group by Status/Project" in favore del cambio puramente visivo — risolve il problema "non scansiono per progetto" senza preferenze persistenti né refactor del raggruppamento.
- [20:32] (Auto) Sessione: 2 file letti (~3.3k tok), 1 letture ripetute, ~9.7k tok risparmiati
- [20:45] (Alek) Documentato il jank residuo del cambio sessione cross-progetto in `bugs/investigation-residual-session-switch-jank.md`. Tre segnali da indagare quando si riprende: (1) `Saved 47 agents` × 3 in `unifiedAgentStorage` — probabilmente subscriber Zustand su `terminals` che persistere ad ogni mutation, fix candidato debounce; (2) `loadAvailableSkills` × 3 (108+108+95 deserializzazioni) — probabili multipli consumer (ChatInput + SkillSelector) con effect `[basePath]/[projectPath]` separati, fix candidato cache modulo-livello con TTL; (3) `[Tab Update]` × 4 + cascata `TerminalActivityBar isActive: false` — multi-recompute della tab state, fix candidato batching delle 5 chiamate setState dentro `handleSessionClick`. Quick wins ordinate per ROI nell'entry.
