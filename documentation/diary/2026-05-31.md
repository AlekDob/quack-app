---
type: diary
project: quack-app
date: 2026-05-31
---

- [09:25] (Auto) Sessione: 1 file letti (~4.7k tok), 1 letture ripetute, ~14.2k tok risparmiati | Brain: 4 gotcha warnings
- [09:30] (Alek) Refactor affidabilità stato sessioni col METODO HERDR (clonato github.com/ogulcancelik/herdr in /tmp per studio). Scoperta chiave: herdr non si fida degli hook — usa lo SCHERMO del terminale come ground truth che corregge hook lossy (Stop persi, eventi fuori ordine, nessun evento per piano/AskUserQuestion). Portato in Quack: (1) `src/utils/agentScreenDetect.ts` = port fedele di `claude_code.rs` (detectClaudeScreenState → blocked/working/idle/unknown, con guardia prompt-box per non marcare "done" Codex/shell); (2) `src/hooks/useScreenStateArbitration.ts` = loop 700ms che legge ogni buffer xterm e applica lo stato, idle debounced 1.5s; (3) `src/utils/sessionStatusWrites.ts` = markWorking/Blocked/Done/Released idempotenti, condivisi DRY tra hook e schermo; (4) hook allineati a herdr (UserPromptSubmit=working, PermissionRequest=blocked, Stop=done, SessionStart/End=released) in `useHookStatusListener.ts` + `terminal.rs`. Esposti `readAgentTerminalScreen`/`listAgentTerminalSessionIds` da AgentTerminalView. CODICE MORTO rimosso: tentativo precedente PreToolUse(ExitPlanMode|AskUserQuestion)/PostToolUse, BLOCKING_TOOLS, isBlockingNotification, Notification handling, idle-expiry sweep, campi tool_name/message nel hook JS. KEY INSIGHT: hook = acceleratore istantaneo (0ms), schermo = autorità che converge in ~1s; entrambi scrivono via sessionStatusWrites così non litigano. Test: `agentScreenDetect.test.ts` 9/9 verdi; tsc + cargo check puliti; i 39 fail della suite sono pre-esistenti (react-reconciler/@pixi, var(--accent-color), ecc.), non miei. Richiede rebuild + re-spawn terminale. Gotcha aggiornata: `gotcha-embedded-cli-plan-question-dot-stuck-yellow.md`.
- [09:45] (Alek) Follow-up caso yes/no: una `AskUserQuestion` con opzioni libere è indistinguibile da un menu slash/settings sullo schermo (stesso widget `❯ 1. … 2. …`). Soluzione IBRIDA: il detector su un menu numerato non-yes/no ritorna `unknown` (defer, non forza "done"), e re-introdotto il hook MIRATO `PreToolUse(ExitPlanMode|AskUserQuestion)`→blocked / `PostToolUse`→working — scatta solo per quei tool → zero falsi viola sui menu. Lo schermo resta autorità per working/idle/permessi/yes-no; il hook copre l'AskUserQuestion generica. Test 10/10, tsc + cargo puliti.
- [10:10] (Alek) Fix divergenza Task Hub vs sidebar: il dot verde "done" funzionava in sidebar ma il Task Hub mostrava la sessione sotto OTHER grigia. Causa: giallo/viola vengono da `chatLoadingMap`/`pendingQuestionsMap` (store condiviso, coerenti), ma il VERDE dipende dal marker `assistant complete` che `sessionStatusWrites` scrive in `chatStore.chatSessions` — letto SOLO dalla sidebar. Il Task Hub (TaskHubView + computeTaskHubBadge) leggeva i messaggi da uno `useState` locale di App.tsx (sync solo App→store, mai inversa) → non vedeva il marker → P4/Other grigio. Fix single-source-of-truth: TaskHubView e il badge in SidePanelAccordion ora leggono `chatSessions` da `useChatStore` (come la sidebar). Rimossa la prop morta `chatSessions` da TaskHubView + SidePanelAccordion (tipo, destructure, pass, import ChatMessage inutilizzato, call site App.tsx). tsc pulito.
- [10:45] (Alek) Nome sessione = Claude Code + resume al click (piano `immutable-purring-flamingo`). (1) Nome: nuovo comando Rust `read_transcript_title(path)` (riusa helper fattorizzato `first_user_message_title`, DRY con la metadata-scan) legge il primo messaggio utente dal transcript; su `Stop` il listener fa `updateSession({title})` SOVRASCRIVENDO sempre (scelta utente) → sidebar/Task Hub/tab si aggiornano (sessionStore condiviso). (2) Resume: persisto `claudeSessionId` dall'evento `hook-status` (porta sia quack_session_id sia il session_id di Claude — nel percorso embedded non veniva mai salvato), e passo `claudeArgs="--resume <id>"` ai due render-site di AgentTerminalView in App.tsx. I guard `createdTerminals`/`launchedTerminals` fanno scattare il resume solo su PTY fresco (riavvio app o riapertura tab), mai su sessione viva. KEY INSIGHT: il `claudeSessionId` per le sessioni embedded va catturato dagli hook, non da handleClaudeEvent (SDK, non più attivo sul main agent). tsc + cargo check puliti, detector 10/10. Richiede rebuild Tauri (nuovo comando).
- [11:05] (Alek) Fix regressione: Cmd+T (e TUTTI gli shortcut globali) non funzionavano più mentre una sessione era aperta. Causa: `useGlobalKeyboardShortcuts.ts` faceva early-return su `.xterm` (via isEditableElement), e l'embedded CLI è ora il centro permanente con focus → ogni keydown è dentro `.xterm` (anche tramite la hidden `xterm-helper-textarea`) → nessuno shortcut processato. Fix: rimosso `.xterm` da isEditableElement; in handleKeyDown gestione esplicita del terminale — digitazione (no Cmd/Ctrl) passa al terminale, combo con modificatore vengono matchate (solo gli shortcut registrati fanno preventDefault; Ctrl+C/Cmd+V passano intatti). Fix puro frontend (HMR, no rebuild). tsc pulito. Gotcha: `gotcha-embedded-cli-xterm-blocks-global-shortcuts.md`.
- [11:40] (Alek) Fix leak PTY orfani (embedded CLI): facendo done+elimina sessione il processo `claude` restava vivo → accumulo di istanze orfane ("rischio 1000 istanze"). Causa: `disposeAgentTerminal` (term.dispose + close_terminal→child.kill) esisteva ma NON veniva mai chiamata — col pivot WS8 il ciclo di vita del PTY (`agent-cli-<id>`, figlio dell'app) è scollegato dal record AgentSession; nessun path di delete (store/Task Hub/Kanban/drawer/handleDeleteSession) faceva teardown. Fix a 2 livelli: (1) choke-point in `sessionStore.deleteSession` → `disposeAgentTerminal(id)` via import() dinamico (no xterm eager nello store, no cicli) — copre TUTTI i delete; (2) reaper `useTerminalReaper.ts` (montato in App.tsx): ogni 3s interroga `list_terminals` (backend autoritativo) e killa ogni `agent-cli-*` la cui sessione non è più nello store, grace 8s + skip se isLoading. `disposeAgentTerminal` ora chiama SEMPRE close_terminal (anche senza istanza xterm nel Map → reaper raggiunge orfani non tracciati). KEY INSIGHT: la REGISTRY terminali è in-memory nel processo Rust → al riavvio è vuota, quindi list_terminals ritorna solo PTY della run corrente → zero falsi positivi al boot. Fix puro frontend (HMR, no rebuild Rust: list_terminals + close_terminal già esistono e registrati). tsc pulito. Gotcha: `bugs/fix-embedded-cli-orphan-pty-leak.md`.
- [10:45] (Auto) Sessione: 15 file letti (~261.5k tok), 9 letture ripetute, ~2.2M tok risparmiati | Brain: 13 AST hits, 144 gotcha warnings, 46 breadcrumbs
- [12:15] (Auto) Sessione: 6 file letti (~194.1k tok), 1 letture ripetute, ~10.0k tok risparmiati | Brain: 1 AST hits, 35 gotcha warnings, 45 breadcrumbs
