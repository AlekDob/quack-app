---
type: diary
project: quack-app
date: 2026-05-08
---
- [HH:MM] (Fredric) Fixed File Explorer refresh button regression: `memo` comparator in `src/components/FileExplorer.tsx` checked only `Object.keys(tree).length`, so same-key replacements from `fetchDirectoryChildren` were treated as equal and the UI never re-rendered. Switched to reference equality on `tree` and added `modifiedFiles` to the comparator. Updated `documentation/bugs/fix-file-explorer-refresh-stale-cache.md` with the second-cause section.
