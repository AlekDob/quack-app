# 2026-07-08

- [09:49] **Agent hub — context menu viewport clamp.** Problem: right-clicking a chat row in the right-side agent hub rail sometimes clipped the context menu ("Mark done" cut off) because `HubContextMenu` positioned at raw `clientX`/`clientY` with no viewport bounds check — worst when the rail sits on the window's right edge. Fix: replaced the bespoke `HubContextMenu` portal in `AIChatsRail.tsx` with the shared `ContextMenu` component, which already flips horizontally/vertically and clamps to the window. No CSS change; dead `.agent-hub-menu` rules can be removed later.
- [10:00] **Chat switch veil — visible in release builds.** Loader on session pick / New chat worked in `tauri dev` but not in the production `.dmg`. Root cause: `useChatSwitching` subscribed via `useEffect` + manual `isChatSwitching()` read — React prod doesn't reliably re-render on that external store flip. Fix: `useSyncExternalStore`; double-`rAF` before starting the 500ms hide timer so WKWebView paints at least one frame; keep `ChatSwitchVeil` always mounted in agent mode. Files: `useChatSwitching.ts`, `ChatSwitchVeil.tsx`, `chatSwitch.ts`, `AgentModeShell.tsx`.
- [10:45] **HTML preview — clickable paths + auto-open.** (1) `chatFileLinks.ts` — workspace paths in assistant markdown (`preview-quack.html`, `documentation/foo.md`, bare `045-html-preview.md`) render as light-blue pill links; click → `openFile` with `resolveChatFilePath`. (2) Write/Edit on `.html` auto-opens the file tab once per tool id (`consumeAutoHtmlOpen`); chip primary click opens preview tab instead of diff modal; `ShowHtmlPreview` auto-opens the browser drawer. (3) `ComposeCard` rows on `.html` open preview tab directly. Tokens: `--file-link-bg` / `--file-link-fg`. Docs: `045-html-preview.md`. Files: `chatFileLinks.ts`, `MarkdownPreview.tsx`, `chatToolRender.tsx`, `AIChatPanel.tsx`, `composeCard.tsx`, `App.css`, `preview-quack.html`.
- [11:30] **Chat persistence on disk + unified CLI session bridge.** Problem: `localStorage` ~5 MB quota filled (Virgilio chats) → "Chat not saved" toast; only Claude Code had full on-disk session list/load/recovery. Fix: (1) Rust `chat_store.rs` — atomic per-session JSON under `~/Library/Application Support/codetta/chats/{wsId}/`, global `provider-links.json` reverse index (`provider:cliId` → Quack session + title); auto-migrate from legacy localStorage on first hydrate. (2) Rust `provider_sessions.rs` — unified `provider_list_sessions` / `provider_load_session` for CC (`~/.claude/projects/…`), Cursor (`~/.cursor/projects/…/agent-transcripts/`), OpenCode (metadata from `~/.local/share/opencode/storage/session/`). (3) FE `chatStoreCache.ts` + `chatProviderRecovery.ts`; multi-provider `ProviderSessionsButton` (All/CC/CU/OC filters, linked-title badges); boot hydrates all open workspaces in `store.hydrate()`. `npm run build` + `cargo check` OK.
- [12:00] **Process cleanup — terminals + agent lifecycle.** Analysis: closing a terminal tab called `pty_kill` but only `child.kill()` on the shell pid → `make dev full` / `grep` children could survive as orphans; archive/done/close chat only changed UI state and left Claude/Cursor subprocess trees running. Fix: (1) `pty_kill` now reaps the whole PTY process group (`SIGHUP` + `SIGKILL` on Unix, `taskkill /T` on Windows). (2) New `claude_code_kill_session` / `cursor_code_kill_session` + `stopChatAgent.ts` + `aiStopBus.ts` — `closeAIChat`, `setAIChatLifecycle(done|archived)`, and `closeWorkspace` all stop the agent; `AIChatPanel` listens for HTTP-provider abort. Files: `pty.rs`, `claude_code.rs`, `cursor_code.rs`, `store.ts`, `stopChatAgent.ts`, `aiStopBus.ts`, `AIChatPanel.tsx`.
- [14:00] **HTML preview — relative assets on disk.** Problem: editor preview used bare `srcDoc` with no base URL → `theme.css` and `assets/*` never loaded; dark WKWebView + dark inline text = illegible cover pages (Virgilio `C01-cover.html`). Fix: `htmlPreviewBaseHref` + `prepareHtmlSrcDoc` inject `<base href="file://…/">` and `color-scheme: light`; `EditorPane` / `FileEditorPane` pass `baseHref` from the open file path. Drawer/agent `prev:` tabs unchanged (no path). Docs: `045-html-preview.md`.
- [15:00] **Dev build indicator — distinguish `tauri dev` from production.** Amber DEV band on Dock/taskbar icon (debug `build.rs` merges dev icon paths via `TAURI_CONFIG`; `icon-dev.icns` embedded on macOS dev). In-app: `DEV` pill in top bar + splash, 3px `--warn` inset border, `[DEV]` window title suffix. Frontend gate: `src/devMode.ts` (`import.meta.env.DEV`). Regenerator: `scripts/generate-dev-icons.py`. Docs: `047-dev-build-indicator.md`. No indicators in release builds.
- [16:00] **Claude Code background-task wake.** Problem: CC sometimes launches `run_in_background` Bash / subagents and says it will "wake up when done", but the chat goes idle. Root cause: Quack drives `claude -p` (headless) — stdin closes after the prompt, so plain background shells are killed ~5s after the final result (Anthropic headless docs); subagent wait was capped at 10 min by default (`CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS`). Fix (two layers): (1) Rust `apply_clean_env` sets `PRINT_BG_WAIT_CEILING_MS=0` + `CLAUDE_CODE_RESUME_INTERRUPTED_TURN=1`; (2) `backgroundWake.ts` + `AIChatPanel` — after a turn with background Bash, poll `claude_code_active_sessions`; when subprocess gone, auto-send `BACKGROUND_WAKE_PROMPT` as `--resume` continuation. Cancel on Stop/new send/unmount. Cursor/OpenCode out of scope. Doc: **feature 048** + cross-link in `014`.
