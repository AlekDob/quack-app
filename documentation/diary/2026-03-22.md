---
type: diary
project: quack-app
date: 2026-03-22
---
- [14:00] (Alek) feat: Changes Panel — markdown files (.md) get purple badge+name (#8b5cf6), Open in IDE button reusing OpenInIDEButton from FileExplorer, glowing green badge pulse on accordion even when collapsed. KEY INSIGHT: use `data-category` CSS selector to target specific accordion sections without new props.
- [14:30] (Alek) fix: Changes Panel disappearing on new message — `handleEditsChange` in App.tsx was recreating the Map from scratch, so empty updates between turns cleared it. Fix: merge into existing Map + skip empty updates. Also added `useEffect([activeId])` to reset on session switch. KEY INSIGHT: `ChatView.currentFileEdits` tracks only the LAST assistant message — parent must accumulate across turns.
- [15:00] (Alek) refactor: code review fixes on ChangesPanel — (1) added `onClearModifiedFiles` to `handleCommit` dep array (stale closure), (2) moved `loadDiff` side effect out of `setExpandedFiles` state updater (Brain: gotcha-console-log-inside-state-updater), (3) added `expandedFilesRef` guard in `loadDiff` for race condition, (4) removed `console.log` debug statements, (5) extracted `CommitModal` component to reduce file size. KEY INSIGHT: never call side effects inside React state updater functions — Strict Mode double-fires them.
- [16:00] (Alek) chore: bump SDK dependencies — `@anthropic-ai/claude-agent-sdk` 0.2.76→0.2.81, `@anthropic-ai/sdk` 0.78.0→0.80.0. Nessun breaking change. Novità rilevanti: fix `getSessionMessages()` per risultati paralleli, filesystem memory tools, GA thinking-display-setting, Zod v3.25+ compat. KEY INSIGHT: le due SDK rilasciano ~1 versione a settimana, conviene controllare ogni 2 settimane.
- [17:30] (Alek) feat: AgentResultCard — nuovo componente per renderizzare i risultati dei droid/subagent. Prima il tool `Agent` cadeva in `ToolMinimalStream` mostrando JSON raw; il tool `task` mostrava il `TaskWidget` ma mai il contenuto del risultato. Ora entrambi renderizzano una card glassmorphism con markdown formattato (via MarkdownText), avatar droid, metadata pills (durata, token, strumenti). Collapsabile con animazione. KEY INSIGHT: con SDK v0.2.81 i risultati subagent arrivano come array `[{text,type}]` — primo blocco = markdown report, secondo blocco = metadata con `<usage>` tags. Il parser deve gestire entrambi i formati (array e stringa) per backward compat.
