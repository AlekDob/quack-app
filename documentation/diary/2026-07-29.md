# 2026-07-29

## Plan buy-in — chip only; hide behind questionnaire

ExitPlanMode CTA no longer duplicates the plan in chat:

| Surface | Shows |
|---|---|
| Right Plan tab / FeatureDocDrawer / `plan:` | Full plan markdown (sole full-read) |
| Above composer | **Pass the ball to Milo** chip only |
| While `AskUserQuestion` docked | Milo chip hidden; questionnaire owns the dock |

Removed from `PlanBuyInCard`: plan preview, feature label, Open Plan, Keep discussing
button, hint row. Esc still denies; Enter still builds. Buy-in survives ask answers
via existing `keepPlanBuyIn`.

Docs updated (no new feature number — extends `088`): `088`, `061`, `084`, `073`,
`015`, `068`, `CLAUDE.md`.

Files: `PlanBuyInCard.tsx`, `AIChatPanel.tsx`, `App.css` (`.ai-plan-buyin*`).

## Project picker Recents — MRU, color, fuzzy search

New-chat / composer project menu (`WorkspaceProjectMenu`):

- Sort Recents by `last_opened` (most recent first); bump MRU on
  `setActiveWorkspace` so switching projects counts as usage.
- Folder icons use the project’s workspace color when set.
- Fuzzy search field at the top of the menu (↑/↓ + Enter).

Files: `WorkspaceProjectMenu.tsx`, `store.ts`, `App.css`, `050-composer-context-bar.md`.

## Agent hub — Cursor-minimal status

Dialed status chrome way back (no neon rings/glows on Ready):

- **Ready** — quiet project badge only
- **Working** — soft 2×2 spark in project color (no ring)
- **Needs input** — tiny amber pip with soft breathe
- **Error** — tiny red pip
- Selection — neutral border, not accent neon

Files: `AIChatsRail.tsx`, `App.css`, `009-agent-hub.md`.

## Agent hub Done — quiet rows + Delete all

Done section cleanup:

- Rows: project badge + title only (no status pip, no red/green diff, no Feature pill).
- Sort by `doneAt` / `archivedAt` (most recently parked first).
- Fuzzy search (same matcher as the rest of the app).
- Bulk menu `…`: **Reopen all** + **Delete all** (confirm, permanent).

Files: `AIChatsRail.tsx`, `App.css`, `009-agent-hub.md`.
