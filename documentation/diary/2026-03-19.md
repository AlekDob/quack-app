---
type: diary
project: quack-app
date: 2026-03-19
---
- [10:00] (Alek) fix: empty state missing drag region — when Quack launches with no projects, the sidebar (which holds all `data-tauri-drag-region` areas) is not rendered, making the window non-draggable. Added a 40px invisible drag div at the top of the empty state container in App.tsx. KEY INSIGHT: any full-screen view that hides the sidebar must provide its own drag region.
- [11:35] (Alek) fix: remote team manager can't detect delegated task completion — 3 bugs: (1) `useSessionMessageSync` used `session.agentId` as key for `chatSessions` Map, but SESSION-FIRST keyed by `session.id` → messageCount never synced, (2) `sendMessageForAgent` didn't save messageCount/status on completion (unlike `sendMessageForTargetAgent`), (3) no auto-done on session completion. Fix: corrected key lookup, added messageCount + status:'done' + completedAt to completion save. KEY INSIGHT: after SESSION-FIRST migration, any code using `session.agentId` as a chatSessions key is broken.
- [14:30] (Alek) feat: Codex-style Changes Panel in sidebar — new accordion section showing real-time file diffs from the current session. Per-file accept (git stage), reject (git discard/delete), accept all, and commit modal with title+description. Extracted `InlineDiffView` shared component from `DiffDrawer` (DRY). Added `git_discard_file` Rust command for tracked+untracked files. Also fixed 4 Italian error messages in `git.rs` → English. KEY INSIGHT: after commit, parent `modifiedFiles` map must be cleared via callback — otherwise UI shows stale changes.
