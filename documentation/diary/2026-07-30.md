# 2026-07-30

## Top bar — project chip opens workspace menu

Click (or right-click) the project name chip in the title bar opens the same
`WorkspaceColorPopover` as ActivityBar right-click in IDE mode: New chat here,
Reveal in Finder, Copy path, color swatches, No color.

Files: `TopBar.tsx`, `App.css`, `002`.

## Agent hub — stable project-badge height

Working spark replaced the initials inside `.agent-hub-badge` and the chip
shrank (padding-only box around a 7px grid). Badge is now a fixed
`inline-flex` box (`18×22` expanded / `26×26` collapsed) so Ready ↔ Working
never changes row height. Spark is brighter (lighter mix + higher opacity
floor + soft dot glow).

## Composer — inline @file cites (not chip row)

File mentions now match skills/features: insert `@rel/path` into the textarea and
color it via the highlight mirror (`.tok-file` → `--file-link-fg`, distinct from
skill orange / feature mint). No more pill above the input.

- `@` pick / tree drag / `/file` → insert token + still queue in `attachedFiles`
- Chip row is brain + agent only

Files: `composerInputHighlight.ts`, `ComposerInputHighlight.tsx`,
`ComposerMentionChips.tsx`, `AIChatPanel.tsx`, `App.css`, `072`.

## Agent Hub — Cursor CLI working status

`AgentHubWatcher` only polled `claude_code_active_sessions`, so chats pinned
to Cursor CLI stayed frozen as Ready while the agent ran.

- Added `cursor_code_active_sessions` (same `session_streams` ∩ `children` shape).
- Frontend `agentActiveSessions()` unions Claude Code + Cursor CLI.
- Watcher polls the union every 1.5s.

Files: `cursor_code.rs`, `lib.rs`, `ipc.ts`, `AgentHubWatcher.tsx`, `009`, `026`.
