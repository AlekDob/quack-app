---
type: diary
project: quack-app
date: 2026-04-16
---
- [10:00] (Alek) fix: skill drag-drop to chat intercepted by SplitDropZone overlay — handleChatDrop only handled files, early-returned for skills without clearing isDraggingSidebar. Overlay stayed stuck = frozen UI. Added pendingSkillMention state + prop drilling through ChatView to ChatInput.
- [14:51] (Auto) Sessione: 6 file letti (~220.1k tok), 5 letture ripetute, ~1.5M tok risparmiati | Brain: 8 AST hits, 111 gotcha warnings, 70 breadcrumbs
- [15:22] (Auto) Sessione: 1 file letti (~167.0k tok), 1 letture ripetute, ~5.0M tok risparmiati | Brain: 31 AST hits, 155 gotcha warnings
- [18:34] (Auto) Sessione: 9 file letti (~31.8k tok), 1 letture ripetute, ~4.4k tok risparmiati | Brain: 14 gotcha warnings
- [19:38] (Auto) Sessione: 3 file letti (~25.9k tok) | Brain: 17 gotcha warnings, 8 breadcrumbs
- [20:15] (Alek) chore: bumpato `@anthropic-ai/claude-agent-sdk` 0.2.96 → 0.2.111 e `@anthropic-ai/sdk` 0.80.0 → 0.90.0 (supporto Opus 4.7, `startup()` pre-warm, `getContextUsage()`, `agentProgressSummaries`). Branch `chore/bump-anthropic-sdks-2026-04-16`. Fix forward-compat su `sessions.rs::summarize_tool_use` per matchare sia `"Task"` che `"Agent"` (la prossima minor dell'agent-sdk migrerà il wire name). Salvata gotcha `gotcha-agent-tool-name-task-agent-migration` per tracciare la migrazione.
- [20:15] (Auto) Sessione: 1 file letti (~11.7k tok) | Brain: 5 gotcha warnings
- [20:30] (Alek) fix: `visualizer-mcp-server.js` description diceva "NO JavaScript" ma il sandbox reale usa `allow-scripts`. Emerso durante test live post-bump SDK: l'agente rifiutava di creare un contatore dinamico seguendo la regola (sbagliata) del tool. Corretta la description per allinearla al pattern `pattern-html-visualizer-inline.md` — JS inline ora esplicitamente permesso, solo CDN esterni bloccati da CSP.
- [20:42] (Auto) Sessione: 1 file letti (~1.3k tok)
- [21:15] (Auto) Sessione: 1 file letti (~19.8k tok) | Brain: 10 gotcha warnings, 8 breadcrumbs
- [21:17] (Auto) Sessione: 1 file letti (~240 tok)
- [21:30] (Alek) fix: AgentSessionItem click lasciava la chat a metà scroll. `MessageList` e `MessageListVirtualized` avevano un `useEffect` mount-only con timeout 100ms + `behavior: 'smooth'` che scattava prima che i messaggi async + markdown/code block avessero altezza finale. Aggiunto modulo `utils/sessionScrollMemory.ts` (Map singleton in memoria, zero overhead) per memorizzare `{scrollTop, wasAtBottom}` per sessione. Al mount: se l'utente era risalito in alto → ripristina esattamente la posizione; se era al fondo o stava auto-scrollando → porta al fondo. `ResizeObserver` (500ms) mantiene l'allineamento mentre i blocchi finiscono di montare. Feature toccata: 043-agent-sidebar.
- [21:47] (Auto) Sessione: 1 file letti (~1.4k tok) | Brain: 4 gotcha warnings
- [21:50] (Auto) Sessione: 3 file letti (~34.6k tok) | Brain: 15 gotcha warnings, 8 breadcrumbs
- [22:00] (Alek) refactor: sostituito restore-scroll automatico con UX opt-in basata su ancora. Session open → sempre scrollToBottom (predittibile). Nuovo componente `AnchorIndicator` che segue il thumb della scrollbar; click → ancora il messaggio centrato nel viewport (salva `messageId`); hover ancora → X per rimuovere; click su ancora → jump smooth al messaggio. Storage invariato (`sessionScrollMemory.ts` Map singleton, ma ora salva `{messageId}` invece di `{scrollTop, wasAtBottom}`). MessageListVirtualized per ora skippa l'anchor UX (>100 msg). Aggiunto `data-message-id` sui message-wrapper. Aggiornato pattern doc + feature 043 + AST.md.
- [22:30] (Alek) fix 3 bug sullo scroll sessione: (1) chat apriva in cima — l'effect aveva dep `[currentSessionId]` ma ritornava early se `messages.length === 0`, e i messaggi arrivano async → non si ri-triggerava. Aggiunto ref `appliedInitialScrollForSessionRef` + dep `messages.length > 0` (fires quando passa da false a true). (2) Icona ancora invisibile di default (opacity 0 + container-hover) → portata a opacity 0.45 base, 1 on hover. (3) Rimosso `scroll-to-top-button` (freccia SU che saltava al precedente messaggio utente) — era confuso, lasciato solo `scroll-to-bottom-button` (freccia GIÙ → fondo chat). Applicato sia a `MessageList` che `MessageListVirtualized`.
- [22:09] (Auto) Sessione: 1 file letti (~4.0k tok), 1 letture ripetute, ~4.0k tok risparmiati | Brain: 4 gotcha warnings
- [22:55] (Alek) fix: AnchorIndicator non appariva su alcune sessioni — il calcolo `getBoundingClientRect()` su `scrollRef.current` restituiva height 0 in alcuni casi di layout timing (container non ancora misurato al mount). Refactor: wrapped `.message-list` in `.message-list-wrapper` (position: relative, flex: 1), spostato l'AnchorIndicator come sibling con `position: absolute` + `right: 4px`. Nessun calcolo bounding box, il browser gestisce il posizionamento via CSS. Layout consistente su tutte le sessioni.
- [23:15] (Alek) fix vero causa scroll-up su chat resumed: il `ResizeObserver` osservava `.message-list` (flex:1, dimensione FISSA) — solo il contenuto interno `.message-list-content` cresce quando i messaggi async arrivano. Observer non scattava mai. Fix: osservo ENTRAMBI (container + content) + fallback requestAnimationFrame loop (8 frame, ~130ms) per catturare layout pass multipli. Inoltre chiamo `handleScroll()` dopo ogni `applyTarget` per sincronizzare `showScrollButton` anche quando non c'è un vero scroll event (altrimenti la freccia giù non appariva mai sulle chat resumed bloccate in cima). Timer observer esteso a 1500ms. Applicato a entrambi `MessageList` e `MessageListVirtualized`.
- [22:30] (Auto) Sessione: 1 file letti (~3.6k tok) | Brain: 1 breadcrumbs
- [23:45] (Alek) fix definitivo scroll virtualized (sessioni >100 msg): il bug persisteva su sessioni a 181/225 messaggi perché `ChatView` usa `<MessageListVirtualized/>` invece di `MessageList` e lo scroll-lock era solo nel secondo. Root cause isolata: `useListRef(null)` + `AutoSizer` → `listRef.current` è null al fire dell'effect (AutoSizer deve prima misurare il container, poi `<List>` monta e popola il ref). L'early return `if (!listRef.current) return;` killava l'effect per sempre. Fix: rAF polling loop incondizionato (60 frame / ~1s), `apply()` controlla `list = listRef.current` ad ogni tick, marca `appliedInitialScrollForSessionRef` solo on-success, attacca RO + wheel/touch/key/scroll listeners dentro `apply()` al primo tick in cui `list.element` esiste. Belt-and-suspenders: oltre a `scrollToRow(rowCount-1, 'end')` forzo anche `el.scrollTop = el.scrollHeight` (con `useDynamicRowHeight` il primo scrollToRow è impreciso). Scroll-to-bottom arrow ora funziona anche qui (listener `scroll` attaccato nello stesso blocco). Pattern doc aggiornato con il gotcha useListRef+AutoSizer timing.
