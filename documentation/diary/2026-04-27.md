---
type: diary
project: quack-app
date: 2026-04-27
---

- [16:02] (Auto) Sessione: 5 file letti (~203.2k tok), 2 letture ripetute, ~388.9k tok risparmiati | Brain: 3 AST hits, 32 gotcha warnings
- [16:54] (Auto) Sessione: 4 file letti (~179.5k tok), 2 letture ripetute, ~341.5k tok risparmiati | Brain: 3 AST hits, 21 gotcha warnings
- [17:05] (Alek) Fix race condition "nuova sessione da agente dormiente non si crea". Root cause: `AgentSessionList` rimontato durante `await saveAgentSessions` → `loadSessions()` concorrente sovrascriveva la memoria Zustand con disco vecchio (323 invece di 324) → `onSessionClick` non trovava la nuova sessione. Fix: rimosso `loadSessions()` on-mount in `AgentSessionList`; aggiunto guard `sessionWriteLock.shouldSkipReload()` all'inizio di `loadSessions`; spostato `markWrite()` PRIMA dell'await in `createSession`/`updateSession`/`deleteSession`/`markDone`. 3 test di regressione in `sessionStoreRaceCondition.test.ts`. Brain: `fix-session-create-race-load-overwrite`.
- [17:03] (Auto) Sessione: 6 file letti (~17.1k tok), 1 letture ripetute, ~6.2k tok risparmiati | Brain: 28 gotcha warnings, 21 breadcrumbs
