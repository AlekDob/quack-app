---
type: diary
project: quack-app
date: 2026-04-09
---

# 2026-04-09

- [07:30] (Alek) Investigated "Update Brain button produces no effect" report from Windows user. Found TWO issues:
  1. **Primary**: `onBrainUpdate` called `onSendMessage('/brain')` directly, bypassing `handleSend()` which does slash command expansion. Raw `/brain` sent to SDK → ignored. Fixed by routing through `handleSend('/brain')`. See `bugs/fix-brain-button-bypasses-handleSend.md`.
  2. **Secondary**: `slash_commands.rs` uses `std::env::var("HOME")` 8 times — fails silently on Windows (needs USERPROFILE fallback). Added `get_home_dir()` helper. See `bugs/bug-slash-commands-home-env-windows.md`. Same fix already applied to `rules.rs` and `agency.rs` on 2026-02-17.
- [10:25] (Auto) Sessione: 5 file letti (~77.7k tok), 4 letture ripetute, ~124.8k tok risparmiati | Brain: 65 gotcha warnings
- (Alek) fix: removed auto-done on team delegation sessions. Sessions with `leadSessionId` no longer get `status:'done'` + `completedAt` on first response. `notifyLeadAgent()` also removed. Team sessions stay `in_progress` like any other session. Updated feature doc (025) and pattern doc. Root cause: the first stream response doesn't mean the task is complete.
- [10:35] (Auto) Sessione: 1 file letti (~164.1k tok) | Brain: 1 AST hits, 10 gotcha warnings, 13 breadcrumbs
- (Alek) feat: clickable symbol chips in chat messages. Inline code matching camelCase/PascalCase/snake_case patterns now renders as `md-symbol-link` with left accent border. Click dispatches `quack:open-symbol` -> `findDefinition()` via code-intel -> opens editor tab at definition line via `pendingNavigationLine` store field. Fallback: toast on failure. Code review applied: tighter regex, CSS hover fix, WCAG keyboard support, store-based timing (no setTimeout), debounce guard. Files: MarkdownText.tsx/css, CodeEditorView.tsx, App.tsx, editorStore.ts. Updated feature doc 024.
- (Alek) feat: HTML preview in code editor. `.html`/`.htm` files now show Preview toggle (Cmd+Shift+P) like MD/Mermaid. Renders via existing HtmlVisualizer (sandboxed iframe, auto-resize, collapsible=false, maxHeight=4000). Files: CodeEditorView.tsx, EditorContent.tsx. Updated feature doc 024.
- (Alek) fix: external links in HtmlVisualizer iframe now open in system browser. Previously silently blocked by sandbox (no `allow-top-navigation`). Injected script intercepts `a[href^="http"]` clicks, sends `postMessage('quack-viz-link-click')` to parent, HtmlVisualizer calls `invoke('open_external_url')`. Also handles `mailto:` links. Files: htmlVisualizerUtils.ts, HtmlVisualizer.tsx.
- [11:37] (Auto) Sessione: 23 file letti (~214.6k tok), 16 letture ripetute, ~1.4M tok risparmiati | Brain: 9 AST hits, 88 gotcha warnings, 30 breadcrumbs
- [11:41] (Auto) Sessione: 8 file letti (~178.3k tok), 3 letture ripetute, ~660.9k tok risparmiati | Brain: 5 AST hits, 28 gotcha warnings, 1 breadcrumbs
- [12:47] (Auto) Sessione: 14 file letti (~317.9k tok), 7 letture ripetute, ~2.1M tok risparmiati | Brain: 6 AST hits, 153 gotcha warnings, 14 breadcrumbs
- [12:57] (Auto) Sessione: 13 file letti (~239.6k tok), 5 letture ripetute, ~685.0k tok risparmiati | Brain: 6 AST hits, 51 gotcha warnings, 28 breadcrumbs
- [13:51] (Auto) Sessione: 2 file letti (~5.5k tok) | Brain: 1 gotcha warnings
- (Alek) feat: PiP (Picture-in-Picture) window for active agent monitoring. Floating always-on-top mini-window shows all non-done sessions with real-time status dots, agent avatars, project names, last message, and relative timestamps. Click-to-focus navigates to agent in main window. Geometry persisted via tauri-plugin-store. Settings toggle + auto-open on startup via `.quack-ui-prefs.dat`. Sidebar footer PiP button with active accent highlight. Created feature doc 053-pip-window.
- [14:33] (Auto) Sessione: 1 file letti (~960 tok)
- [14:49] (Auto) Sessione: 6 file letti (~177.9k tok), 2 letture ripetute, ~6.4k tok risparmiati | Brain: 1 AST hits, 10 gotcha warnings, 14 breadcrumbs
- [15:00] (Alek) perf: Performance audit con code-explorer + code-reviewer droids. 17 problemi trovati (3 critical, 7 high, 7 medium). Creata spec-kit feature 005-performance-critical-refactor con 24 task in 4 fasi.
- [15:30] (Alek) perf: Fase 1 Quick Wins completata. (1) esbuild.pure strip console.log/debug in prod. (2) Tree-shaking riabilitato con Mermaid+PixiJS isolati in chunk dedicati (mermaid 1.1MB, pixi 500KB separati dal vendor). (3) MessageListVirtualized attivata condizionalmente per sessioni 50+ msg con tutte le props allineate + React.lazy loading. (4) PipAgentCard memoizzato con React.memo + comparatore custom + stili modulo-level + onClick stabile. Build prod verificato OK.
- [16:00] (Alek) perf: Fase 2 Stato e Streaming completata. (1) chatStore: aggiunto setSession/removeSession con singolo set(). (2) Sync loop chatSessions->chatStore ottimizzato: da clearSession+addMessage*N a singolo setSession (da centinaia di Zustand set()/sec a 1). (3) updatePipAgents throttled a 500ms (era 200ms). (4) normalizeModelName estratta a src/utils/modelUtils.ts (modulo-level). (5) ZustandProvider confermato non montato (N/A). (6) TerminalSidebar prop drilling rimandato a Fase 3. (7) handleScroll in MessageList: hasUserMessages estratto in useMemo. (8) usePipWindow: listener Tauri registrati con [] + storeRef.
- [15:25] (Auto) Sessione: 1 file letti (~857 tok)
- [17:45] (Alek) chore: Committed and pushed Cargo.lock version bump to 0.9.2 on 005-performance-critical-refactor branch. All PiP and performance refactor changes already committed in prior session (61a5259: "perf: critical performance refactor (Phase 1+2) + PiP improvements"). Push completed successfully to origin.
- [16:18] (Auto) Sessione: 3 file letti (~172.7k tok), 2 letture ripetute, ~169.8k tok risparmiati | Brain: 2 AST hits, 20 gotcha warnings, 28 breadcrumbs
- [16:27] (Auto) Sessione: 1 file letti (~4.1k tok)
- [16:36] (Auto) Sessione: 3 file letti (~18.7k tok), 1 letture ripetute, ~12.0k tok risparmiati | Brain: 10 gotcha warnings
- [17:00] (Alek) fix: app crash on session retrieve (100+ messages). Root cause: committed code used `require()` (CJS) in two places — `MessageListVirtualized.tsx` (module-level `require('react-window')` and `require('react-virtualized-auto-sizer')`) and `App.tsx` (inline `require('./components/TeammateStreamTab')` in render). `require` doesn't exist in browser/WebView, crash as soon as lazy-loaded MessageListVirtualized executes. Fix: replaced all CJS require() with ESM imports. Migrated MessageListVirtualized from react-window v1 API (VariableSizeList, itemData, itemSize) to v2 API (List, rowComponent, rowProps, rowHeight, useListRef). AutoSizer migrated from children render-prop to v2 renderProp. Removed stale @types/react-window v1. Files: MessageListVirtualized.tsx, App.tsx.
- [17:08] (Auto) Sessione: 1 file letti (~31.7k tok) | Brain: 1 gotcha warnings
- [17:30] (Alek) ui: @ mention autocomplete icons aligned with SidePanelAccordion. Skills badge: filled star -> outline star SVG (stroke, matching `icons.skills`). Features badge: yellow dot -> connected-circles graph SVG (matching `icons.features`). Added `.feature-badge` CSS class with gold-tinted background. Updated feature doc 051 with "@ Mention Autocomplete Icons" section + icon consistency rule.
- [17:20] (Auto) Sessione: 1 file letti (~165.9k tok), 1 letture ripetute, ~1.2M tok risparmiati | Brain: 8 AST hits, 55 gotcha warnings, 42 breadcrumbs
- [18:00] (Alek) feat: PiP context menu + sorting fix. Right-click on agent cards in PiP window now shows Mark as Done / Rename Session / Delete Session (same actions as sidebar and TaskHub). Rename uses inline dialog within PiP. Actions emit Tauri events (`pip-session-mark-done`, `pip-session-delete`, `pip-session-rename`) to main app which calls sessionStore. Sorting within each status group (Working/Completed/Idle) now uses `lastActivity` descending (most recent first) — was insertion order. PiP click-to-focus upgraded: now calls `handleSessionClick(sessionId)` via ref for full session activation (tab switch, chat focus, personality inject, sidebar scroll). Files: PipWindow.tsx, PipAgentCard.tsx, App.tsx. Updated feature doc 053.
- [18:42] (Auto) Sessione completata: sessione "retrieve app", 83 messaggi, 2h 14m
