---
type: diary
project: quack-app
date: 2026-03-30
---

- [11:30] (Alek) fix: file explorer icons/count clipped on deep indentation — added `flex-wrap: wrap` to `.explorer-row`, changed `.explorer-count` to not use `margin-left: auto`, added `flex-shrink: 0` to `.explorer-file-actions`. Known limitation: `flex-wrap` alone may not force wrap in all cases due to `flex: 1` on name.
- [11:45] (Alek) fix: file explorer refresh doesn't show filesystem changes — root cause: `loadDirectory` merges tree cache (only updates requested path, leaves subdirectories stale). Fix: refresh button now reloads ALL expanded directories; `refreshExplorerTrigger` clears entire tree cache before reloading root.
- [11:50] (Alek) fix: folder @ mention now uses `@folder:` prefix instead of `@file:` for directory entries. `onMentionFile` callback updated to receive `isDirectory` param.
- [14:00] (Alek) feat: Telegram Bidirectional Chat Integration (002-telegram-bidirectional-chat) — Split monolithic telegram_central.rs (483 lines) into 5 modules (telegram_types, telegram_send, telegram_commands, telegram_notifications, telegram_central). Added notification bridge subscribing to WsBroadcast for real-time agent turn summaries, reply-to-message routing for bidirectional chat, inline Stop button via callback queries, 3s per-session debounce, global mute toggle. Key pattern: WsBroadcast shared via `app.manage()` for cross-module subscription.
