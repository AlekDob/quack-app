---
type: diary
project: quack-app
date: 2026-05-18
---

- [09:00] (Alek) Crash hardening da crash report v0.9.3: `abort()` su `tokio-runtime-worker` ~13 min dopo wake-from-sleep (uptime 39h, binario stripped → frame +2907672 non risolvibile). Causa sistemica individuata in `Cargo.toml`: `panic = "abort"` → QUALSIASI panic in un task background (blip rete post-sleep, channel chiuso, scheduler) uccideva l'intero processo, e per di più rendeva `catch_unwind` inefficace. Mutex-poisoning cascade già sanato (zero `.lock().unwrap()`, time-unwrap già `unwrap_or_default`/`if let Ok`) → panic NUOVO e diverso. Fix scelto (hardening architetturale, approvato): (1) `panic = "abort"` → `"unwind"` → isolamento per-task nativo tokio, un panic non abbatte più l'app; (2) `install_panic_hook()` come prima riga di `run()` — logga ts+thread+location(file:line)+payload+backtrace su stderr e `~/Library/Application Support/quack/panic.log` (necessario: `tauri_plugin_log` solo in `cfg!(debug_assertions)`, in release nessun sink); (3) `strip=true` mantenuto + `debug=1` + `split-debuginfo="packed"` → genera `target/release/app.dSYM` separato (binario distribuito resta piccolo). Gate: `cargo check` pulito 24s, solo dead-code warning preesistenti, zero regressioni. KEY INSIGHT: con `panic="abort"` `std::panic::catch_unwind` NON funziona (niente unwinding) — il "future hardening #2" annotato nella gotcha era inerte da sempre; ora è sbloccato. Doc: aggiornato `documentation/bugs/fix-mutex-poisoning-cascade-abort.md` (sezione "Hardening implemented" + last_verified). Breadcrumb `// Brain: fix-mutex-poisoning-cascade-abort` in Cargo.toml e lib.rs. Follow-up tracciati: archiviare il `.dSYM` per ogni release (processo); identificare il panic-site originale alla prossima ricorrenza via panic.log; valutare supervisione/restart esplicito dei task long-lived (scheduler cron, WS/Telegram reconnect) ora che unwind lo consente.
- [08:48] (Auto) Sessione: 2 file letti (~17.5k tok), 1 letture ripetute, ~16.7k tok risparmiati | Brain: 15 gotcha warnings, 2 breadcrumbs
- [10:05] (Alek) Codex M1 RE-TARGET a codex-cli 0.130 + RETTIFICA overclaim del 2026-05-17. Innesco: l'utente ha linkato `developers.openai.com/codex/concepts/subagents` chiedendo "e questo?". Indagando, due scoperte (una mia colpa): (1) il `codex` installato era **0.42.0** (Homebrew-stale) mentre l'ultima è **0.130.0**, 88 minor di distanza; (2) tutti i miei "spike empirici" del 2026-05-17 (incluso "subagent assente, confermato Claude-only") giravano su quel binario morto → **conclusione FALSA**. Ritirata senza ammorbidire: nel diary 2026-05-17, in `research/codex-exec-capability-matrix.md` e nell'addendum di `decision-quack-abstraction-agent-level-not-model-level`. Aggiornato `codex` a 0.130 (brew→npm; effetto collaterale: `ripgrep` auto-rimosso da brew, offerto reinstall). Ri-spike dal vivo su 0.130 → 3 fixtures RAW (`src-tauri/src/agents/fixtures/`): **subagent FUNZIONA** in `codex exec` via `.codex/agents/*.toml` (`collab_tool_call`/`spawn_agent`/`wait`, thread figlio, 21+21→"42") — l'utente aveva ragione; **skill** nessuna discovery nativa locale (Codex fa solo `cat`) ma 0.130 le espone come **risorse MCP**; **AGENTS.md** ok. Problema grosso: M1 era cablato su 0.42 e **rotto su 0.130** — 4 breaking change: `--experimental-json`→`--json`; `exec` si appende su "Reading additional input from stdin…" se stdin non chiuso; `session.created/session_id`→`thread.started/thread_id`; `item_type/assistant_message`→`item.type/agent_message`; usage **in-stream** in `turn.completed` (architettura rollout-JSONL-tail obsoleta). Re-target completo: `events.rs` (`codex_stream_to_quack` riscritto + helper `codex_tool_identity`/`codex_aux_tool_output`, subagent/MCP come tool call, TDD sulle 3 fixtures reali, rimosso `codex_rollout_*`); `codex_backend.rs` (`--json`, `Stdio::null()` su stdin = fix hang, `thread_id` con fallback, rimossi `read_last_usage_for_session`/`walk_jsonl` → killato il debito M3 "sync fs in async", `codex_auth_probe` idem). `QuackAgentEvent` **invariata** → zero churn TS, `codexEventAdapter.ts`/App.tsx non toccati. Gate: cargo full **39p/2f** (i 2 fail sono ESATTAMENTE i preesistenti `claude_usage`/`telegram_obfuscation`, zero nuovi, Claude arm `claude_event_tests` verdi byte-identici), tsc pulito, adapter TS **20/20 invariato** (prova che il contratto QuackAgentEvent ha tenuto). KEY INSIGHT: **mai dichiarare una capability di un tool esterno senza fissare la versione del binario** — un `which codex && codex --version` a inizio task avrebbe evitato un intero ciclo di analisi su fatti falsi. Corollario: il confine resta agent-level, ma "subagent su Codex" NON è più impossibile-da-gate, è scelta di prodotto (0.130 li ha nativi). Follow-up: (a) **smoke GUI dal vivo su 0.130 NON ancora fatto** — residuo umano critico prima del merge; (b) M2 probe versione codex + guard versione minima (schema non stabile tra major-gap); (c) M2 valutare canale skill via risorse MCP di 0.130; (d) `-c mcp_servers={}` non isola più del tutto l'MCP su 0.130. Niente commit (regola: solo su richiesta).
