---
type: diary
project: quack
date: 2026-08-18
---

- [11:05] (Alek) Audit sicurezza pre-release MIT. Scansione completa: tutti gli oggetti git dei 6452 commit, zero chiavi API/token/chiavi private mai committati (unici match: due chiavi PostHog `phc_`, una attuale e una rimossa in `ec3778bb`). Codice già solido lato upstream: confronto token timing-safe, bind non-loopback che rifiuta di partire senza auth-token, serving file con realpath+allowlist+grant a 2 min, zero `shell: true`, SQL parametrizzato, Electron con `contextIsolation`/`sandbox`, `pull_request_target` senza checkout del codice PR. Due fix applicati: (1) telemetria ora **opt-in** — `SYNARA_TELEMETRY_ENABLED` default `false` e `SYNARA_POSTHOG_KEY` senza default, più un gate `telemetryEnabled` che richiede entrambi (altrimenti un fork che accende il flag POSTerebbe con `api_key` vuoto riempiendo il log di flush failures); (2) LICENSE con la catena di copyright completa T3Code → Synara → Quack. Restano due cose che dipendono da una decisione: 4014 icone Central Icons in `apps/web/public/central-icons-*` (set commerciale, l'MIT sopra concede a tutti la ridistribuzione che probabilmente non abbiamo) e 59 vulnerabilità transitive, di cui `seroval <=1.5.2` critica via `@tanstack/react-router` e `builder-util-runtime <9.7.0` che leaka credenziali su redirect cross-origin — questa conta perché spediamo aggiornamenti desktop. Gotcha: la repo ha 1923 ref `refs/synara/checkpoints/*` (snapshot dei turni degli agent); contengono solo file sorgente, nessun file ignorato, ma un `git push --all`/`--mirror` li pubblicherebbe tutti.
