---
type: diary
project: quack-app
date: 2026-05-27
---

- [09:03] (Auto) Sessione: 2 file letti (~6.6k tok) | Brain: 1 gotcha warnings
- [09:05] (Auto) Sessione: 1 file letti (~12.8k tok) | Brain: 4 gotcha warnings
- [09:20] (Auto) Sessione: 3 file letti (~17.8k tok) | Brain: 4 gotcha warnings
- [09:30] (Alek) Fix render Codex: stretto il regex `Following rules:` in `StreamMessage.tsx:763` da `(.+)$` greedy a `([\w-]+(?:\s*,\s*[\w-]+)*)\s*\.?\s*$`. Causa: `personality.rs:492` chiede a ogni agente di dichiarare "Following rules: X, Y" all'inizio; Claude rispetta il template (1 riga pulita), Codex/gpt-5-codex lo rimescola con la prosa italiana ("Following rules: skill-x. Spiego prima di agire: ..., oppure ..., ti posso ...") e il `(.+)$` greedy ingoiava l'intero messaggio splittandolo per virgola in 5 `rule-pill` mega, lasciando la bolla `assistant-message-text` vuota. Insight: ogni parser del template del prompt deve fallire silenziosamente quando il modello "improvvisa" — il regex stretto è la safety net contro backend che non rispettano il formato. Brain: `gotchas/gotcha-following-rules-regex-codex-prose-collision.md`. Zero regressione su Claude (template rispettato matcha ancora). Smoke test pendente su sessione Codex reale.
- [09:24] (Auto) Sessione: 14 file letti (~241.1k tok) | Brain: 2 AST hits, 11 gotcha warnings
- [09:26] (Auto) Sessione: 1 file letti (~13.3k tok) | Brain: 10 gotcha warnings, 2 breadcrumbs
- [09:29] (Auto) Sessione: 4 file letti (~21.4k tok), 1 letture ripetute, ~8.4k tok risparmiati | Brain: 12 gotcha warnings, 2 breadcrumbs
- [16:40] (Alek) WS4 — Handoff agente: implementazione completa frontend + slash command Quack built-in. Aggiunto `parentSessionId?: string` a `AgentSession` (`types.ts:498`) come genealogy SOLO visiva (nessuna semantica runtime, scelta esplicita dell'utente). Nuovo `src/services/handoffService.ts` con `generateHandoffSummary` (chiama `streamClaudeMessage` one-shot con MCP disabilitato e sessionId nullo per evitare collisioni con la sessione viva, fallback su `createLocalSummary` se l'SDK fallisce) e `executeHandoff` (riusa `useSessionStore.createSession` + `initialPrompt` wrappato in `<handoff-context from=X to=Y>…</handoff-context>`). Nuovo modal `HandoffDialog.tsx` (+ CSS): macchina a stati `pick → generating → review → submitting` con AbortController per cancellare la generazione se l'utente chiude. Bottone "Handoff to…" dopo Compact nel `SessionPopover` (`UnifiedActionBar` passa la prop solo se `internalSessionId` esiste). Slash command `/handoff @agent` intercettato in `ChatView.handleSend` prima di `expand_slash_command` — registrato come Quack built-in in `slash_commands.rs` per l'autocomplete. Sidebar: in `AgentSessionList.tsx` ricostruisco la mappa `parent → children` per branch, render ricorsivo con `depth` clamp a 2; `AgentSessionItem` accetta prop `depth` (marginLeft = depth*14) + connector decorativo a sinistra che lega visivamente la forkata al genitore. `tsc --noEmit` pulito. Breadcrumbs `// Brain: handoff-agent-fork` posati nei punti chiave. Plan: `~/.claude/plans/mi-piacerebbe-abilitare-funzione-parsed-glade.md`. Workstream: `documentation/workstreams/04-handoff-agent-fork.md`.
- [17:40] (Alek) WS4 — Handoff iterazione 4 (auto-send): la nuova sessione adesso parte da sola, niente più "premi Enter manualmente". Prima il `wrappedPrompt` finiva in `initialPrompt` → pre-popolato in ChatInput (pattern Kanban) ma non auto-inviato. Nuovo modulo `src/services/sendMessageBridge.ts` con `registerSendMessageForTargetAgent` / `getSendMessageForTargetAgent` — App.tsx registra la sua `sendMessageForTargetAgent` via `useEffect` al mount (dipendenza l'identity della useCallback). `executeHandoff` legge il bridge e chiama `send(newSession.id, wrappedPrompt, { workingDirectory })` subito dopo `createSession`. Cambiata sessione: `status: 'todo'` (auto-transitions a in_progress al primo send, vedi `sendMessageForTargetAgent` App.tsx:3848) + `initialPromptConsumed: true` (così il wrappedPrompt NON si rimostra in ChatInput quando l'utente apre la sessione). Fallback grazioso: se il bridge non è ancora registrato (race con App mount), warn + l'utente vede comunque il prompt in input. `tsc --noEmit` pulito.
- [17:25] (Alek) WS4 — Handoff iterazione 3 (rifiniture UX): (1) Filtro agenti per progetto in `HandoffDialog`: `availableAgents` ora prende solo i `TerminalInfo` con `cwd === fromSession.projectPath`, deduplicati per `id`. Risolto bug visibile in screenshot — la lista mostrava i duplicati di tutti i progetti aperti (Agent Jack/Swift comparivano 2-3 volte). (2) Avatar agenti nella lista: `<img>` 26x26 tondo con `getAvatarUrl(agent.avatar)` (filename in `~/.claude/agents/avatars/`) e fallback su `getDuckdroidUrl()` via `onError`. Stile coerente con sidebar. (3) Clamp del context menu in `AgentSessionItem.handleContextMenu`: la position cliccata viene clampata a `(innerWidth - menuWidth, innerHeight - menuHeight)` con padding 8px. Risolto clipping del bottone "Delete Session" quando si cliccava vicino al bordo inferiore (causato dall'aggiunta della voce Handoff che aumenta l'altezza menu). `tsc --noEmit` pulito.
- [17:05] (Alek) WS4 — Handoff iterazione 2: (1) Globalizzato il dialog via nuovo store `src/stores/handoffDialogStore.ts` (Zustand: `isOpen`, `sessionId`, `initialTargetAgentId`, `open()`, `close()`) — il `HandoffDialog` ora vive come singleton in `App.tsx` (non più dentro `ChatView`), tutti gli entry points lo aprono dallo store. Tagliato così il prop-drilling che avrebbe richiesto di passare la callback dal ChatView fino agli item della sidebar. (2) Voce "Handoff to…" aggiunta nel context menu di `AgentSessionItem` (tra Rename e Delete) — chiama `useHandoffDialogStore.getState().open(session.id)`. (3) Rimosso il filtro "stesso agente" — `availableAgents` ora include il current agent (badge "stesso agente" giallo nel dialog per chiarezza UX), e tolto il check `target.id === session.agentId` nello slash command. Use case: fork in parallelo sullo stesso agente con il sunto come bootstrap (utile per esplorare varianti senza inquinare la sessione viva). `tsc --noEmit` pulito.
- [16:42] (Alek) BLOCKER per smoke test handoff: `cargo check` di `src-tauri` fallisce su `terminal.rs:576-577` — `crate::remote_ws::WS_BROADCAST` e `WsEvent::TerminalOutput` non esistono. Bug pre-existing introdotto in `2f568e6 feat(quack-remote): PWA Task Hub mobile rewrite with live state sync`. Non toccato da WS4 — fix spetta a un workstream dedicato. Smoke test `pnpm tauri dev` impossibile finché il bug Rust resta. La parte frontend di WS4 è verificabile via `pnpm dev` (browser-only) ma il path completo richiede backend funzionante (per accesso ai messaggi della sessione e creazione sessione Tauri Store).
- [19:10] (Alek) WS6 — PoC Fase 1 ✅ `visualizer-mcp-server.js` refactor in dual transport (stdio | http). Aggiunto flag `--http --port N` (N=0 → OS-assigned), stampa `MCP_HTTP_PORT=<port>` su stdout per parent pickup. Usa `StreamableHTTPServerTransport` con `sessionIdGenerator: () => randomUUID()`. Endpoint: `GET /health` (readiness JSON), `POST/GET /mcp` (handshake MCP). Graceful shutdown su SIGINT/SIGTERM. Test eseguiti con curl: (1) `/health` → `{status:'ok', name:'visualizer', version:'1.1.0'}`, (2) `POST /mcp initialize` → 200 + SSE event con serverInfo + protocolVersion 2024-11-05 + `mcp-session-id` header, (3) `tools/list` → ritorna `visualize_html` con full schema, (4) `tools/call visualize_html` → ritorna `Rendered "PoC" (13 chars of HTML)`, (5) `notifications/initialized` → 202 (corretto, no response per notification). Stdio retrocompat: `node visualizer-mcp-server.js` (default) accetta initialize via stdin e risponde su stdout. Regression baseline: `pnpm tsc --noEmit` EXIT=0, `cargo check` EXIT=0 (7 warning pre-existing, zero nuovi). **Note operative:** in produzione `stream-daemon.js:782-787` continua a invocare `visualizer-mcp-server.js` come stdio — zero impact runtime. Il flag `--http` è opt-in, attivabile dal pool quando lo cablerò in Fase 2. Il bundle `/Applications/Quack.app/Contents/Resources/node-sdk/visualizer-mcp-server.js` resta vecchio finché non si rebuilda Quack (`pnpm tauri build`), ma è ininfluente perché in produzione il transport non cambia. Brain breadcrumb `// Brain: ws6-mcp-http-pool` posato. Workstream: `documentation/workstreams/06-mcp-http-pool.md`.
- [18:30] (Alek) Diagnosi performance "Mac va a palla con Quack": indagine col terminale (`ps -A -o pid,ppid,pcpu,pmem,rss,command`). Verità vs sospetti iniziali: (1) il picco `codesign 210%` era **Squirrel ShipIt di Claude Desktop**, NON Quack — auto-updater Electron. (2) **3 sessioni Quack `claude` attive simultanee** (non 2), ognuna spawn-ate dal binario nativo SDK 0.3.150, ~330 MB ciascuna. (3) **Daemon zombie `agent-browser`** (PID 57989, installato globalmente in `/opt/homebrew/lib/node_modules/agent-browser`) teneva aperto Chromium headless con GPU process al 85% CPU costante — orfano lasciato da skill (medium-writer/seo-audit). Killato `kill 57989` → tutti i figli (chrome-headless-shell renderer/gpu/utility) morti in cascata, recuperato ~1 core CPU + 400 MB RAM. (4) **Bottleneck architetturale identificato:** 3 sessioni × 3 MCP server stdio = 9 processi Node MCP (~540 MB). Scala male: 10 sessioni → 30 processi (~1.8 GB). Causa: `stream-daemon.js:782-787` passa `ide-tools`/`code-intel`/`visualizer` come stdio, binario nativo `claude` fork-spawna 1 child per sessione. (5) **WS6 aperto** — MCP HTTP Server Pool (`documentation/workstreams/06-mcp-http-pool.md`): refactor i 3 server in doppio transport stdio/HTTP, McpPool class in stream-daemon spawnia 1 per server al boot, sessioni si collegano via `{type:'http', url:'http://127.0.0.1:PORT'}` (SDK già supporta). Target: 3 MCP totali a prescindere dal numero di sessioni (-90% RAM ausiliaria @ 10 sessioni). Blocca su WS1 smoke test. **Verificato:** `code-intel` NON droppabile — `EditorOutlinePanel.tsx` lo usa per outline dell'editor integrato di Quack, oltre alla skill `code-navigation`.
- [17:15] (Alek) WS5 — Remote Terminal Management: implementazione completa. 6 endpoint REST sotto `/api/terminals` (create, list, get, write, output, close) che permettono ad agenti esterni (via skill `quack-remote`) di creare terminali visibili nell'UI, eseguire comandi e leggere output senza bloccarsi. **Backend Rust**: nuovo `remote_api_terminal.rs` (~160 righe, 6 handler axum), `OutputRingBuffer` in `terminal.rs` (5000 righe, VecDeque + pending line), `flush_and_store` helper che alimenta ring buffer + emette Tauri event + WsEvent broadcast, 3 nuove varianti WsEvent (`TerminalCreated`/`TerminalOutput`/`TerminalClosed`), `WS_BROADCAST: OnceCell<WsBroadcast>` globale per output thread senza accesso a ApiState. Reso `pub(crate)` su 6 `_impl` fn + `REGISTRY` + `compile_info`. **Frontend**: listener `terminal-list-changed` in App.tsx (auto-select + focus Terminal Window via `openTerminalWindow` + `emitTo`), listener `terminal-window-select-terminal` in TerminalWindowApp.tsx. **Docs**: sezione Terminal Management in skill `quack-remote.md`, 6 nuovi endpoint in feature doc 062. `cargo check` pulito (zero errori). Risolve anche il BLOCKER [16:42] — `WS_BROADCAST` e `WsEvent::TerminalOutput` ora esistono. Workstream: `documentation/workstreams/05-remote-terminal-management.md`. Smoke test `pnpm tauri dev` + curl pendente.
- [10:17] (Auto) Sessione: 2 file letti (~3.7k tok) | Brain: 1 gotcha warnings
- [20:05] (Alek) WS1 SDK 0.3.150 — fix rendering TodoWrite widget righe vuote (screenshot utente: "3/7 completed" con i 3 pending in cima testati e i 4 in_progress/completed sotto VUOTI). Root cause: type mismatch fra `StreamMessage.tsx:432` (salva `Map<string, content-object>`) e `taskAccumulator.ts:51` (`Map<string, string>` su cui chiama `JSON.parse`). `JSON.parse` su `[object Object]` esplode → `parsed?.task?.id` è sempre `null` → in `TaskCreate` l'id ricade su `pending-${toolUseId}` → quando arriva `TaskUpdate` con `input.taskId` (es. `task-abc`) non matcha mai, e l'accumulator crea voci NUOVE con `content:''` perché `TaskUpdate` non re-include `subject` se aggiorna solo `status`. Fix: aggiunto `toolResultTexts: Map<string, string>` accanto a `toolResults` nello stesso `useMemo`, estraendo robustamente `content` (string | array di block | block singolo) come fa già `ChatView.tsx:807`. `accumulateTodos` ora riceve il text Map. `tsc --noEmit` pulito. Brain entry: `documentation/bugs/fix-task-accumulator-toolresult-text-mismatch.md`. Smoke test pendente — l'utente deve riprodurre con una sessione che usa TaskCreate+TaskUpdate sequenziali.
