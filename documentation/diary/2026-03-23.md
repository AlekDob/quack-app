---
type: diary
project: quack-app
date: 2026-03-23
---
- [14:00] (Alek) fix: daemon mode was missing `options.betas = ['context-1m-2025-08-07']` → SDK operated at 200k context window instead of 1M, causing auto-compaction at ~155k tokens. Also fixed `calculateTokenBudget()` double-counting (outputTokens + overhead added to inputTokens that already includes overhead) and aligned TokenUsageIndicator formula with StaminaBarBorder. Brain: fix-daemon-missing-1m-context-betas
- [15:00] (Alek) fix: session limit drains fast — ideContext and gitContext were appended to systemPrompt, breaking prompt cache on every turn. ~50k tokens of system context re-processed at full price instead of 1/10 cache rate. Moved dynamic content to user prompt as `<system-reminder>`. Added rate limit (429) error detection with user-friendly Italian message. Brain: fix-session-limit-prompt-cache
