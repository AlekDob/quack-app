---
type: diary
project: quack-app
date: 2026-03-11
---
- [14:30] (Alek) Creato BTW side-chain chat: hook `useBTW.ts` + `BTWDrawer.tsx` + `BTWDrawer.css`. Il hook gestisce stato ephemeral, invoca `btw_query` via Tauri, registra shortcut globale da `general.btwShortcut`. Il drawer usa Framer Motion spring slide-in da destra, glassmorphism `rgba(10,10,18,0.82)` + `backdrop-blur(24px)`, accenti `#FF6B35`, auto-focus input, ESC per chiudere. Nessuna persistenza — stato resettato on close by design.
- [15:30] (Alek) Implementato Quick Loop UI: `src/hooks/useQuickLoop.ts` (setInterval-based loop con start/stop/pause/resume, validazione minimo 10s, auto-stop su maxRuns), `src/components/loop/QuickLoopPopover.tsx` (form configurazione + stato running, glassmorphism dark theme, opzioni intervallo 30s-30min), `src/components/loop/QuickLoopIndicator.tsx` (pill badge pulsante nel toolbar). TypeScript strict, zero errori tsc.
- [17:00] (Alek) Implementato Rewind UI: `src/hooks/useFileCheckpoints.ts` (ascolta `claude-event:{sessionKey}` per eventi `file_checkpoint`, mantiene array ordinato DESC per timestamp, espone `clearCheckpoints`/`markRewound`), `src/components/rewind/RewindTimeline.tsx` (sidebar slide-in da destra 320px, timeline verticale con nodi espandibili, warning banner se agente running, CSS transitions senza framer-motion non installato), `src/components/rewind/RewindPreviewModal.tsx` (modale glassmorphism centrata, lista file con icone, warning distruttivo, spinner su confirm, CTA primaria #FF6B35). TypeScript strict, zero errori tsc.
- [17:30] (Alek) Completata integrazione SDK Updates 2026 in ChatView: aggiunto Quick Loop (bottone toolbar + popover + indicator) e Rewind (bottone timeline + sidebar + modal preview con conferma rewind). Creato `src/services/structuredOutputService.ts` (build outputFormat da schema registry, parse/validate JSON, extract da markdown fences, retry prompt builder). Aggiunta sezione BTW in ClaudeCodeSettings con dropdown modello (Haiku/Sonnet/Opus). Tutte e 4 le feature spec-004 implementate: BTW Side-Chain, Quick Loop, Rewind UI, Structured Outputs. Zero errori tsc su intera codebase.
- [18:00] (Alek) Fix BTW: 1) Shortcut cambiato da Cmd+B a Ctrl+B (conflitto con Side Panel). 2) Conversazione persiste on close — `closeBTW` fa solo `isOpen: false`, non reset a INITIAL_STATE. Bug: anche il keyboard shortcut toggle resettava stato (riga 164 useBTW.ts). 3) BTW usa `send_message_via_sdk_streaming` (stessa pipeline della chat) — il precedente btw_query faceva HTTP diretto e falliva con OAuth.
- [18:30] (Alek) Fix Quick Loop: tradotte tutte le stringhe IT→EN (Avvia Loop→Start Loop, Ferma Loop→Stop Loop, etc.). Fix altezza input Max Runs vs select Interval (aggiunto `height: 36` fisso). Uniformate icone toolbar: aggiunte classi CSS dedicate `.chat-btw-btn`, `.chat-loop-btn`, `.chat-rewind-btn` con stile neutro `rgba(255,255,255,0.5)` uguale alle altre icone. Aggiunto `toggleBTW` a ShortcutActionId + DEFAULT_SHORTCUTS.
- [19:00] (Alek) Scoperto che Rewind già esiste in `StreamMessage.tsx` (bottone "Undo" inline su ogni messaggio con file changes, chiama `rewindFiles()` via `rewind-files.js`). I componenti spec-004 (RewindTimeline, RewindPreviewModal, useFileCheckpoints) sono duplicati — da rimuovere.
- [19:30] (Alek) Aggiornato SDK: `@anthropic-ai/claude-agent-sdk` 0.2.47→0.2.72, `@anthropic-ai/sdk` 0.72.1→0.78.0. Nessun breaking change — `query()` ancora presente. Nuove API v2 disponibili: `unstable_v2_createSession`, `unstable_v2_prompt`, `unstable_v2_resumeSession` (sessioni persistenti multi-turn), `listSessions`, `getSessionMessages` (lettura storico sessioni). Per ora restiamo su v1 (`query()`) — v2 è `@alpha`/unstable.
- [21:00] (Alek) Fix: abilitati team primitives nativi di Claude Code nella `allowedTools`. Root cause: la lista filtrava via TeamCreate, SendMessage, TaskCreate, TaskUpdate, TaskList, TeamDelete — skill che usano team nativi giravano a vuoto perché i tool erano bloccati. Aggiornati 5 file: `stream-claude.js`, `stream-daemon.js`, `App.tsx` (4 occorrenze). Aggiunti anche TaskOutput, EnterPlanMode, BashOutput, KillShell che mancavano dalla lista frontend. Colori team tools in StreamMessage.tsx. Report da utente Discord (RustyAutopsy): skill funzionava su Claude Code CLI ma non su Quack.
