---
type: diary
project: quack-app
date: 2026-02-14
---
- Brain v2 complete overhaul: two-level architecture (project `documentation/` + global `~/.quack/brain/`)
- Visual Brain window: separate Tauri webview with Timeline, Knowledge browser, Graph, inline Editor
- Content migration: 157 -> 112 files, killed notes/, purged stale patterns and one-liner bugs, slimmed diary entries
- Scope toggle: Project/Global switch in sidebar, reloads all counts and clears editor
- Path display + Finder button: shows brain path with icon to open in macOS Finder
- Global brain fix: recursive directory scan + type inference from folder path for old files without `type` frontmatter
- Knowledge Graph: force-directed with tag-based connections, charge -120, distanceMax 400, common tag cap >20
- Map button + CLAUDE.md button: pinned in sidebar, opens architecture map and project CLAUDE.md inline
- Title bar: TitleBarStyle::Overlay, empty native title, "Quack Brain" as white HTML text in sidebar
- Window dragging: `data-tauri-drag-region` attribute (Tauri pattern, not CSS `-webkit-app-region`)
- Fonts reduced across all components, markdown renderer fixed (backtick handling, paragraph spacing)
- All Italian strings replaced with English
- CLAUDE.md Knowledge Base section: links map.md, 4 critical gotchas, patterns folder (see `pattern-brain-system.md`)
- Rule `use-quack-brain.md` restructured with 3-level Access Chain: CLAUDE.md -> documentation/ -> brain/
- Skill updated: `last_verified` field in template, staleness policy (>3 months = re-verify), access chain in reading instructions
- All 7 gotcha files now have `last_verified: 2026-02-14`
- Nami analysis reviewed: audience filtering (AI vs Human view) parked as v3, CLAUDE.md-Brain connection implemented instead
- Mermaid (.mmd) support: rendering in BrainEditor, zoom/pan, fit-to-container minZoom, trackpad sensitivity fix
- Timeline fix: Brain entries (bugs, decisions, patterns, gotchas) now feed the timeline; dynamic filters hide empty types
- Timeline click-to-open: events open in BrainEditor like Knowledge view
- Brain Breadcrumbs: `// Brain: {slug}` convention for code-to-brain linking, added to quack-brain skill + global rule
- Marketplace updated: quack-brain v3.0.0, use-quack-brain v2.0.0, brain-migrate v1.0.0 (new plugin)
