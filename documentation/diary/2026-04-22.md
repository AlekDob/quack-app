---
type: diary
project: quack-app
date: 2026-04-22
---
- [HH:MM] (Alek) Sidebar agent UX refactor in `RepositoryGroup.tsx`:
  - Rinominato bottone "New Agent" → "Add Agent" nell'header del progetto.
  - **Split active vs dormant agents**: gli agent con almeno una sessione non-done vengono renderizzati come card complete (avatar, DnD, sessioni); gli agent senza sessioni attive (dormant) compaiono in una "dormant chip row" compatta sotto l'elenco. Click su un chip apre `NewSessionModal` preselezionato sull'agent esistente → crea una nuova sessione **senza duplicare** l'agent.
  - **Motivazione**: (1) performance — i dormant non istanziano più `SortableAgent`/`useSessionStore` per-agent né caricano avatar/timer; (2) anti-duplicate — i nomi dormant restano visibili, impedendo all'utente di ricrearli per sbaglio via "Add Agent"; (3) clutter ridotto — active agent prominenti, dormant sempre recuperabili ma non invadenti.
- [11:03] (Auto) Sessione: 1 file letti (~26.3k tok), 1 letture ripetute, ~131.7k tok risparmiati | Brain: 30 gotcha warnings
- [11:59] (Auto) Sessione: 2 file letti (~18.5k tok) | Brain: 6 gotcha warnings
- [12:07] (Auto) Sessione: 4 file letti (~31.7k tok), 1 letture ripetute, ~52.9k tok risparmiati | Brain: 21 gotcha warnings
- [12:18] (Auto) Sessione: 6 file letti (~9.1k tok) | Brain: 5 gotcha warnings
- [12:21] (Auto) Sessione: 1 file letti (~32.3k tok) | Brain: 2 gotcha warnings
- [12:22] (Alek) Chat input: **Tab cicla l'effort level** (low → medium → high → xhigh → max → low), Shift+Tab cicla all'indietro. Implementato in `ChatInput.tsx` come fallback in `handleKeyDown` dopo i check esistenti (autocomplete slash/@ e snippet-expand hanno priorità e fanno `return` prima). Usa `unifiedBarProps.settingsProps.effort/onEffortChange`. Rubbiamo Tab-nav del textarea ma la richiesta è esplicita e l'effort è molto usato per switch rapidi.
- [12:24] (Auto) Sessione: 1 file letti (~3.0k tok)
- [13:09] (Auto) Sessione: 3 file letti (~3.9k tok) | Brain: 1 gotcha warnings
- [14:42] (Auto) Sessione: 12 file letti (~38.5k tok), 2 letture ripetute, ~1.6k tok risparmiati | Brain: 13 gotcha warnings, 1 breadcrumbs
- [15:30] (Alek) **Office View v2 — completati Task 6–15** del plan `docs/superpowers/plans/2026-04-22-office-view-top-down.md` su branch `feat/office-view-v2`. Il risultato: OfficeView v2 montato dietro feature flag (default ON), v1 PixiJS ancora raggiungibile via `localStorage.setItem('quack:forceOfficeV1', 'true')`.
  - **Componenti creati** in `src/components/office/v2/`: `OfficeDuckAvatar.tsx` (avatar circolare con session-dots arco e animazioni bob/pulse CSS), `OfficeRoomCard.tsx` (card HTML absolute-positioned con door-plate, tags, ducks, activity bar), `OfficeZone.tsx` (SVG gradient + resize handles), `OfficeBreakRoom.tsx` (SVG furniture top-down), `OfficeTagFilter.tsx` (pills filter bar), `OfficeActionMenu.tsx` (portato da v1 con `createPortal(document.body)` per escape overflow clipping), `OfficeCanvas.tsx` (root SVG+HTML overlay con pan/zoom wheel + middle-click), `OfficeView.tsx` (compone tutto + gestisce ActionMenu state).
  - **Hook** `useOfficeDrag.ts`: gestisce card+zone drag con threshold 4px, hit-test su zone durante drag per highlight drop-target.
  - **View models** `officeViewModels.ts`: aggregate da `useSessionStore` + `useChatStore` (Map shapes, non Record come suggeriva il plan) → produce `ducksByProject`, `doorPlateByProject`, `busyRatioByProject`, `countsByProject`. Session dot flags derivati da `chatSessions[sessionId].lastMessage.status` (logica replicata dal v1 `OfficeActionMenu.tsx`).
  - **Wiring** `src/views/OfficeTabView.tsx`: dispatcher su `isOfficeV2Enabled()` che mantiene l'offscreen-preserve pattern per il tab inattivo.
  - **Insight / divergenze dal plan**:
    1. `useSessionStore.sessions` è un array `AgentSession[]`, non un Record keyed.
    2. `chatLoadingMap` e `pendingQuestionsMap` sono `Map<string, _>` con chiave = `sessionId`, non `agent.id:session.agent.id` come suggeriva il template.
    3. `lastAssistantCompleted` non esiste su `AgentSession` → derivato da `chatSessions.get(sessionId).lastMessage.status`.
    4. `TerminalInfo.avatar` è un filename (non URL) — passato direttamente a `DuckViewModel.avatarUrl`; se serve resolver, scelta per follow-up.
  - **Stato**: tsc passa su tutto il progetto. Manca manual QA (Task 16) prima di procedere a Task 17 (Floor Plan Overlay) e Task 18-20 (eliminazione v1 + PixiJS). Totale 10 commit in sequenza (`b2e8097` → `6e6ffad`).
