---
type: diary
project: quack-app
date: 2026-04-04
---

- [10:57] (Auto) Sessione: 1 file letti (~645 tok)
- [11:00] (Auto) Sessione: 3 file letti (~41.9k tok), 1 letture ripetute, ~156.6k tok risparmiati | Brain: 25 gotcha warnings
- [--:--] (Alek) UI polish: fixed `.mode-presets-actions` layout — changed from horizontal flex to `flex-direction: column` so description text appears below the reset button instead of beside it (AgentModesSettings.css)
- [--:--] (Alek) UI polish: removed all custom icon colors from `repo-action-row` buttons (Saved Commands teal, Brain purple, Memory cyan, Team orange, Remove dimmed white) — now all icons use uniform white `rgba(255,255,255,0.6)` from `.repo-action-btn` base style. Only the favorite star retains its yellow when active. Files: RepositoryGroup.tsx, MetroStyle.css
- [11:20] (Auto) Sessione: 5 file letti (~11.0k tok)
- [--:--] (Alek) Feature Map Whiteboard v2 — major layout rework: left-aligned column layout (2 cols dynamic), minimal layer headers (no background rects, just label + arrow + count), fit-to-content auto-zoom on first render, collapsible layers (click header to toggle), left-aligned node text, new FeatureMapMinimap component (dots + viewport rect + click-to-navigate). Files: featureMapLayout.ts, FeatureMapCanvas.tsx, FeatureMapView.tsx, FeatureMapMinimap.tsx (new)
- [15:38] (Auto) Sessione: 35 file letti (~49.0k tok), 2 letture ripetute, ~6.1k tok risparmiati | Brain: 3 gotcha warnings
- [15:45] (Auto) Sessione: 16 file letti (~23.8k tok), 1 letture ripetute, ~1.5k tok risparmiati | Brain: 4 gotcha warnings
- [--:--] (Alek) Windows compatibility audit (Feature 024 Code Editor + Feature 026 Feature Map) — 9 fixes across frontend and Rust: `split(/[\\/]/)` cross-platform path splitting, `normalizeToForwardSlash()` DRY utility extracted to `src/utils/platform.ts` (replaces 5 inline copies), `get_node_sdk_path()` `#[cfg]` per-OS branches in code_intel.rs, `get_extended_path()` Windows paths + `;` separator in shell_env.rs, platform-aware `Cmd/Ctrl` shortcut in EditorEmptyState, IDE default path `C:\Program Files` on Windows, Windows font stack (Cascadia Code/Mono, Consolas), `WebkitBackdropFilter` vendor prefix in minimap. Code review score 8.2/10 PASSED. Brain: gotcha-windows-path-separators
- [15:56] (Auto) Sessione: 4 file letti (~12.7k tok) | Brain: 1 gotcha warnings
- [--:--] (Alek) Whiteboard Agent Bridge — migrated annotation+position storage from localStorage to file-based `.whiteboard.json` for agent interaction. New: `whiteboardFileService.ts` (Tauri file I/O), `useWhiteboardFile.ts` (unified hook with 2s polling for external changes), `/whiteboard` skill (list/add-postit/add-group/move/clear/organize). Deleted `useAnnotations.ts`. React polls file every 2s to pick up agent writes. Files: annotationTypes.ts, whiteboardFileService.ts (new), useWhiteboardFile.ts (new), FeatureMapView.tsx, useAnnotations.ts (deleted), .claude/commands/whiteboard.md (new)
- [16:03] (Auto) Sessione: 7 file letti (~18.4k tok), 3 letture ripetute, ~11.0k tok risparmiati | Brain: 2 gotcha warnings
- [16:15] (Auto) Sessione: 3 file letti (~10.3k tok) | Brain: 1 gotcha warnings
- [--:--] (Alek) Whiteboard skill migrated from `.claude/commands/` slash command to proper `.claude/skills/whiteboard/SKILL.md` format (skill-creator process). Added `whiteboard` + `quack-remote` as bundled skills in Rust (`BUNDLED_SKILLS` array in skills.rs) — auto-installed to `~/.claude/skills/` on app startup with semver versioning (v1.0.0). Files: .claude/skills/whiteboard/SKILL.md (new), src-tauri/templates/skills/whiteboard.md (new), src-tauri/templates/skills/quack-remote.md (new), src-tauri/src/skills.rs, .claude/commands/whiteboard.md (deleted), 030-built-in-skills.md (updated)
- [16:29] (Auto) Sessione: 1 file letti (~3.5k tok) | Brain: 1 gotcha warnings
- [16:37] (Auto) Sessione: 2 file letti (~7.6k tok), 1 letture ripetute, ~2.1k tok risparmiati
- [--:--] (Alek) Feature Map search — added search bar in toolbar to filter nodes by title/tags. Non-matching nodes/links dim to 0.15 opacity, auto-pans to single match. Escape clears search. Also wrapped `customPositions` Map in `useMemo` to prevent unnecessary re-renders. Files: FeatureMapCanvas.tsx (searchMatches + auto-pan + opacity logic), FeatureMapView.tsx (search input UI + state), FeatureMapView.css (search bar styles), useWhiteboardFile.ts (useMemo fix)
- [16:52] (Auto) Sessione: 9 file letti (~217.4k tok), 2 letture ripetute, ~41.9k tok risparmiati | Brain: 1 AST hits, 18 gotcha warnings, 12 breadcrumbs
- [--:--] (Alek) Whiteboard rename + shortcut rebind — renamed all user-facing "Feature Map" labels to "Whiteboard" across UI: ActionIcons tooltip/aria-label, tab label (useFeatureMapTab), header title (FeatureMapView), popout loading message (TabPopoutWindowApp). Shortcut changed from Cmd+Shift+M to Cmd+W (shortcutsStorage.ts). Updated feature doc 026-feature-map-whiteboard.md. Internal code identifiers (featureMap, feature-map) kept for stability.
- [17:02] (Auto) Sessione: 20 file letti (~39.8k tok), 1 letture ripetute, ~33.8k tok risparmiati | Brain: 19 gotcha warnings, 3 breadcrumbs
- [17:04] (Auto) Sessione: 2 file letti (~24.3k tok) | Brain: 6 gotcha warnings
- [--:--] (Alek) ChangesPanel refactor — estratti 3 tab content in sub-componenti separati (PendingTab, CommittedTab, HistoryTab) + FileRow componente condiviso + ChangesPanelContextBar + ChangesPanelTabs + hook useChangesPanelState. ChangesPanel.tsx sceso da 627 a 129 righe. Zero regressioni TypeScript. Preparazione per aggiungere 3 nuovi tab (Branches, Worktrees, Remotes).
- [--:--] (Alek) Source Control Sections (033) — expanded Changes Panel from 3 to 6 tabs: added Branches (switch/create/delete), Worktrees (list/add/remove), Remotes (read-only list). Implemented `git_list_remotes` Rust command + `GitRemote` struct. Major refactor: ChangesPanel.tsx 627->159 lines via extraction of PendingTab, CommittedTab, HistoryTab, FileRow, ChangesPanelContextBar, ChangesPanelTabs, useChangesPanelState hook. All files under 300 lines. Color-coded tab badges (purple branches, sky worktrees, pink remotes). Abbreviated tab labels for 6-tab fit. Spec-Kit full workflow (033-source-control-sections).
- [--:--] (Alek) Quack Store — created feature doc `038-quack-store.md` (full architecture, resource categories, install flow, cross-platform notes). Fixed Windows path separator bugs: 3 hardcoded `/` in `downloadSkillDirectory`/`downloadDirectoryEntries` (useMarketplace.ts:86,104,117) replaced with `join()`, removed broken `getHome()` in useBundleOperations.ts and fixed 11 hardcoded path constructions with `join()`. Added error message detail in QuackStoreDrawer install toast for better Windows debugging. Marco (Win11 Pro) reported "Failed to install" for non-agent resources — skills confirmed broken by path bug, commands need console log from Marco to diagnose (code uses `join()` correctly).
- [--:--] (Alek) Git Graph Visualization (034) — Fork-style branch graph in History tab. Extended Rust `git_commit_history` with `%P` (parent hashes) + `%d` (refs) + `--all` flag. Created lane algorithm (`gitGraphLayout.ts`, swimming lanes approach, max 8 lanes) + 8-color palette (`gitGraphColors.ts`). New components: GraphColumn (SVG per-row with dots, pass-through lines, bezier merge curves), RefBadge (colored branch/tag badges), CommitPopover (click detail with hash copy, parent info, glassmorphism). GitTimelineItem gets `graphMode` prop for backward compatibility. Fallback to legacy linear timeline when no parent data. All files under 300 lines, zero TS errors.
- [17:29] (Auto) Sessione: 1 file letti (~928 tok) | Brain: 1 gotcha warnings
- [17:31] (Auto) Sessione: 1 file letti (~990 tok) | Brain: 1 gotcha warnings, 2 breadcrumbs
- [--:--] (Alek) Code Editor header cleanup — added "Mostra in Finder/Explorer/Files" option to EditorIDEDropdown (uses existing `reveal_in_finder` Tauri command, platform-aware label via `getFileManagerName()`). Dropdown chevron threshold lowered from 2+ to 1+ IDE so Finder option is always reachable. Removed ugly SVG terminal icon from IDE main button (text-only now). Replaced hamburger icon (&#9776;) on Outline button with "Outline" text + descriptive English tooltip. Files: EditorIDEDropdown.tsx, EditorHeader.tsx, CodeEditorView.css. Feature doc 024 updated.
- [--:--] (Alek) Git graph visualization utilities — created `src/utils/gitGraphColors.ts` (8 lane colors + `getLaneColor`, `MAX_LANES`) and `src/utils/gitGraphLayout.ts` (core lane algorithm `computeGraphLanes`). Algorithm maintains `activeLanes` slot array (newest-first, max 8 lanes), assigns each commit a lane, resolves first-parent continuity and merge edges, computes `GraphEdge[]` with row/lane indices for renderer. TypeScript strict, zero `any`, all functions under 20 lines (helpers extracted), total 204 lines.
- [18:12] (Auto) Sessione: 1 file letti (~1.6k tok)
- [18:16] (Auto) Sessione: 1 file letti (~3.1k tok) | Brain: 5 gotcha warnings
- [--:--] (Alek) fix: ChangesPanel CPU loop 108% on agent commit — two overlapping infinite re-render loops in useChangesPanelState.ts (reconcileWithGit deps on committedFiles/onRefreshGitStatus) and ChatView.tsx (onAgentCommitDetected inline function in effect deps). Fixed both with ref pattern. Brain: fix-changes-panel-cpu-loop
- [--:--] (Alek) Code review fixes (033+034) — extracted FileStatus/DiffState to types.ts (DRY, was duplicated in 5 files), replaced `void branchName` with `_branchName`, unified Italian strings to English ("No remotes configured", "No commits found"). Review score 8.2/10 PASSED, all 3 fixable minors addressed.
- [18:26] (Auto) Sessione: 10 file letti (~33.1k tok) | Brain: 5 gotcha warnings, 3 breadcrumbs
- [18:30] (Auto) Sessione: 1 file letti (~6.1k tok), 1 letture ripetute, ~6.1k tok risparmiati
- [--:--] (Alek) Side panel compact mode — when user-collapsed, panel shows 44px icon strip instead of disappearing. On hover (80ms debounce), full panel slides in as fixed overlay (420px, z-index 9999) with shadow. Mouse leave (300ms) collapses back. Badges become colored dots in compact strip. Grid col 3 = 44px (was 0px), with 0px fallback for sidebar-hidden/no-agent. Renamed feature doc 035 to `035-side-panel-accordion.md`. Files: SidePanelAccordion.tsx, SidePanelAccordion.css, App.tsx, App.css
- [--:--] (Alek) Side panel compact fix v2 — added `userCollapsed` prop to distinguish user toggle from auto-collapse (tab switch to docs/kanban/feature-map). Compact strip only shows on explicit user collapse; auto-collapsed tabs get `display: none` as before. Replaced `@keyframes` animation with CSS `transition: transform` on always-fixed container (`translateX(calc(100% - 44px))` → `translateX(0)`) for smooth bi-directional slide. Fixed icon/badge positioning: `justify-content: flex-start` + `left: 30px` badge dot (within 44px visible strip). Updated feature doc 035 + gridTemplateColumns logic in App.tsx.
- [--:--] (Alek) Whiteboard multi-select + nested components foundation — (1) Renamed action icon "Feature Map" to "Whiteboard", shortcut Cmd+Shift+M to Cmd+Shift+W with migration in shortcutsStorage (auto-migrates old default). (2) Extended annotationTypes with `parentComponentId?`, `isComponent?`, `ComponentNavigation`, `LassoRect` types — data model ready for nested components. (3) New `useCanvasSelection.ts` hook (lasso rect state, selectedIds Set, toggle/clear). (4) Added 'lasso' AnnotationMode + toolbar button (dashed rect icon) + selection count badge. (5) Lasso hit-test includes both feature nodes (via getPos) and annotations (post-its/groups/images). (6) Blue border (#3b82f6) on multi-selected elements. (7) Shift+click toggles multi-select on any element. (8) Group drag: drag one multi-selected item → all move together (feature nodes via onNodeDrag, annotations via update callbacks). (9) Space+drag = pan in any mode (Figma-style). (10) Middle-click pan. (11) Trackpad: two-finger = pan, pinch = zoom. (12) Spec-Kit 034-whiteboard-nested-components (spec + plan + tasks). Files: annotationTypes.ts, useCanvasSelection.ts (new), FeatureMapCanvas.tsx, FeatureMapView.tsx, AnnotationToolbar.tsx, CanvasPostIt.tsx, CanvasGroupRect.tsx, CanvasImage.tsx, FeatureMapView.css, ActionIcons.tsx, shortcutsStorage.ts, App.tsx, 026-feature-map-whiteboard.md
