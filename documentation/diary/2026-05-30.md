---
type: diary
project: quack-app
date: 2026-05-30
---
- [12:30] (Alek) Fix stato dot sessioni inaffidabile post-WS8 (embedded CLI). Tre cause provate sul codice: (1) il viola era derivato solo da `Notification`/`PermissionRequest`, ma in interattivo piano (ExitPlanMode) e domanda (AskUserQuestion) NON sparano Notification → dot bloccato GIALLO invece che viola; (2) grigio invece di verde quando l'hook `Stop` si perde (marker assistant mancante); (3) viola "a caso" appeso da Notification idle non bloccanti. Fix: registrato `PreToolUse`/`PostToolUse` con matcher `ExitPlanMode|AskUserQuestion` in `ensure_status_hooks_installed` (terminal.rs), inoltrato `tool_name`+`message` in `quack-status.js`, e in `useHookStatusListener.ts` PreToolUse→viola+loading off, PostToolUse→loading on+clear, Notification filtrato su `permission`. Segnale PreToolUse confermato autorevole dalla doc ufficiale Claude Code hooks. KEY INSIGHT: in CLI interattivo l'unico segnale affidabile per "agente in attesa di decisione utente" è PreToolUse(ExitPlanMode|AskUserQuestion), non Notification. Richiede rebuild + re-spawn terminale (registrazione hook è Rust per-progetto). Gotcha: `gotcha-embedded-cli-plan-question-dot-stuck-yellow.md`.
- [12:20] (Auto) Sessione: 11 file letti (~215.1k tok), 4 letture ripetute, ~196.5k tok risparmiati | Brain: 2 AST hits, 38 gotcha warnings, 2 breadcrumbs
