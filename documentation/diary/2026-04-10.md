---
type: diary
project: quack-app
date: 2026-04-10
---

- [11:43] (Auto) Sessione: 1 file letti (~166.1k tok), 1 letture ripetute, ~996.4k tok risparmiati | Brain: 7 AST hits, 35 gotcha warnings
- [12:00] (Alek) fix: split tab disappears when sending message — tab management useEffect was rebuilding entire tabs array on every terminal status change (activeTerminal dependency), dropping code-editor tabs. Guarded full rebuild to only run on agent switch; status-only changes now update just chat tab label/color. Brain: fix-split-tab-disappears-on-send
- [14:00] (Fredric) Cache bisection investigation: tested whether Quack can drop persistent subprocess and use SDK `query()` per message (like the Kite reference app). Ran 5 test scripts with incremental option addition, string vs generator prompts, constant vs changing ideContext, and realistic user timing. KEY FINDINGS: (1) No individual Quack option breaks caching — systemPrompt.append, allowedTools, mcpServers, betas, env all produce >99% hits. (2) AsyncGenerator prompt format works fine. (3) With constant ideContext, query() per message matches persistent subprocess (~120-200 tokens cache create). (4) Changing ideContext causes ~6-7k cache_create per turn on cold starts — this is the gap vs persistent subprocess's ~230 tokens. See `documentation/bugs/investigation-query-per-message-cache-bisection.md`.
- [15:30] (Fredric) Investigated SDK CLI internals — found that `cli.js` injects `git status --short` + `git log --oneline -n 5` into system prompt on every subprocess spawn (memoized within process but recalculated per fresh query()). Initially thought this was root cause, but DISPROVED: the git status theory does NOT explain why changing ideContext (in user message) showed degraded Turns 2-3 while constant ideContext didn't — git status was identical in both tests (agent only said "OK"). The real explanation for Turns 2-3 degradation is **cache slot competition** from running the constant ideContext test 8s before. Turn 4 of the changing ideContext test proves this — it recovered to 99.4% with only 235 tokens created, matching the persistent subprocess. Git status injection remains a concern for production (where agent modifies files) but needs separate validation. Next step: re-run changing ideContext test in isolation (no prior test competing for cache slots).
