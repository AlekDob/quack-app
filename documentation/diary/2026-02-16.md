---
type: diary
project: quack-app
date: 2026-02-16
---
- (Agent Leo) Kanban: added 4th "Human Review" column — virtual column filtering `in_progress` tasks by `chatStore.hasPendingQuestion()`. Purple theme (#a855f7), auto-moves cards when agent calls AskUserQuestion or requests plan approval, auto-returns when user answers
- (Agent Leo) KanbanCard: new "Awaiting Input" purple badge replaces "Ready" badge when `hasPendingQuestion` is true
- (Agent Leo) KanbanColumn: new `pendingQuestionsChecker` prop, `human_review` column color, updated empty state
- (Agent Leo) KanbanView.css: grid from 3→4 columns, `--kanban-review-color` variable, `.kanban-awaiting-badge` with pulse animation
- (Agent Leo) Drag-drop: collision detection, drag-over, drag-end all updated — dropping on `human_review` maps to underlying `in_progress` status. Zero persistence changes
- (Agent Leo) Brain: updated `pattern-kanban-board.md`, created `guide/kanban/human-review-column.md` + `kanban-flow.mmd` Mermaid diagram, added Kanban guide link to CLAUDE.md
- [11:37] (Agent Leo) Manual drag to Human Review: added `manualHumanReviewIds` Set in kanbanStore — tasks dragged manually to Human Review stay there even without `hasPendingQuestion`. Cleared on status change or drag out
- (Agent Leo) Brain sync: updated `pattern-kanban-board.md` (manual drag, OR filtering, data flow diagram), `human-review-column.md` (manual drag section, updated drag behavior table, dual-mechanism architecture note), `kanban-flow.mmd` (manual placement node, OR logic detection subgraph)
