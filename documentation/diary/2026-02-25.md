---
type: diary
project: quack-app
date: 2026-02-25
---

- [08:35] (Alek) fix: Ollama offline in build ma connesso in dev — aggiunto `http://localhost:*` al `connect-src` della CSP in `tauri.conf.json`. In dev mode Tauri bypassa la CSP, in build la applica rigorosamente bloccando il `fetch()` verso `http://localhost:11434/api/tags`.
- [18:30] (Alek) feat: aggiunto bottone "See more / Show less" inline nei messaggi utente troncati nella chat. Prima solo il messaggio sticky aveva expand, ora tutti i messaggi utente con testo lungo mostrano il link. Feature request dalla community Discord (Asa).
- [13:30] (Alek) fix: automation job fired repeatedly every 30s — nextRunAt was never advanced after execution. Added getNextFireTime() call in fireJob().
- [13:40] (Alek) fix: new automation job skipped current minute — getNextFireTime() always added +1 min, so creating a job at 13:20 for 13:20 would schedule tomorrow. Added `inclusive` param (default false); addJob/initialize/updateJob use inclusive=true, fireJob uses false.
- [14:30] (Alek) fix(architecture): automation tick listener was inside AutomationView (tab-scoped) — jobs only fired with Automation tab open. Moved scheduler tick + fire logic to App.tsx useEffect (always mounted). AutomationView now UI-only. Also fixed: initialize() no longer overwrites future nextRunAt, CronPresetInput "Every 6 hours" no longer co-highlights with "Every day".
- [14:50] (Alek) fix(automation): job fired with wrong agent personality — inject_personality_to_claude_md was never called before creating the session. Added personality injection in both the global scheduler tick handler and handleAutomationFireJob (Fire Now button).
- [15:30] (Alek) fix(automation): jobs STILL re-firing (110 sessions created!) — race condition: async updateJob hadn't completed before next tick read stale state. Root cause: nextRunAt was advanced inside async IIFE but next tick read store before async completed. Fix: 3-layer defense — (1) synchronous `claimJobForFiring()` lock in store, (2) advance nextRunAt synchronously via `setState` before any async, (3) set lastRunStatus='running' synchronously. Also made `initialize()` idempotent with `initialized` flag to prevent concurrent store reloads from disk.
- [09:30] (Alek) feat(pwa): pull-to-refresh gesture (touchstart/move/end), refresh button in header, stealth auto-refresh every 30s. Added CSS for typing-dots animation, refresh-bar progress indicator.
- [09:45] (Alek) feat(pwa): real Quack icon for PWA — generated full-bleed PNGs (180/192/512) from icons/icon.png via PIL, served via include_bytes!() routes in remote_dashboard.rs. iOS requires square images without rounded corners (it applies its own mask).
- [10:00] (Alek) fix(pwa): removed chatInput.focus() that auto-opened keyboard on chat open. Added enterkeyhint="send" and autocomplete="off" to chat input.
- [10:15] (Alek) feat(pwa): execute now opens chat immediately with virtual session + user prompt. Typing indicator (3 bouncing dots) shows while agent works. Polling updated to track session status changes.
- [10:20] (Alek) feat: created `quack-remote` skill (`~/.claude/skills/quack-remote/SKILL.md`) — full API reference for controlling Quack from any project via REST. Covers all endpoints, common workflows, WebSocket events.
- [10:30] (Alek) fix(types): added `isResumed?: boolean` to ClaudeSystemEvent in types.ts — was blocking production build.
