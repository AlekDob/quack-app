---
type: diary
project: codetta
date: 2026-07-16
---
- [08:54] (Alek) Fixed the "app freezes / 100% CPU on launch and chat switch" loop. Stack-sampled the pegged `target/debug/Quack` process → main thread stuck 100% in `provider_list_sessions → list_claude_sessions → summarize_jsonl → serde_json::from_str`. Root cause: the CC session drawer poll (`AIChatPanel` diskHydrate effect, 12s interval) calls `guessClaudeSessionId` whenever a chat has no saved `claudeSessionId`, which runs the **synchronous** `provider_list_sessions` Tauri command on the main thread — parsing the ENTIRE `~/.claude/projects/<proj>` dir line-by-line. With `~/.claude` at 1.1 GB / 1298 files (single files up to 98 MB), one call blocks the UI for seconds; on a failed guess it re-parsed every 12s forever. Fix (3 layers): (1) `provider_list_sessions`/`provider_load_session` → `async` + `spawn_blocking` (off the main thread — no UI freeze even cold); (2) per-file summary cache in `summarize_jsonl` gated on (mtime,size) so inactive JSONL is parsed once ever (mirrors the usage-monitor freeze fix, feature 019); (3) frontend `guessAttemptRef` so the guess runs once per distinct turn count, not every poll. Verified with a stack-sampling + CPU probe: `Quack` Rust 98%→5%, webview 83%→6% at idle. KEY INSIGHT: on macOS the Tauri webview is a separate `com.apple.WebKit.WebContent` process — when the Rust `Quack` process itself pegs a core, the culprit is backend/IPC, not React; a JS-only render/effect probe will (correctly) show nothing. `sample <pid>` on the Rust process is the fastest way to locate a backend main-thread block.
- [08:54] (Alek) NOTE / follow-up latent bug found while investigating: `src-tauri/src/watcher.rs` watches each workspace root with `RecursiveMode::Recursive` and **no ignore list** (target/, node_modules/, .git/, dotdirs). Any write under a root (builds, dev-server caches, git ops) floods `fs:event` → `gitStatusStore` runs `git status` per workspace + `FileTree`/`EditorPane` rescans → webview CPU. Not the freeze fixed above, but worth adding an ignore filter in the watcher.
- [09:07] (Alek) Added a gradual chat/session-switch loader (feature 075) so switching doesn't feel janky now that it's fast. Reworked `chatSwitch.ts`: the veil shows immediately on `pulseChatSwitch`, stays a graceful MIN floor (240ms) even if hydration is instant, ends via `endChatSwitch` (from `onHydrated`) after the floor, with a 1000ms cap fallback. `ChatSwitchVeil` now fades (translucent `--bg` wash + `backdrop-filter: blur` + slim indeterminate top bar) and stays mounted through the fade-out; hosts render it unconditionally with a foreground-gated `active` prop (`switching && visible`) so background panels never flash it. Triggers (`AgentModeShell.selectSession`, `AIChatsRail.focusChat`) now veil on EVERY switch, not just cross-workspace. KEY INSIGHT: the min-visible floor must live in the pulse store, not the component — otherwise fast hydration drops the veil in <100ms and it flashes; and the veil must stay mounted through fade-out (own `mounted` state) since the host flag flips off instantly.
- [09:30] (Alek) Switch loader now GLOBAL + always visible. The per-host veil showed on same-project switches but not cross-project — mid-switch the old workspace host unmounts and the new one isn't `visible` yet, so nothing rendered the veil and it felt stuck. Fix: one `<ChatSwitchVeil global />` at the app root (`App.tsx`), `position: fixed` full window, driven purely by `useChatSwitching()`; removed the three per-host mounts. Raised `MIN_VISIBLE_MS` 240→320 and shortened fade 240→160 so even an instant load clearly shows the loader (explicit ask: "un minimo si deve vedere sempre"). KEY INSIGHT: cross-project transitions have a mount gap where no chat host is both mounted and visible — a per-host overlay can't cover it; a single root overlay driven by the global pulse is the only place that always paints.
- [09:40] (Alek) Agent Hub lifecycle simplified (`009`): context menu is now Rename · Mark done/Reopen · **Delete** (confirm, removes disk transcript + tab). Dropped **Archive** from the menu and **Archive all** from Done bulk — Done + Archive overlapped. Legacy `archivedAt` chats still show in the Archived section; click focuses without auto-unarchive; sending a message unarchives. `deleteChat` → `deleteSession` + `closeAIChat`.
- [09:40] (Alek) Preset model on new chat tab (`062`): fixed Jack/Team model tier (e.g. Opus) being overwritten by Sonnet on **New chat** tab. Two bugs: (1) fresh-tab `useEffect` never called `applyJackDefaultsIfConfigured()`; (2) `applyPreset` skipped model when `selected === ""` before discovery — added `agenticProviderForPresetApply()` to fall back to first agentic CLI.
- [09:40] (Alek) Model picker refresh UX (`025`): `.is-hydrating` only when catalog empty; `.is-refreshing` keeps cached rows visible during background CLI fetch. `warmPickerCatalogs()` pre-imports provider chunks; `addNewAIChat` now pulses switch veil (`veil: true`) so new tabs don't feel stuck during panel mount.
- [09:45] (Alek) Agent Hub Done pile (`009`): removed separate **Archived** section — legacy `archivedAt` chats merge into **Done** with the same search/preview (10 default, 30 search cap). Collapsed rail caps at 10 done dots. Context menu: Reopen for done pile, Mark done for live rows. `hubPrefs` first-run collapse is `"done"` only.
- [10:05] (Alek) Closing the "new chat also feels blocked" thread from earlier today. Stack-sampled the webview mid-freeze while creating chats: main thread parked in a **synchronous IPC round-trip** (`WebKit::WebPage::rootViewToScreen` → `sendSync` → blocking wait), triggered by accessibility notifying a selection change during a render pass — i.e. the webview was waiting on the Rust/UI side while something rendered synchronously and heavily. That points squarely at `ModelPickerPopover.openPicker`'s `flushSync(...)` (forced a synchronous full render of the model catalog/favorites/groups on every open) — and a **second, concurrent** Quack session (Cursor-backed agent, same working tree) found and fixed exactly that in the interim: `flushSync` removed from `ModelPickerPopover` (025), the Jack/Team preset model-tier bug on fresh tabs fixed (062 — `applyJackDefaultsIfConfigured()` was never called on brand-new tabs, and `applyPreset` skipped the model entirely while `selected === ""` pre-discovery), plus the switch-veil-on-new-chat change I'd started (`addNewAIChat.ts`, `veil: true`) landed in the same commit since we share a working directory. Verified post-fix: no `flushSync` left in `ModelPickerPopover.tsx`. KEY INSIGHT: two Quack chat sessions on the same project share one git working tree — concurrent agents can pick up and commit each other's in-progress edits; check `git log` before assuming a fix is still needed.
- [10:15] Chat perf (`076`, also `043`/`001`/`064`/`032`): DONE/archived chat hosts now **unload** when hidden (`chatHostMount.ts`) instead of sticky-mounting forever; live chats stay mounted for multitask. Transcript hydrate is **lazy** — `chat_store_load_workspace` returns index only, `chat_store_load` pulls one body, boot warm-loads live session ids, `ensureSessionLoaded` on panel open, `dropCachedSessionBody` when DONE unmounts. Message virtualization still a follow-up. Feature doc: `076-chat-lazy-hydrate-done-unload.md`.
- [10:25] (Alek) **Plan mode = explore bypass** — `find … -exec grep` still carded because `\bexec\b` matched `-exec`. Fix in `ClaudePermissionOverlay`: `exec`/`eval` only as command heads; don't split on escaped `\;`. Writes + `ExitPlanMode` still card. Composer Plan chip desc updated.
- [10:35] (Alek) **ExitPlanMode / AskUserQuestion "not available"** — Jack's `ToolSearch select:ExitPlanMode` failed, then he pasted the plan as prose (no Build card / ask dock). Root cause: CC ≥2.1.72 defers those tools behind ToolSearch; `select:…` often returns "No matching deferred tools" (upstream #45294 / #49843). Fix: `ENABLE_TOOL_SEARCH=false` in `apply_clean_env` + prompt line forbidding ToolSearch for those two. Needs a **new** CC spawn (rebuild + new turn). Docs: `014`, `015`, `073`.
