---
type: diary
project: quack-desktop
date: 2026-06-29
---
- [18:30] (Alek) **Feat: aprire immagini e PDF in un tab di anteprima dall'explorer** (prima toast "File appears to be binary" → niente). Cliccando un PNG/JPG/GIF/WebP/BMP/ICO/AVIF o un PDF nell'albero file ora si apre un **tab read-only**: immagine inline (click per fit↔dimensione reale), PDF nel viewer nativo della webview (`<iframe>`). KEY DECISION: SVG **resta editabile come testo** (è XML sorgente), solo i raster + PDF vanno in preview. Nuovo `src/mediaPreview.ts` (`mediaKindOf(path)` = unico punto di verità per la classificazione) + `src/components/MediaPreviewPane.tsx` (legge i byte da disco come data: URL via `read_image_data_url`, riusato — non più solo "image"). GOTCHA architetturale: il tab dei file richiede `ws.files[path]` per montarsi/persistere, ma `readFile` rigetta il binario → in `openFile` (e nella restore di hydration) i file media ricevono un **buffer sentinella vuoto** (`contents===original`), così il tab esiste, traccia come non-dirty e `saveFile` fa no-op (Ctrl+S non può azzerare il file). Il `MediaPreviewPane` legge da disco per conto suo, indipendente dal buffer. Portal in `WorkspaceShell` sceglie `MediaPreviewPane` vs `EditorPane` per path. Backend: aggiunti mime `bmp`/`ico`/`avif`/`pdf` a `read_image_data_url`; PDF via data: URL (CSP=null lo consente) invece di abilitare l'asset protocol Tauri (meno config). `tsc --noEmit` + `cargo check` verdi. Doc `features/017-media-preview.md`.
- [17:30] (Alek) **Feat: allegare immagini nella chat (Cmd+V + drag dal Finder), fino a 10, con compressione + zoom modale**. "Strada A" per Claude Code: niente refactor multimodale di `ChatMessage`, niente protocollo CLI — le immagini vengono compresse client-side, salvate su disco e il loro **path** viene iniettato nel prompt (`ccTurnContext`, stesso pattern di `attachedFiles`) così CC le legge col tool **Read**. KEY INSIGHT (ribalta l'analisi pessimista iniziale): la CLI di CC NON ha `--image`, ma il Read tool rende i file immagine visivamente → basta passargli il path. Nuovo `src/imageAttach.ts` (compressione canvas: long-edge ≤1568px WebP q0.82 + thumb 320px; `compressAndSave`/`attachFromBlob`/`attachFromPath`; bus drop `registerChatDropZone`/`tryRouteDropToChat`). Backend: 2 comandi in `fs_ops.rs` — `save_image_attachment` (base64→`<temp>/quack-attachments/`, fuori dal repo) e `read_image_data_url` (full quality on-demand per lo zoom, così localStorage tiene solo path+name+thumb). `ChatMessage.images?` additivo/opzionale (zero impatto sugli altri provider). GOTCHA drag: Tauri intercetta i drop HTML → l'unico segnale è `onDragDropEvent` window-level in `App.tsx` (già apre i file come tab); ora hit-testa la `position` (÷devicePixelRatio) contro il rect del pannello chat e instrada gli image-drop al composer PRIMA dell'open-as-tab. Gating: solo `claude-code` (altri provider = toast "solo con Claude Code", Strada B fuori scope). Trade-off temp-dir: in Ask mode il Read può mostrare 1 card di permesso per immagine (silenzioso in Auto/Bypass). `tsc --noEmit` + `cargo check` verdi. Doc `features/016-image-attachments.md`.
- [18:00] (Alek) **Polish allegati immagine: il Read di un'immagine è identificabile + il drawer mostra la foto**. Testato e funzionante l'allegato (paste/drag → CC legge col Read tool). Tre rifiniture: (1) il chip del tool quando CC fa `Read` di un'immagine ora ha icona `image` + tint **teal** (`ai-tcall-image`, nuovi token `--img`/`--img-bg` in dark+light) — distinto dall'arancione `Skill` e dal grigio chrome; rilevato via `READ_NAMES` + `isImagePath(fileRef)` in `chatToolRender.tsx`. (2) Cliccando il chip, il `ToolResultDrawer` ora mostra **l'immagine** invece del placeholder testuale `[image]` che CC restituisce: nuovo campo `ToolDrawerData.imagePath` → il drawer la carica come data: URL via `read_image_data_url` (comando già esistente). (3) Fix naming: i file non sono più `image.png.jpg` — `compressAndSave` strippa l'estensione originale prima di ri-encodare. `tsc --noEmit` verde, nessuna modifica Rust. Doc 016 aggiornata.
- [16:00] (Alek) **UX: il chip del tool `Skill` ora mostra il nome della skill** (`src/components/chatToolRender.tsx`). Prima il chip diceva solo "Skill" senza dettaglio: il tool `Skill` passa il nome nell'arg `skill`, ma né `primaryToolDetail` (transcript) né `toolDetailFor` (ticker live) cercavano quella chiave. FIX: aggiunto `"skill"` in cima a entrambe le liste di chiavi → ora si legge "Skill feature-creator" sia durante l'esecuzione sia nel transcript. Cambio chirurgico, `tsc --noEmit` verde.
- [14:00] (Alek) **Fix: il permission-mode (Auto / Auto-edit) non veniva MAI rispettato — chiedeva permessi per tutto anche in automode**. ROOT CAUSE: Quack tiene SEMPRE attivo l'hook `PreToolUse` (scelta voluta), quindi l'unico giudice frontend allow/deny è `ClaudePermissionOverlay`; ma `shouldAutoAllow` guardava solo privacy-gate, read-only e regole always-allow salvate — **non leggeva mai `ccPermMode`**. Quindi `--permission-mode acceptEdits` passato alla CLI era inerte (l'hook intercetta prima che la CLI applichi l'auto-accept). Inoltre `auto`/`dontAsk` NON sono permission-mode reali della CLI (valida solo default|plan|acceptEdits|bypassPermissions): erano etichette orfane, e lo stato di default era `"auto"` → il bottone diceva "Auto" ma si comportava da "Ask". FIX (4 file): nuovo `src/permModeStore.ts` (ponte module-level mode↔overlay, keyed by CC session id + cwd, pattern `aiTaskStore`); l'overlay ora ha `modeAutoAllow` — `auto`=allow tutto (Bash incl., privacy-gate+AskUserQuestion-redirect restano), `acceptEdits`=solo Edit/Write/MultiEdit/NotebookEdit; `AIChatPanel` pubblica il mode nello store + lo persiste (`lcp.claudeCode.permMode`) e ora **default = Ask (null)**, non più "auto" (fix mismatch label/comportamento + niente full-allow di default su install nuova). Aggiunto **mode Bypass** = `bypassPermissions` reale di Claude Code (prima RIFIUTATO dal backend): `claude_code.rs` lo whitelista, salta l'installazione dell'hook e bypassa la refusal-guard (è opt-in esplicito) → zero card, niente privacy gate. Il backend ora passa alla CLI solo `plan`/default (mai l'invalido `auto`). Rimossi i riferimenti orfani a `dontAsk`. `tsc` verde, `cargo check`+4 test verdi.
- [15:30] (Alek) **Repo migration: questa cartella ora è Quack (origin → quack-app)**. Questo codebase nasceva sul fork upstream `getcodetta/codetta` (il vecchio origin), ma `AlekDob` non aveva permessi di push lì (403). Decisione di Alek: questa repo *è* Quack, non più codetta. PROBLEMA: la repo target `AlekDob/quack-app` ha una storia git **completamente scollegata** (è l'altro progetto, embedded-CLI/xterm; il commit-radice `9a63a4b` di codetta non esiste in quack-app) → un push su `main` sarebbe stato rifiutato, e il force avrebbe distrutto il main di quack-app. SOLUZIONE non distruttiva: pushato `main` locale su un branch NUOVO `quack-1.0` di quack-app (il main di quack-app e gli altri branch restano intatti). Poi repoint dei remote: `origin` (getcodetta/codetta) rinominato e infine **rimosso**; `quack-app` rinominato in `origin`; local `main` ora traccia `origin/quack-1.0` (allineato 0/0). GOTCHA da ricordare: `main` locale pusha su `quack-1.0` (mismatch nome voluto); le due storie codetta↔quack-app restano separate. Nessuna modifica al codice — solo operazioni git.
- [15:00] (Alek) **Fix: in Bypass mode chiedeva ancora i permessi**. BUG nella mia implementazione precedente di Bypass: avevo fatto girare la CLI con `--dangerously-skip-permissions` + hook NON installato. Due errori di assunzione: (1) `--dangerously-skip-permissions` **non disattiva gli hook PreToolUse** (salta solo il permission-system nativo) → l'hook spara comunque; (2) "non installare l'hook" non rimuove quello già scritto in `.claude/settings.local.json` da run precedenti. Quindi l'hook continuava a POSTare → card. FIX (coerente con l'architettura "overlay = unico giudice"): Bypass ora è gestito **nell'overlay**, non sul backend. `ClaudePermissionOverlay` allow-a tutto se `getPermModeFor(req) === "bypassPermissions"`, **prima** del privacy-gate (differenza documentata vs Auto, che lo mantiene). Backend: rimossa tutta la logica `bypass` (skip-hook + skip-refusal) → ogni mode tiene l'hook attivo, tutti enforced GUI-side. BONUS: l'overlay legge il mode live da `permModeStore` → cambiare in Bypass **a metà run** ha effetto dal tool successivo (prima no). `tsc` + `cargo check` verdi. Doc 015 corretta (la riga Bypass diceva il falso "hook OFF").
- [14:20] (Alek) **Plan mode: troppi permessi → auto-allow del Bash di sola lettura**. CHIARIMENTO ARCHITETTURALE (domanda di Alek): le regole dei permessi NON sono ereditate da Claude Code né passano da API — l'hook `PreToolUse` (sempre attivo) scavalca il permission-system della CLI e l'unico giudice è `ClaudePermissionOverlay`. I Read erano già auto (`READ_ONLY_HOOK_TOOLS`); ciò che chiedeva in plan mode era il **Bash** (sempre dietro card per design). FIX: `modeAutoAllow` ora gestisce `plan` → auto-allow Bash solo se `isReadOnlyBash`: head ∈ safelist read-only (ls/cat/grep/rg/tree/stat/wc/head/tail/echo/pwd/which/du/df/sort/uniq/cut/basename/dirname/realpath…) oppure `git` + subcomando read-only (status/log/diff/show/blame/ls-files/rev-parse/describe), e nessun `>`/`|`/`;`/backtick/`$(` (riuso `BASH_CHAIN_RE`). Safelist volutamente stretta: esclusi i runner (env/xargs/sudo), `sed -i`, e git branch/tag/config/remote (hanno forme mutanti). La CLI in plan mode già blocca le scritture, quindi l'unico rischio era il side-effect Bash → coperto. `tsc` verde. Doc aggiornata in `features/015-claude-permission-mode.md`.
- [12:30] (Alek) **Feat: icone per tipo di file nell'explorer (stile VS Code, ma monocrome)**. Prima l'albero usava un'unica icona `file` fissa; ora ogni tipo ha una forma distinta. Nuovo modulo `src/fileIcons.ts` con `fileIconName(name)` — singolo punto di verità: mappa per nome esatto (`package.json`→braces, lock files→lock, dotfile git→git-branch) e per estensione (sorgenti→file-code, html→globe, css→hash, json→braces, yaml/toml/ini→settings, md/txt/pdf→file-text, sh→terminal, immagini→image, secrets→lock), fallback `file`. Aggiunte 4 icone al registry `Icon.tsx`: `braces`, `image`, `file-code`, `lock` (stesso linguaggio stroke/currentColor). KEY DECISION (con Alek): icone monocrome distinte, NON colorate stile Seti — rispetta la regola brand "chrome monocromo, colore solo su badge progetto + stati semantici". Cambio chirurgico in `FileTree.tsx`: `name={entry.is_dir ? "folder" : fileIconName(entry.name)}`. `tsc --noEmit` verde.
- [10:30] (Alek) **Doc: due nuove feature-doc.** `014-claude-code-bridge.md` (bridge CC: spawn/stream/attach/stop, modello a 4 thread, `kill_process_tree`, mappa `children` con pid invece di `Child` lockato — copre il fix Stop) e `015-claude-permission-mode.md` (la feature permission-mode del 14:00: `permModeStore` come ponte mode↔overlay, modi Ask/Plan/Auto-edit/Auto/Bypass). Aggiornata la Knowledge Base in `CLAUDE.md`.
- [10:00] (Alek) **Fix: premere Stop su una chat Claude Code rallentava/freezava l'app** (`src-tauri/src/claude_code.rs`). Due bug sommati. BUG 1: il thread `wait()` faceva `child.lock().wait()` tenendo il `MutexGuard` per TUTTA la durata del processo → `claude_code_kill` (e il watchdog `try_wait`) si bloccavano dietro quel lock e non riuscivano mai a uccidere finché la run non finiva da sola. In pratica lo Stop su run lunghe non funzionava. BUG 2: `Child::kill()` manda SIGKILL solo al processo `claude` padre; i sottoprocessi tool (Bash, ricerche file tipo `bfs`/`find`) restavano ORFANI (reparented a launchd) e continuavano a girare → un `bfs` a metà scansione ricorsiva resta a ~524% CPU e affossa tutto il Mac, Quack incluso. KEY INSIGHT: `bfs` (breadth-first find) visto in Attività NON era nel repo — era un figlio orfano di Claude Code. FIX: (1) `claude` lanciato in un process group dedicato (`cmd.process_group(0)` unix / `CREATE_NEW_PROCESS_GROUP` win); (2) nuova `kill_process_tree(pid)` che fa `libc::kill(-pid, SIGKILL)` (intero gruppo) su unix e `taskkill /T /F` su win; (3) la mappa `children` ora tiene solo il `u32` pid (non più `Arc<Mutex<Child>>`) → kill/watchdog non lockano mai il `Child`, di cui è proprietario solo il thread `wait()`; (4) watchdog coordinato con un `AtomicBool finished`. Aggiunta dep unix `libc`. `cargo check` verde. Gotcha salvata in `documentation/claude-stop-process-group.md`.
- [15:50] (Scott — Builder) **Feat: Whiteboard (organigramma agente↔skill + .md operativo)**. Nuova editor tab "Whiteboard" per workspace con 3 sotto-tab (Overview / Organigramma / Workflows); drag-and-drop di una chip-skill sopra un agent scrive `skills:` nel frontmatter del `.md` sottostante; × sulla chip rimuove; output .md con sezione "Workflow" per agente copiabile o salvabile in `.codetta/whiteboard.md`. DECISIONI CHIAVE: (1) **nuovo tab kind** in `parseKey` (`wb:<wsId>`, persistente, sopravvive al restart tramite whitelist in `clean()`, monitored come `ai:`) invece di un pannello laterale o modal — il tab entra nella normale tab-bar dell'editor; (2) **frontmatter extend** invece di sidecar: aggiunto `skills: string[]` al `SubagentDef` con parser block + inline + JSON-list (`frontmatterList`), e **`path: string | null`** per consentire la scrittura. (3) **`frontmatter.ts`** nuovo modulo (read+write) — il write è una patch chirurgica (regex + replace-in-place del blocco `skills:`) per evitare una dipendenza YAML; preserva il body markdown verbatim. (4) DnD HTML5 nativo (funziona in chrome UI, il bug Monaco-DnD non ci tocca); `dataTransfer.setData("text/plain", slug)` (costante `SKILL_DT_MIME` condivisa) → drop su un `whiteboard-org-agent` aggiunge lo slug; × sulla chip dentro l'agent rimuove. Wrapper chip = `<div>` (affidabilità dragstart su WKWebView). (5) `renderWhiteboardMd` produce un runbook-style doc con sezioni Project/Global/Unassigned; live-anteprima via `MarkdownPreview` esistente + bottoni Copy (`navigator.clipboard`) / Save (`fs.writeFile` di `.codetta/whiteboard.md`, dir creata se manca). (6) Hint sulla command palette ora prioritario su accel (`c.hint ?? c.accel`); aggiunto `CommandSpec.hint?` + comando `view.open_whiteboard` categoria View. **Verifiche** (tutte verdi): `npm run build` (TS strict, no any); `patchFrontmatterList` 18 unit test (insert/replace/remove/synthesise/round-trip/key-in-middle); E2E drag-and-drop simulato su agent reale (link→unlink→salva→read-back); `vite dev` parte pulito (serve index.html Quack). Nuovi file: `src/frontmatter.ts`, `src/whiteboardMd.ts`, `src/components/WhiteboardPane.tsx`, `src/components/WhiteboardOrganigramma.tsx`. Modificati: `src/store.ts`, `src/subagents.ts`, `src/components/Icon.tsx` (+`whiteboard` icon), `src/components/PaneNode.tsx` (+`isWhiteboard` dispatch), `src/components/WorkspaceShell.tsx` (+portal mount), `src/components/CommandPalette.tsx` (hint priority), `src/actions.ts` (+`view.open_whiteboard`), `src/App.css` (+regole `.whiteboard-*`). Doc: `features/018-whiteboard-organigramma.md`.
- [16:10] (Scott — Debugger) **Fix DnD whiteboard: usavo MIME custom `"text/skill-name"` su dataTransfer**. WKWebView (Tauri/macOS) lo filtra da `dataTransfer.types` su `dragover` → guard `types.includes("text/skill-name")` false → `preventDefault` non scatta sull'agent → browser non lo riconosce come drop target → `drop` mai fired → canvas muto. Primo tentativo: costante `SKILL_DT_MIME = "text/plain"` condivisa da chip.dragstart e agent.dragover/drop, + wrapper chip da `<span>` a `<div>` per affidabilità dragstart su inline in WKWebView. Build verde.
- [16:20] (Scott — Debugger) **Followup: "nada" → secondo strato WebKit + click → open .md**. ROOT CAUSE VERA, dietro il primo fix: anche il `dragover` su WebKit può esporre `dataTransfer.types` **VUOTO** per security policy (anti data-leak al drop target). Quindi anche la guard su `types.includes("text/plain")` ritornava false → `preventDefault` mai chiamato → `drop` mai fired. FIX ROBUSTO in `WhiteboardOrganigramma.tsx`: drop della guard su `types` durante `dragover`/`dragenter`. `e.preventDefault()` sempre, `dropEffect = "link"` sempre; la validazione "è una skill?" si fa solo in `drop` con `dataTransfer.getData("text/plain")` (qui sì, il valore è leggibile — la security policy riguarda l'esposizione del *tipo* durante dragover, non il dato al momento del drop). Se `getData` ritorna stringa vuota → no-op silenzioso. Risultato: l'agent È SEMPRE un drop target valido nel momento in cui l'utente ci passa sopra col chip. **+ Feature "click su agent apre .md"** richiesta da Alek: `useStore.getState().openFile(wsId, agent.path)` su `onClick` dell'AgentNode, con `role="button" tabIndex={0}` per accessibilità (Enter/Spazio aprono); `e.stopPropagation()` sul × dei linked-chip così l'unlink non apre anche il file. CSS: `.whiteboard-org-agent` ora `cursor: pointer` + `:hover` (border → `--border-strong`) + `:focus-visible` (outline `--accent`). Doc 018 da aggiornare col secondo strato di gotcha.
- [16:30] (Scott — Debugger) **Log diagnostici `[wb-dnd]` sul flow DnD whiteboard (TEMPORANEI)**. Aggiunti `console.log` prefissati `[wb-dnd]` in 4 punti chiave di `WhiteboardOrganigramma.tsx`: (1) `SkillChip.dragstart` → log "dragstart skill=X" + read-back di `getData("text/plain")` in setTimeout(0) per confermare cosa il browser ha tenuto; (2) `AgentNode.dragover` → log continuo (1 per movimento) "dragover agent=X"; (3) `AgentNode.dragenter` → log al primo ingresso con `types=[...]` serializzato; (4) `AgentNode.drop` → log finale con `types` + `data` (lo slug letto). Obiettivo: dopo hot-reload + un drag di prova, la console di WebKit mostra esattamente DOVE il flow si rompe: se vedo solo `dragstart` ma niente `dragenter` → l'agent non riceve l'evento (overlay/pointer-events); se vedo `dragenter` ma niente `drop` → è ancora un problema di `preventDefault`. NOTA: questi log vanno **rimossi** appena scoperta la causa — sono diagnostici, non di produzione. Filtro console: cercare `[wb-dnd]`.
- [16:40] (Scott — Debugger) **ROOT CAUSE trovato + fix definitivo: DnD whiteboard → mouse events, niente HTML5**. Dopo i log `[wb-dnd]` l'evidenza è cristallina: il `dragstart` sulla chip fire, il `dragend` alla fine pure, ma tra i due **ZERO eventi `dragover`/`dragenter`/`drop` su qualsiasi elemento del DOM**. Conferma empirica di un bug noto di Tauri 2 con `dragDropEnabled: true` (default): WKWebView delega il drag al layer nativo Tauri che lo rivendica per i drop OS-level, e gli eventi interni al webview non vengono mai dispatchati. FIX DEFINITIVO in `WhiteboardOrganigramma.tsx`: abbandono completo di HTML5 DnD sul whiteboard, sostituito da pattern mouse puro. (a) `SkillChip.onMouseDown = startDrag(slug, x, y)` — registra `window.mousemove`/`mouseup` con flag `active` per cleanup; (b) `startDrag` mantiene `startClientX/Y` e aspetta `hypot(dx, dy) >= 4px` prima di iniziare a hit-testare (mouse-down senza movimento = click, non drag); (c) `findAgentAt(x, y) = document.elementFromPoint(x, y).closest('[data-wb-agent]')` — gli agent hanno `data-wb-agent={name}` per consentire la ricerca ricorsiva; (d) `mouseup` su un agent → `linkSkill(agent, slug)`, altrove no-op silenzioso. Rimosso: `SKILL_DT_MIME`, tutti gli `onDrag*` handler, `[wb-dnd]` useEffect diagnostico. Click-on-agent / unlink / × restano invariati (già erano `onClick`). Build verde, file 392 linee. Doc 018 aggiornata col nuovo pattern + sezione gotcha riscritta (Tauri bug al primo posto).
- [16:50] (Scott — Builder) **Drag ghost visivo sul whiteboard DnD (mouse-based)**. Funzionava ma senza ghost-follow-cursor sembrava non fare nulla (solo bordo agent che si accendeva, troppo debole). FIX visivo in `WhiteboardOrganigramma.tsx` + `App.css`: (1) `SkillChip.onMouseDown` ora passa `e.currentTarget` al callback; (2) `startDrag(slug, x, y, sourceChip)` clona la chip, appende a `document.body` come `<div class="whiteboard-drag-ghost">` con `position:fixed pointer-events:none z-index:9999`, posizionato a `(e.clientX, e.clientY)` e `transform: translate(-50%,-50%) scale(1.05)` (il `-50%,-50%` lo centra sul cursore); (3) la chip originale riceve `.whiteboard-skill-chip--dragging` (opacity 0.4) per il "lift" effect, rimossa in cleanup; (4) ogni `mousemove` aggiorna `ghost.style.left/top` 1:1 con il cursore; (5) `mouseup` aggiunge `.whiteboard-drag-ghost--fading` e rimuove il nodo dopo 120ms (transizione CSS), così il drop senta "fine" anche senza agent hit. CSS: classe `--dragging` sull'originale (opacity + cursor grabbing), classe `--fading` sul ghost (opacity 0, scale 0.9, transition 120ms). `pointer-events:none !important` sul ghost per non intercettare hit-test degli agent sottostanti. Build verde, file a 434 linee. Hover dell'agent (`.is-drop-target`) già funzionava e ora si vede chiaramente in combo col ghost che ci passa sopra.
- [17:00] (Scott — Builder) **Click su skill chip → apre SKILL.md (discriminazione click-vs-drag)**. Con il mouse-based DnD il chip nel pool e la chip linkata non aprivano nulla al click (mousedown partiva ma mouseup faceva solo "drop"). Aggiunto pattern click-vs-drag unificato in `WhiteboardOrganigramma.tsx`: `SkillChip.onMouseDown` registra `mousemove`+`mouseup` globali con soglia `DRAG_THRESHOLD_PX=4`. Se il mouse si muove oltre → chiama `onDragStart(skill, x, y, el)` (che crea ghost + listeners); al mouseup con drag attivo → chiama la `cleanup(x, y)` restituita da `onDragStart`. Se il mouse NON si muove → al mouseup è un CLICK → chiama `onClick(skill)` che apre `openFile(wsId, skill.path)`. Cambi richiesti: `SkillDef` esteso con `path: string` (popolato da `readSkillsDir` come `${e.path}/SKILL.md`); `openSkillFile(skill)` callback in `WhiteboardOrganigramma` che chiama `useStore.getState().openFile(wsId, skill.path)`; `OrgGroup` e `AgentNode` ricevono `openSkillFile` e lo passano alle linked-chip; wrapper dell'agent ha onClick che skipppa se `e.target.closest("[data-wb-skill]")` (così cliccando una chip linkata si apre SOLO la SKILL, non anche il .md dell'agent). Rimpiazzato il vecchio `<div className="whiteboard-skill-chip linked">` con `<SkillChip>` (uniforme) e il × ora è standalone `whiteboard-skill-chip-x-standalone` posizionato `absolute top-right` nel `<li>`. `keydown` Enter/Space sull'agent wrapper guard `e.target !== e.currentTarget` per non aprire da chip-focale. Build verde, file 527 linee (sotto 600). Doc 018 aggiornata.
- [17:10] (Scott — Builder) **Icona Organigramma nella sidebar di sinistra**. Aggiunto bottone `<Icon name="whiteboard" />` in `ActivityBar.tsx` subito dopo il bottone AI chat (entrambi sono "feature launcher", non section toggle). Click → `wbOpen(activeId)` per il workspace attivo. Active state: walk del pane tree per trovare il `tabs.active` del focused pane, `startsWith("wb:")` → highlight via classe `.active` standard. Rinominato tab label da "Whiteboard" → "Organigramma" (in `PaneNode.tabLabel` e `store.ts.labelFor`). Aggiunto CSS `.activity-icon[aria-label="Organigramma"].active::before` con bordo `--border-strong` per distinguere dai section toggle puri (è un feature launcher). Build verde, ActivityBar.tsx ora 402 linee.
- [17:15] (Scott — Builder) **Icona Organigramma spostata penultima** (prima del duck AI, che ora è ultima nella sidebar). Swap puro in `ActivityBar.tsx`: il bottone Organigramma è stato spostato dal fondo alla posizione precedente del bottone AI. Build verde.
