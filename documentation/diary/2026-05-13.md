---
type: diary
project: quack-app
date: 2026-05-13
---

- [09:39] (Auto) Sessione: 1 file letti (~7.0k tok)
- [09:41] (Auto) Sessione: 2 file letti (~5.0k tok)
- [09:49] (Auto) Sessione: 11 file letti (~29.6k tok) | Brain: 3 gotcha warnings
- [09:57] (Auto) Sessione: 4 file letti (~25.7k tok), 2 letture ripetute, ~29.0k tok risparmiati | Brain: 6 gotcha warnings, 2 breadcrumbs
- [12:30] (Alek) feat(providers): MVP feature 037 Anthropic-compatible providers — T001-T022. Nuovi tipi `ProviderPreset`/`CustomProvider`/`ActiveProviderState`, 6 preset built-in (Anthropic/Z.AI/MiniMax/Kimi/Qwen/DeepSeek) in `src/constants/providerPresets.ts`, settingsStore esteso con `activeProvider`+`customProviders` (v12→v13 migration), Tauri command `test_provider_connection` (POST /v1/messages max_tokens=1), nuovi command namespaced `save_provider_api_key`/`get_provider_api_key` (additive, OpenAI key intoccata), `ProviderManager`+`ProviderCard`+`ProviderTestButton` UI in Settings → Claude Code, `providerService` + `providerEnvBuilder`. **Architectural deviation dal spec**: il daemon Quack è persistente (singleton), quindi il provider config passa nel JSON di query (`providerConfig` field in `ClaudeCliRequest`), NON come env var allo spawn — il daemon Node imposta/ripristina `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN`/`ANTHROPIC_DEFAULT_*_MODEL` per-query e clear `ANTHROPIC_API_KEY` per evitare conflitto Bearer vs x-api-key. INSIGHT: `save_api_key` esistente è hardcoded su `openai_api_key` singleton, non aveva namespace — aggiunto campo `provider_api_keys: HashMap<String,String>` in `AppPreferences` (additive). US2-US4 + polish ancora da fare.
- [10:23] (Auto) Sessione: 2 file letti (~173.2k tok), 2 letture ripetute, ~173.2k tok risparmiati | Brain: 2 AST hits, 24 gotcha warnings, 4 breadcrumbs
- [14:00] (Alek) feat(providers): completata feature 037 — US2 + US3 + US4 + Polish. **US2** (T023-T028): `ProviderAddModal` con validazione baseUrl/contextWindow, action "Duplicate" sulle card preset → modale pre-popolata, pulsante "+ Add provider" in `ProviderManager`, fallback su delete-active già nel MVP. **US3** (T029-T035): override per-sessione in-memory via `sessionProviderOverrides.ts` (NON persisted, `useSyncExternalStore`), `NewSessionProviderPicker` + `SessionProviderBadge` montati in `UnifiedActionBar` (picker quando idle, badge durante streaming), `getActiveProviderConfig(sessionId)` ora accetta sessionId e applica override > default; tutte le 5 call site (`App.tsx` ×4 + `usePopoutKanbanChat`) passano sessionId. **US4** (T036-T038): `ChatView` calcola `effectiveProviderContextWindow` dal provider attivo (o override) e lo passa allo `StaminaBarBorder.maxTokens` — MiniMax 1M, Kimi/Qwen 256k, DeepSeek 128k ora rispettati nella barra. **Polish**: feature-doc `documentation/features/065-anthropic-compatible-providers.md`, pattern doc `pattern-anthropic-compatible-providers.md` con anti-patterns (no env-at-spawn, no API_KEY+AUTH_TOKEN insieme), link al pattern in CLAUDE.md Knowledge Base, breadcrumbs `// Brain: 037-anthropic-compatible-providers` su tutti i blocchi chiave. **TS check**: 0 errori. **Cargo check**: 0 errori. **Rimanenti**: solo unit test (T009/T021/T028/T038) e smoke E2E manuali (T048/T049) che richiedono API key vere — saltati come da decisione "MVP pragmatico".
- [10:29] (Auto) Sessione: 1 file letti (~4.7k tok) | Brain: 5 gotcha warnings
- [10:35] (Auto) Sessione: 1 file letti (~12.5k tok) | Brain: 12 gotcha warnings, 3 breadcrumbs
- [14:30] (Alek) fix(providers): hotfix crash ChatView `TypeError: undefined is not an object (evaluating 'activeProviderState.kind')`. CAUSA: per utenti con stato persisted intermedio (build dev con `version:13` già impostata ma senza il campo `activeProvider`), il check `if (version < 13)` non scattava → `claude.activeProvider` rimaneva `undefined` → ChatView crashava al render. FIX: (1) migration ora idempotente — popola `activeProvider`/`customProviders` per QUALSIASI persisted state che ne è privo, non solo `version < 13`. (2) Guard difensivo `?? { kind: 'anthropic' }` e `?? []` in tutti i 6 consumer: `ChatView`, `ProviderManager`, `NewSessionProviderPicker`, `SessionProviderBadge`, `providerService` (add/update/delete), `providerEnvBuilder.buildProviderConfig`. INSIGHT: bumpare `version` senza rendere la migration idempotente è una trappola classica — quando si hot-reloada in dev, la version può essere settata prima che il campo sia popolato.
- [10:40] (Auto) Sessione: 2 file letti (~15.9k tok), 1 letture ripetute, ~7.9k tok risparmiati
- [10:43] (Auto) Sessione: 1 file letti (~7.7k tok)
- [10:46] (Auto) Sessione: 1 file letti (~8.6k tok)
- [10:57] (Auto) Sessione: 2 file letti (~6.3k tok), 2 letture ripetute, ~6.3k tok risparmiati | Brain: 8 gotcha warnings, 2 breadcrumbs
- [15:10] (Alek) fix(providers UX): 3 fix UX dopo feedback utente. (1) **"Set as default" sempre cliccabile**: rimosso il vincolo `hasToken` che disabilitava il bottone quando mancava la key — l'utente lo vedeva spento e pensava fosse rotto. Ora si può sempre attivare; un chip "Needs API key" giallo segnala visivamente che la sessione fallirà finché non c'è la key. (2) **Field API key più leggibile**: aggiunta label "API key" sopra il campo, placeholder esplicito `Paste your <Provider> API key`, link "Get key" che apre `docsUrl` del provider. (3) **ChatSettingsMenu mostra i provider Anthropic-compatible**: nuova select sotto i tab Claude/Ollama (visibile solo quando tab Claude attivo) che lista i 6 preset + customs e scrive direttamente su `claude.activeProvider`. L'utente ora può cambiare provider sia da Settings sia dal popover compatto in chat. INSIGHT: bloccare un'azione finché non si soddisfa un prerequisito è anti-pattern quando il prerequisito è visivamente ovvio (campo key vuoto sopra); meglio permettere l'azione e mostrare un warning passivo.
- [11:01] (Auto) Sessione: 1 file letti (~8.9k tok)
- [15:30] (Alek) refactor(providers UX): per-session picker rimosso da UnifiedActionBar (l'`onMouseDown preventDefault` della bar bloccava il select — non era cliccabile e l'utente lo segnalava giustamente). Sezione "Anthropic-compatible" in `ChatSettingsMenu` rinominata in **"Custom Model"** e ora filtra dinamicamente: alla riapertura del popover scatta una `Promise.all(getProviderToken(id))` su tutti i preset/custom; solo quelli con key salvata appaiono nella dropdown (sempre presente: "Anthropic (Official)"). Se non ci sono custom configurati, hint sotto la select rimanda a Settings. Rimossa anche la prop `sessionId` da `UnifiedActionBar` (più non serve) — il picker ora vive solo dentro il popover settings, che NON è soggetto al mousedown intercept. INSIGHT: ogni elemento interattivo aggiunto a `unified-action-bar` deve essere whitelistato nel `closest()` check del mousedown handler, altrimenti `e.preventDefault()` ne blocca il click — soluzione: spostare i picker dentro popover/menu già whitelistati invece di patchare il check.
- [15:50] (Alek) fix(providers UX): doppia selezione modello (Custom Model = MiniMax + Model = Opus 4.7) confondeva l'utente — un solo modello "vincente" non era chiaro. FIX: quando `activeProvider.kind === 'custom'`, il dropdown "Model" viene rimpiazzato dinamicamente con le opzioni REALI del provider (es. `MiniMax-M2`, oppure per z.ai `glm-4.6` + `glm-4.5-air`). Le opzioni sono mappate a friendly id (`sonnet46`/`haiku45`/`opus47`) per attivare correttamente i corrispondenti `ANTHROPIC_DEFAULT_*_MODEL` env override nel daemon. Auto-correzione: se il modello persistito (es. `opus47` da Anthropic) non è in nessuna delle opzioni provider, `queueMicrotask(() => onModelChange(opts[0].friendly))` propaga il default. Deduplicazione: se sonnet/haiku/default sono lo stesso modello (MiniMax M2 ha sonnet=haiku), mostra una sola riga. INSIGHT: con SDK Anthropic-compatible, l'env override `ANTHROPIC_DEFAULT_SONNET_MODEL` scatta SOLO se la stringa "sonnet" (o "haiku") compare nel model id inviato; quindi è essenziale che la selezione UI mappi a friendly id che generano `claude-sonnet-*` / `claude-haiku-*` come model field nel body API.
- [11:29] (Auto) Sessione: 2 file letti (~20.6k tok), 1 letture ripetute, ~5.6k tok risparmiati | Brain: 16 gotcha warnings, 8 breadcrumbs
- [15:35] (Alek) fix(daemon): regressione AskUserQuestion empty-answers ricomparsa dopo il fix di ieri, ma in scenario specifico — utente lascia il widget pending >60s prima di rispondere. Diag log: `PreToolUse fired ... toolUseId=toolu_xxx` poi 10 min dopo `RESULT_EVENT: success`, `canUseTool bypassed`, `PostToolUse: no staged answers (skipping)`. Causa: SDK `HookMatcher.timeout` default è **60 secondi** — il PreToolUse hook abortisce, query continua, modello vede vuoto. Fix: `timeout: 86400` (24h) sull'HookMatcher PreToolUse. Aggiornato Brain `bugs/fix-askuserquestion-sdk-0.2.138-pretool-posttool-hook.md` con sezione "Hook timeout — must override the 60s default".
- [16:10] (Alek) fix(providers UX): label "SONNET46"/"Sonnet 4.6" mostrato anche quando il provider attivo era MiniMax. CAUSA: il modello salvato è il friendly id (`sonnet46`) necessario per attivare `ANTHROPIC_DEFAULT_SONNET_MODEL` nell'env, ma il display leggeva il `modelLabels[sonnet46]` invece del modello reale del provider. FIX: (1) aggiunta funzione `getActiveModelDisplayName(friendly)` in `claudeSDK.ts` che, quando un custom provider è attivo, ritorna il vero model name del provider (es. `MiniMax-M2`, `glm-4.6`) mappando `sonnet*`→`sonnetModel`, `haiku*`→`haikuModel`, `opus*`→`defaultModel`. (2) Esteso il tipo `MessageSettingsMetadata` con `modelDisplayName?: string` (optional, retro-compatibile). (3) `MessageSettingsBadges` ora preferisce `modelDisplayName` quando presente, altrimenti fallback al label friendly tradizionale. (4) Tutti i 3 call site in `App.tsx` che popolano `settings.model` ora aggiungono anche `modelDisplayName`. (5) `getModelLabelText` in `ChatSettingsMenu` ora mostra il nome reale nel chip in basso ("Opus 4.7 → MiniMax-M2"). I messaggi storici (senza `modelDisplayName`) continuano a mostrare il vecchio label — invariante storica preservata.
- [11:34] (Auto) Sessione: 5 file letti (~200.3k tok), 1 letture ripetute, ~337.6k tok risparmiati | Brain: 3 AST hits, 50 gotcha warnings, 41 breadcrumbs
- [18:30] (Alek) security+docs(providers): hardening + Brain entries. **Security fix**: `usingProviderConfig=${usingProviderConfig}` nel diag log del daemon stava scrivendo la API key in chiaro su disco — `a && b && c` in JS ritorna l'ultimo operando truthy (la chiave), non un booleano. Cast a `!!` + commento di warning sul punto + log `~/.quack/daemon-diag.log` esistente sanitizzato (backup `.pre-sanitize.bak` per riferimento, rotazione chiave consigliata). Rimossa anche la funzione morta `getCachedProviderModels` dal `providerService.ts` (mai usata da nessun consumer). **Brain entries**: due nuovi gotcha — `gotcha-anthropic-compatible-identity-hallucination.md` (cloni Anthropic-compatible dicono "I am Claude" per default — debug attraverso JSONL `model` field o `PROVIDER_CONFIG_APPLIED` diag) e `gotcha-js-template-literal-secret-leak.md` (anti-pattern generale, esempio concreto + script di sanitizzazione). Linkati entrambi in CLAUDE.md Knowledge Base. **Pattern + Feature 065**: aggiunte sezioni "Live model list", "Identity hallucination caveat", "Diag logging" con esempi; lista anti-patterns estesa con (a) OAuth precedence, (b) custom model name senza "sonnet" trigger, (c) trust del "I am Claude", (d) interpolazione di `&&` chain. **MMD**: creato `pattern-anthropic-compatible-providers.mmd` — sequence diagram completo del flow (Refresh → cache → ChatSettings → activeModel → env override → provider echo). **TS check**: 0 errori. **Cargo check**: 0 errori.
- [17:55] (Alek) docs(037+065+pattern): sincronizzati 3 file di documentazione con lo stato attuale del sistema providers. `037-unified-settings-panel.md` era fermo a v7/v11 del `settings-storage` e non menzionava la sezione Providers — aggiornato a v13, aggiunti i 4 file `ProviderManager/Card/TestButton/AddModal`, i 3 service `providerService/providerEnvBuilder/sessionProviderOverrides`, i 4 nuovi Tauri command (`save_provider_api_key`, `get_provider_api_key`, `test_provider_connection`, `fetch_provider_models`), descrizione di `claude.activeProvider`+`customProviders`+`providerModelCache`. `065-anthropic-compatible-providers.md` e `pattern-anthropic-compatible-providers.md`: rimossi riferimenti residui a Qwen/DeepSeek (non più built-in), aggiornato il context-window mapping (1M MiniMax, 256k Kimi, 200k Z.AI).
- [17:40] (Alek) chore(providers): ridotti i preset built-in da 6 a 4 — tenuti Anthropic + Z.AI + MiniMax + Kimi, rimossi Qwen (DashScope) e DeepSeek (SiliconFlow). Razionale: ora che `Refresh models` recupera dinamicamente la lista da `/v1/models`, i preset rari aggiungono solo rumore — chi vuole Qwen/DeepSeek può creare un custom provider in Settings. Aggiornati anche i copy in `ClaudeCodeSettings` (description sezione) e `ChatSettingsMenu` (hint). **TS check**: 0 errori.
- [17:15] (Alek) diag+fix(providers): l'utente ha settato MiniMax M2.7-HighSpeed, badge UI corretto, ma il modello risponde "Sono Claude Sonnet 4.6" e Token Usage modal mostra `Sonnet 4.6` con 99.7% cache hit — la chiamata sta uscendo verso Anthropic, non MiniMax. (1) Aggiunto diag log `PROVIDER_CONFIG: present=... usingProviderConfig=...` su OGNI query e `PROVIDER_CONFIG_APPLIED: baseUrl=...` quando l'override scatta — così si può verificare in `~/.quack/daemon-diag.log` se il providerConfig sta arrivando al daemon. (2) Fix preventivo: il daemon ora cancella anche `CLAUDE_CODE_OAUTH_TOKEN` durante una query con custom provider (e lo ripristina in finally). Ipotesi: l'SDK/binary `claude-code` quando trova OAuth Pro/Max env var lo preferisce a `ANTHROPIC_AUTH_TOKEN`+`ANTHROPIC_BASE_URL`, bypassando silenziosamente l'override.
- [16:45] (Alek) feat(providers): lista modelli aggiornabile via `GET /v1/models`. Nuovo Tauri command `fetch_provider_models` (10s timeout, manda sia `Authorization: Bearer` sia `x-api-key` per compat OpenAI/Anthropic shape, parsea `data[].id` da entrambi i formati). Lato TS: `fetchProviderModels(id)` in `providerService.ts` cacha il risultato in `claude.providerModelCache: Record<id, {models, fetchedAt}>` (campo additivo, migration idempotente). `ProviderCard` ora ha pulsante "Refresh models" + chip di anteprima dei primi 12 model id + timestamp. `ChatSettingsMenu` dropdown Model: quando il provider attivo è custom E la cache è popolata, mostra la lista LIVE (es. tutti i model id di z.ai/MiniMax), altrimenti fallback alle 3 slot sonnet/haiku/default bundled. **Trick env override**: il modello scelto dall'utente va salvato in `activeProvider.activeModel` (esteso il type `ActiveProviderState` con questo campo opzionale), ma il friendly id inviato dall'app rimane forzato a `sonnet46` — così `claude-sonnet-4-6` arriva al daemon, matcha "sonnet", e l'env override `ANTHROPIC_DEFAULT_SONNET_MODEL` viene popolato con `activeModel`. `providerEnvBuilder` ora preferisce `activeModel` su tutte e 3 le slot tier quando settato. `getActiveModelDisplayName` e `getModelLabelText` aggiornati per mostrare `activeModel` nel badge/chip. **TS check**: 0 errori. **Cargo check**: 0 errori. INSIGHT: non tutti i provider Anthropic-compatible espongono `/v1/models` con lo stesso shape — Anthropic ufficiale ritorna `{data:[{id, display_name, type:"model"}]}`, mentre cloni stile OpenAI ritornano `{object:"list", data:[{id, object:"model"}]}` — entrambi hanno `data[].id`, quindi parsing unico funziona. Inviare BOTH `Authorization: Bearer` E `x-api-key` evita gestione provider-specific.
- [11:45] (Auto) Sessione: 9 file letti (~43.0k tok), 1 letture ripetute, ~12.9k tok risparmiati | Brain: 42 gotcha warnings, 11 breadcrumbs
- [11:53] (Auto) Sessione: 1 file letti (~8.9k tok)
- [13:18] (Auto) Sessione: 3 file letti (~38.8k tok) | Brain: 21 gotcha warnings, 12 breadcrumbs
- [14:07] (Auto) Sessione: 1 file letti (~4.1k tok)
- [14:31] (Auto) Sessione: 4 file letti (~25.7k tok) | Brain: 15 gotcha warnings, 3 breadcrumbs
- [18:00] (Alek) style(action-icons): riordinati gli ActionIcons in `src/components/ActionIcons.tsx` — sequenza nuova Kanban → Office → Whiteboard → Automation → Usage → Git → Terminal → Store → Telegram → Open → SidePanel. Razionale: i 4 entry-point "spaziali/operativi" (kanban+office+whiteboard+automation) ora sono raggruppati e prioritari, il resto resta in coda. Modifica puramente di ordine, nessuna logica toccata.
- [18:05] (Alek) feat(office-v2): pan canvas senza Spazio, in linea con Whiteboard. (1) **Trackpad two-finger scroll / rotella senza modifier** → pan; `Cmd/Ctrl + Wheel` o pinch (ctrlKey) → zoom. Prima la rotella zoomava soltanto. (2) **Plain left-drag su canvas vuoto in mode `select`** → pan, senza necessità di tenere Spazio. `panStartRef` traccia `didDrag` con threshold 2px; su pointerup la logica di deselezione (threshold 4px) scatta solo se `didDrag` resta `false`. Space+drag, middle-click e lasso (Shift+drag) preservati. Aggiornata feature-doc `063-office-view-v2.md` (sezioni "Canvas Pan / Zoom", "Multi-select gestures", "Keyboard Shortcuts"). INSIGHT: replicato lo stesso pattern di `FeatureMapCanvas.tsx` — pinch-zoom via `ctrlKey` è un dettaglio standard browser (sia pinch trackpad sia Ctrl+wheel impostano `ctrlKey=true` su WheelEvent), evita di dover esporre un toggle UI per "mode pan vs zoom".
- [18:10] (Alek) fix(whiteboard): `CanvasMdCard` ora apre in **preview di default** anche per le card inline (prima `useState<boolean>(isInline)` → ora `useState<boolean>(false)`). Razionale: la card è uno strumento di lettura/visualizzazione sulla canvas; aprire in edit-first sorprende l'utente che si aspetta di vedere il rendering. Il toggle preview/edit nell'header resta cliccabile in 1 step per le inline cards (le file-backed restano preview-only come prima). Aggiornato il commento del file e la sezione MD Preview Cards in `documentation/features/026-feature-map-whiteboard.md`.
