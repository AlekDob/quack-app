---
type: diary
project: quack-desktop
date: 2026-07-03
---
- [11:30] (Alek) **Turn status dock** — `ai-status-pill` / `ai-inline-status` spostato dal message stream a `.ai-status-dock` sopra il composer (stessa famiglia di `ai-ask-dock` / `ai-todos-bar`). Resta visibile mentre si scrolla il transcript; `max-height: 40%` + scroll interno per liste tool lunghe. Doc: feature 022 aggiornata.
- [13:00] (Alek) **User message bar** — nuovo `UserMessageBar.tsx`: turni utente come card allineata a destra con `MarkdownPreview`, azioni copy/re-send/branch su hover (`z-index` per turno per stacking). Rimossi label "You" + bottoni nell'header assistant-style. Doc: feature 030.
- [13:00] (Alek) **Session diff hub subtitles** — `sessionDiffStats.ts` + `chatDiffStore.ts`: righe hub espanse mostrano `Edited foo.ts −N +M` o aggregate `+N −M · K files` dall'ultimo turno agente. `AIChatPanel` pubblica live; hub idrata da `chatHistory` per chat in background; `clearChatDiff` alla chiusura tab. Doc: feature 029 + 009 aggiornata.
- [13:00] (Alek) **ComposeCard dedup** — soglia recap file `≥1` edit (era ≥2); `InterleavedBlocks` accetta `hideEdits` per nascondere pill Edit inline quando `ComposeCard` è presente (niente doppio elenco). Doc: feature 006 aggiornata.
- [13:00] (Alek) **Dev port** — Vite/HMR `1420/1421` → `1422/1423` (+ `tauri.conf.json` devUrl) per evitare collisioni con sessioni Tauri già in ascolto. Gotcha whiteboard 018 aggiornata.
- [14:45] (Alek) **Cursor-style turn scroll** — on send pin the user turn at the top (`chatScroll.ts` + `followModeRef`); no tail-follow during the turn unless the user scrolls or hits jump-to-bottom. Inline `Planning next moves…` hint in the stream; dock keeps tools/tps/stale only.
- [15:45] (Alek) **Model discovery cache + startup perf** — `modelDiscoveryStore.ts`: shared TTL cache (60s), prefetch during splash, lazy `ensureCloudCatalog`, deduped inflight, invalidate on API key. `AIChatPanel` subscribes instead of per-mount full probe; `WorkspaceShell` lazy `AIChatHost` + active-workspace-only; `store.hydrate` parallel workspace open. Docs: features 031, 032; updated 001, 025, CLAUDE.md.
