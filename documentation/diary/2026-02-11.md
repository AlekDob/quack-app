---
type: diary
date: 2026-02-11
---
# 2026-02-11

- Fixed navigation bugs: session view not clearing on done/new agent, added callback chain to reset activeSessionId/activeTaskId
- Fixed notification badge showing on all agents at startup: empty `lastReadTimestamps` at boot + badge counting `done` sessions
- Refactored macOS release signing/notarization system: removed all hardcoded paths/identities, centralized in `.env` + Keychain. New `sign-and-notarize.sh` with 7-step pipeline (see patterns/macos-release-signing-notarization)
- Gotchas resolved: process substitution quote parsing, Tauri auto-notarization when env vars present, .app deletion after DMG creation
- Implemented Teammate Stream Drill-Down: clicking TeammateWidget opens dedicated tab with real-time JSONL activity stream (see patterns/teammate-stream-drilldown)
- Fixed auto-scroll during streaming: added `userHasScrolledUpRef` to MessageList.tsx
