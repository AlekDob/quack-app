# 2026-07-27

- [10:15] (Alek) **Chat stream max-width (Cursor-style)** — stream rows + composer/docks share `--chat-stream-max: 760px`, centered in wide panes; narrow panes stay full-width. Token + `.ai-messages` / `.ai-composer-shell` + dock siblings in `App.css`. Docs: `003-design-system.md`.
- [10:25] (Alek) **Context ring refresh after `/compact`** — Quack ignored CC `compact_boundary.postTokens`, so the ring stayed on the pre-compact snap (and JSONL zero-usage "No response requested." masked disk hydrate). Stream → `context_snapshot` via `contextTokensFromCompactMeta`; skip zero-sum `message_start`; Rust `last_context_snap` prefers `postTokens`; idle disk poll steals on ≥50% drop; panel syncs `lastUsage.contextTokens` on snapshot. Tests: `contextUsage.test.ts`, `session_jsonl` compact snaps. Docs: expanded `023` (`/compact` section), `014` bridge, `gotcha/cc-compact-context-ring.md`, CLAUDE.md index.
