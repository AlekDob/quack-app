# 2026-07-27

- [10:15] (Alek) **Chat stream max-width (Cursor-style)** — stream rows + composer/docks share `--chat-stream-max: 760px`, centered in wide panes; narrow panes stay full-width. Token + `.ai-messages` / `.ai-composer-shell` + dock siblings in `App.css`. Docs: `003-design-system.md`.

- [11:05] (Alek) **New chat → project picker** — hub New chat opens shared Recents menu (`WorkspaceProjectMenu`); **Open folder…** fixed at bottom while list scrolls. Composer chip reuses same body. Docs: `050`.

- [11:00] (Alek) **Agent Mode: drop project icon rail** — removed `.agent-wsrail` from `AgentModeShell`; switch projects via hub badges / composer picker. Docs: `001`, `002`.

- [11:00] (Alek) **More conversation padding-x** — `--chat-stream-pad-x: 40px` (56px in Agent Mode compact); stream/composer/docks share it.
- [11:40] (Alek) **Fix: Cursor CLI models never listed** — the picker's Cursor tab showed "Install cursor-agent" on a machine where it was installed and logged in. Two causes, both frontend: (1) `cursorCliProvider.listModels()` returned `[DEFAULT_MODEL]` on *both* branches, never calling `cursor_code_list_models` — now delegates to `refreshCursorModelsLive`; (2) `warmLiveCliCatalogs` gated on `snap.cursorCliAvailable`, which starts `false` and is only corrected by a fire-and-forget probe, so opening the picker early skipped the warm-up — now awaits `probeCliAvailability`. Rust side (`cursor_code.rs` resolve + `parse_list_models`) was correct all along. Docs: `026`, `025`, `031`.
