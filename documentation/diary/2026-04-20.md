---
type: diary
project: quack-app
date: 2026-04-20
---
- [14:36] (Alek) SessionRecovery quota cascade fix: implemented quota management for session backups (MAX_BACKUPS=100, MAX_MESSAGES_PER_BACKUP=500) with LRU eviction to prevent localStorage QuotaExceededError. Added retry-once logic on quota failure. Brain breadcrumb in modelService.ts: emergency fallback warning now deduped via `emergencyFallbackWarned` flag to prevent console spam in hot paths (every select dropdown, every settings read) when Supabase is unreachable. Tied to PiP mode session restoration trigger. Feature documented in fix-session-backup-quota-cascade.md Brain entry.
