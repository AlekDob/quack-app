---
type: diary
project: quack-app
date: 2026-05-15
---
- [Alek] Analisi strategica multi-vendor SDK post annuncio Anthropic (15 giugno: Agent SDK su credito separato $20–$200, non più subscription). Valutati spec Codex SDK (2026-04-28) e decision doc Antonio (2026-05-14). KEY INSIGHT: Codex SDK supporta auth via abbonamento ChatGPT (OAuth `codex login`) → parità economica con Claude Pro per segmento ChatGPT. Verificato su developers.openai.com/codex/auth.
- [Alek] Ispezionato branch `036-vercel-ai-sdk-multi-provider`: 4.601 righe, mai shippato. `vercel-tools.js` (418) + `vercel-mcp.js` (150) reimplementano Read/Bash/Grep/Edit/WebFetch+MCP da zero. KEY INSIGHT: il Vercel AI SDK è completion+tool-calling, NON un harness agentico → astrarre al livello modello costa la ricostruzione dell'intero agente. Le parti UI riusabili del 036 sono già superate da 037-anthropic-compatible (ieri). Del 036 si salva solo la lezione.
- [Alek] Creato `documentation/decisions/decision-quack-abstraction-agent-level-not-model-level.md`: il layer Quack-owned va al LIVELLO AGENTE (interface + capability registry + event translator sopra Claude Agent SDK / Codex SDK), non al livello modello. Reply formale che raffina il doc Antonio. Roadmap AI-velocity ~14 giorni con stack 037 come fondazione. Open question urgente pre-15-giugno: API key Console pay-as-you-go è fuori dal credit-cap? (richiede conferma Anthropic, non codice).
- [Alek] Verifica fallback 037 end-to-end: catena `providerEnvBuilder → sessionProviderOverrides → stream-daemon.js:377-394` integra. Preset zai/minimax/kimi funzionanti, il daemon setta `ANTHROPIC_BASE_URL`+`ANTHROPIC_AUTH_TOKEN` bypassando subscription/OAuth Claude. KEY INSIGHT: il fallback al credit-cap del 15 giugno è GIÀ shippato e usabile oggi — basta API key di un provider Anthropic-compatible in Settings.
- [Alek] Spike Codex CLI (Milestone 0, parziale) → `documentation/research/codex-cli-events.md`. KEY INSIGHT: codex 0.42 ha DUE schemi JSON incompatibili (`--json` deprecato/wrappato vs `--experimental-json` flat con nomi dotted) — la mapping table §3 dello spec Antonio è OBSOLETA. `session.created` porta subito il `session_id` per resume. Codex legge il suo `~/.codex/config.toml` MCP indipendentemente (MCP rotti rallentano ogni exec). BLOCCO: auth refresh 401 (rischio #1 register confermato dal vivo) → spike non completabile senza `codex login` utente o OPENAI_API_KEY valida.
- [Alek] Continuazione spike Codex CLI (Milestone 0) → riscritto `documentation/research/codex-cli-events.md` in inglese tecnico. Probe auth ri-eseguita: blocco confermato (`Failed to refresh token: 401`, 5 retry, backoff esponenziale 182ms→3.5s). NON ritentato per regola. Diagnostica `auth.json`: campo `OPENAI_API_KEY` VUOTO (ecco perché `preferred_auth_method=apikey` non fa fallback), `last_refresh` 2025-09-26 (~7.5 mesi stale). Catturato schema RAW reale NUOVO: `session.created` = `{type, session_id}` (UUIDv7, primo evento, model NON presente), `error` = `{type, message}` SENZA `code`/`recoverable`. KEY INSIGHT: `codex exec` esce con **exit code 0 anche su auth failure terminale**, stderr vuoto, errore solo su stdout → `CodexBackend` NON può usare l'exit code per rilevare fallimenti, deve parsare lo stream `error` (ultima riga senza "retrying" = failure marker). `QuackAgentEvent::Error{code,message,recoverable}` non popolabile nativamente: code/recoverable vanno inferiti via string-match. Nuova mapping table §6 (VERIFIED: SessionStarted, Error; BLOCKED: tutto il resto). `normalize_tool_name` resta IPOTESI non verificata. Spike resta `status: partial` — completamento richiede `codex login` utente.
- [Alek] Spike Codex CLI SBLOCCATO via `codex login --api-key` → `documentation/research/codex-cli-events.md` portato a `status: complete`. Catturati tutti i capture mancanti su codex 0.42 `--experimental-json`. KEY INSIGHT 1 (architetturale): Codex espone DUE superfici di eventi NON equivalenti — stdout stream (`session.created`, `item.started/completed`, `error`) SENZA token usage, e rollout JSONL (`~/.codex/sessions/Y/M/D/rollout-<ts>-<sid>.jsonl`) con `event_msg/token_count` (`total_token_usage` cumulativo vs `last_token_usage` per-turn, `cached_input_tokens`, `reasoning_output_tokens`, `model_context_window`). `CodexBackend` deve consumare ENTRAMBE (stream + file watcher sul rollout) — lo spec assumeva un singolo `completion.done.usage` inesistente. KEY INSIGHT 2: ipotesi `normalize_tool_name` dello spec FALSIFICATA — niente `apply_patch/shell/view`, Codex fa tutto via `command_execution` (`bash -lc`), il discriminante è `item.item_type` non un tool name; distinguere Read/Edit richiede euristiche sulla stringa `command` (fedeltà < Claude). KEY INSIGHT 3 (Open Q3): `codex exec` è non-interattivo, `approval_policy=on-request` NON emette alcun evento — `QuackAgentEvent::PermissionRequest` NON ha sorgente via exec, unica leva è la sandbox mode allo spawn. Open Q5: resume PRESERVA il contesto (codeword richiamata), flag PRIMA del subcomando `resume`, `session.created` riemesso con STESSO id (dedupe per id), stesso rollout file appeso. Altri: `error` solo `{type,message}` (no code/recoverable), exit code 0 anche su failure terminale (parsare lo stream). Nuova mapping table §6 tutta VERIFIED, `normalize_tool_name` §7 reale.
- [Alek] Scritto piano implementazione Milestone 1 (Foundation) Codex backend → `docs/superpowers/plans/2026-05-15-codex-milestone-1-foundation.md` (skill writing-plans). Piano fasato A→E con TDD pieno sul translator `events.rs` (fixture RAW verbatim dallo spike), trait `AgentBackend`, `CodexBackend` spawn-per-query + tail rollout, riuso stack provider 037 per auth, toggle UI. KEY INSIGHT: il contratto eventi autorevole è `codex-cli-events.md §6/§7` (spike verificato), NON lo spec §3/§6/§7 obsoleto — pianificato sopra la realtà misurata, non sulle assunzioni di Antonio. Task B2 (`ClaudeBackend` wrapper) marcato come REVIEW GATE isolato (zero-regressione Claude = rischio #1). PermissionRequest documentato come ASSENTE (spike §4.6: nessuna sorgente via codex exec) — non è un gap. Mappato il codebase reale: daemon path `claude_cli.rs:412/461`, comando `send_message_via_sdk_streaming:1952`, keychain namespaced 037 `preferences::save_provider_api_key`, `AgentSession` types.ts:457, `createSession` sessionStore, modal/sidebar. Zero codice produzione scritto (solo piano).
- [15:47] (Auto) Sessione: 1 file letti (~683 tok)
- [15:58] (Auto) Sessione: 1 file letti (~4.3k tok)
- [16:18] (Auto) Sessione: 19 file letti (~106.8k tok), 4 letture ripetute, ~89.4k tok risparmiati | Brain: 36 gotcha warnings
- [16:43] (Alek) Fase A Milestone 1 Codex completata (subagent-driven-development, 5 task A1-A5, un subagent fresco + review spec+qualità per task). Creato `src-tauri/src/agents/` (mod.rs, events.rs + fixtures RAW dallo spike). Translator puro `events.rs`: `QuackAgentEvent` enum, `normalize_tool_name`, `codex_stream_to_quack`+`classify_codex_error`, `codex_rollout_to_usage`+`codex_rollout_model`, `claude_event_to_quack` (additive). 18 test verdi, tutti TDD rosso→verde da fixture verbatim. KEY INSIGHT: il contratto `Error{code,message,recoverable}` NON è una contraddizione col commento spike §4.3 — i campi sono SINTETIZZATI per Codex via string-match (`classify_codex_error`: 401/refresh→auth/non-recoverable), come da §6 VERIFIED; respinta la proposta del code-reviewer di renderli Option (avrebbe rotto il contratto verificato e i test prescritti di A3). Deviazione segnalata: Fase A NON parallelizzata come da brief — A2-A5 toccano lo stesso `events.rs`, la skill vieta implementer concorrenti → eseguiti in sequenza, logica resta pura/testata. Branch `038-codex-backend-m1`.
- [17:03] (Alek) Fase B Milestone 1 Codex completata (B1 trait AgentBackend + value types, B2 ClaudeBackend, B3 CodexBackend). B1: aggiunto `async-trait` 0.1.89. B2 = REVIEW GATE ISOLATO superato: misurata baseline `cargo test` su branch PRIMA del task, verificato in worktree pulito su `daec7fd` che `claude_usage::test_parse_usage_output` + `telegram_obfuscation::test_xor_token` falliscono GIA' su main (env/data-dependent, non regressioni nostre). B2 post-task = baseline identica (35p/2f), `claude_cli.rs` zero-diff sull'intero branch, `ClaudeBackend` non cablato in nessun runtime path → zero-behavior-change provato staticamente. B3: assunto `codex exec --cd` VERIFICATO LIVE su 0.42 (probe isolato → `CDOK`, che conferma anche auth Codex funzionante via `codex login --api-key`); 38p/2f (no regressione), `lib.rs` puramente additivo (comando `send_message_via_codex` + `codex_auth_status`). KEY INSIGHT: il gate zero-regressione va interpretato come "nessun test che passava diventa rosso", NON "suite tutta verde" — la baseline reale del repo è `N passed; 2 failed` con 2 nomi fissi preesistenti. Salvato `documentation/gotchas/gotcha-preexisting-test-failures-main.md`. Follow-up M3 (fuori scope M1, non fixati per disciplina): `walk_jsonl` usa `std::fs::read_dir` sync in contesto async; `cancel()` Codex è noop (kill child reale rimandato). Smoke GUI manuale Claude = checkpoint umano differito. TENSIONE da risolvere al gate finale: il brief chiede "cargo test verde inclusi i preesistenti" ma i 2 rossi sono di main, fuori scope M1.
- [17:18] (Alek) Fase C Milestone 1 Codex completata (C1 tipo + mirror, C2 persist + migrazione). C1: `src/types/agentBackend.ts` mirror TS fedele al contratto serde Rust (`kind` discriminant snake_case, `args:unknown` non `any`), 3 campi opzionali su `AgentSession` (`backend?`, `backendSessionId?`, `backendAuthMethod?`) — migration-safe by design. C2: `normalizeSessionBackend()` con breadcrumb `// Brain: decision-quack-abstraction-agent-level-not-model-level`, applicato all'UNICO load-site persistence->state (`loadSessions:107`, NON createSession/render), TDD vitest 2/2. KEY INSIGHT: la migrazione legacy e' puramente difensiva — sessione senza `backend` -> `'claude'` (spread preserva tutti i campi), quindi le sessioni esistenti si comportano IDENTICHE a prima; nessun rischio churn (loadSessions gira una volta al mount progetto con write-lock guard). Baseline tsc frontend CLEAN confermata su 2 review indipendenti -> il requisito "tsc pulito" del gate finale e' raggiungibile lato frontend.
