---
type: diary
project: quack-app
date: 2026-03-14
---
- [14:00] (Alek) feat: 1M context window support. Anthropic announced 1M GA for Opus 4.6 / Sonnet 4.6 (no surcharge). Added: dynamic `contextWindow` extraction from SDK `modelUsage`, propagated through token tracking pipeline to StaminaBarBorder/TokenUsageModal/TokenUsageIndicator. Added `[1m]` toggle button in ChatSettingsMenu (cyan accent, visible only for Opus 4.6/Sonnet 4.6). Updated `stream-claude.js` and `stream-daemon.js` to handle `[1m]` suffix in model IDs. Updated `modelService.ts` for `[1m]` suffix in getModelId/getModelLabel. Updated `conversationRecovery.ts` to accept optional `contextWindow` parameter. All UI strings now dynamic ("200k" or "1M"). Fallback to 200K when SDK doesn't provide contextWindow.
- [15:30] (Alek) debug: investigated why CLI `/context` shows 200k after Quack 1M session. Root cause: known CLI display bug (GitHub #23432). The `[1m]` suffix is handled by the CLI subprocess (9 refs in cli.js). Since March 13 2026, 1M is GA — no beta header needed, requests over 200k work automatically. Added contextWindow debug logging in result events.
- [16:00] (Alek) refactor: toggle 1M ora solo per Sonnet 4.6. Opus 4.6 ha 1M nativo automatico (Max/Team/Enterprise/API), toggle rimosso. Sonnet 4.6 richiede `[1m]` esplicito (extra usage billing). Rimosso `betas` ridondante da stream-claude.js/stream-daemon.js (GA, ignorato dal server).
