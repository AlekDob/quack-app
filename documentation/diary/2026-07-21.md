# 2026-07-21

- [12:45] (Alek) **Fix Cursor citation fences in chat markdown** (`049`). Fence opener only accepted `\w*` lang tags, so ` ```12:15:path/file.ts ` bodies leaked — lines starting with `>` became callouts (Findings “Tu” blockquote glitch). Now accept full info strings; derive lang from path ext. Vitest: `markdown.test.ts`.

- [12:45] (Alek) **Fix chat file link → wrong workspace** (bug `006`). Agent Mode click on `firma.html` switched to the first open project: `isUnderRoot` joined absolute sibling paths onto the wrong root (`joinPath(codetta, /…/alekdob/file)` still prefixed `codetta/`), so `workspaceForAbs` activated that project. Fix: absolute paths outside root are not under it; longest-root wins in `workspaceDocOpen` + `smartFileOpen`. Vitest: `pathUtils.test.ts`. Docs: bug `006` (full repro/fix table), feature `070` (ownership contract + verify), feature `037` (`isUnderRoot` contract), `CLAUDE.md`.

- [11:50] (Alek) **Fix session model/effort/mode bleed** (bug `005`). Changing Opus/Auto in one chat rewrote siblings: idle Agent hosts remount (`076` / `chatHostMount`), discovery + global `localStorage` seeded the wrong knobs, then persist stamped them onto the bound session before hydrate. Fix: warm-cache seed (`sessionComposerSeed.ts`), `sessionReady` persist gate, discovery skips global last-used until ready, `mergeSessionKnobs` includes `model`, Team preset apply only on the visible host. Updated feature `040` (full remount-isolation section), cross-links in `025` / `062` / `076`, `CLAUDE.md`. Vitest: `sessionComposerSeed.test.ts`.
