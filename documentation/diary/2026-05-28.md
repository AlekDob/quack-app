---
type: diary
project: quack-app
date: 2026-05-28
---

- [10:24] (Auto) Sessione: 1 file letti (~3.8k tok) | Brain: 2 gotcha warnings
- [--:--] (Alek) Feature 066 Codex backend: messa on-hold. Rimossi tutti i riferimenti UI/UX dalla feature doc (componenti NewSessionModal, ChatSettingsMenu, SessionsPanel, ChatView, UnifiedActionBar, App.tsx listener/routing, model picker M1.5b). Aggiornata decision `decision-quack-abstraction-agent-level-not-model-level` da proposed → on-hold. Pattern `pattern-backend-capability-gated-ui` marcato on-hold. Backend Rust (AgentBackend trait, QuackAgentEvent, CodexBackend) e adapter TS puro restano documentati come riferimento architetturale. Codice sorgente non toccato — solo documentazione.
- [--:--] (Alek) Fix opus46 hardcoded fallback overrides opus47 default: rimpiazzati 11 punti `'opus46'` con `'opus47'` in `App.tsx` (5 fallback), `ChatView.tsx` (default prop + background task), `claudeSDK.ts` (`getActiveModelName`), `modelUtils.ts` (LEGACY_MAP + normalize full API IDs), `settingsStore.ts` (LEGACY_MODEL_MAP). Aggiunta `<option value="opus47">Opus 4.7</option>` nella select BTW Side-Chain (`ClaudeCodeSettings.tsx`). NON toccati EMERGENCY_FALLBACK, label storiche, mapping alias->API, migrazione v11 (gia' corretta), `defaultEffortForModel`. KEY INSIGHT: il flagship model id non va mai hardcodato in codice runtime — leggerlo da `defaultClaudeSettings.model` o `modelService.getDefaultModel()`. Brain entry: `fix-opus46-hardcoded-fallback-overrides-opus47-default.md`. Linkata in CLAUDE.md Knowledge Base. Aggiornato anche `gotcha-model-name-non-anthropic-provider.md` con riferimento.
- [10:59] (Auto) Sessione: 7 file letti (~212.5k tok), 4 letture ripetute, ~548.5k tok risparmiati | Brain: 4 AST hits, 94 gotcha warnings, 107 breadcrumbs
- [15:22] (Auto) Sessione: 2 file letti (~3.4k tok)
- [--:--] (Alek) Rimosso il binding `Option (Alt)` standalone in `ChatInput.tsx::handleKeyDown` che ciclava il modello base (feature 051). Su tastiera IT Mac `@` si digita con `Alt+Shift+ò`: il keydown di Alt scattava il cambio modello mentre l'utente apriva un'`@`-mention (agent/skill/file/feature). Rimossi anche import non piu' usati (`getModelOptions`, `useModelsConfig`) e l'`useMemo` `modelOptionsForCycle`. Aggiornata feature doc 051 (rimossa riga shortcut Option). Tab/Shift+Tab (effort cycle) preservati. Il modello ora si cambia solo da Chat Settings.
- [--:--] (Alek) Fix crash "Provider Error: Git" durante streaming di Jack: `MarkdownText.tsx` faceva `JSON.parse(codeContent)` inline (senza try/catch) per i 4 PM widget fenced (`ws-board`, `task-suggest`, `agent-grid`, `briefing`, commit `e164235`). Quando il chunk SSE chiudeva la fence ma il JSON era ancora troncato, `SyntaxError: JSON Parse error: Expected '}'` bubblava all'outermost ErrorBoundary (`GitProvider` wrappa `{children}` in `contexts/index.tsx`) → fallback "Provider Error: Git" anche se Git non c'entrava nulla. KEY INSIGHT: con streaming SSE non si può `JSON.parse` inline nel JSX — pattern skip-then-mount con helper `safeParseWidgetData` (ritorna `null` su throw, render successivo monta il widget quando il JSON è completo). Brain: `fix-markdown-pm-widgets-streaming-json-crash.md`. Breadcrumb aggiunto in `MarkdownText.tsx`.
- [16:13] (Auto) Sessione: 1 file letti (~5.6k tok), 1 letture ripetute, ~5.6k tok risparmiati | Brain: 16 gotcha warnings, 3 breadcrumbs
- [--:--] (Alek) Fix TodoWrite widget "0/N completed" post SDK 0.3.150: il widget mostrava tutti i task come pending perche' l'SDK non emette piu' `tool_result` come eventi `user` per i managed Task tools. `taskAccumulator.ts` dipendeva dal tool_result per estrarre il `task.id` reale dal `TaskCreate` — senza di esso ogni entry era keyed come `pending-${toolUseId}` e i `TaskUpdate` con il `taskId` reale non matchavano. Aggiunto meccanismo di riconciliazione: `unresolvedPending[]` traccia le TaskCreate senza task.id, e TaskUpdate rimappa il primo pending non risolto al taskId reale. Il path via `toolResultTexts` resta come primary per backward compat. KEY INSIGHT: SDK 0.3.150 managed tools (Task*) sono gestiti internamente — il tool_result torna al modello ma non viene emesso nello stream come evento user, a differenza dei tool "normali" (Read, Write, Bash). Brain: `fix-task-accumulator-pending-reconciliation.md`.
