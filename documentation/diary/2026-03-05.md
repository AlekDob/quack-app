---
type: diary
project: quack-app
date: 2026-03-05
---
- [15:00] (Alek) fix(office): PixiJS black screen in production builds — root cause: CSP `script-src 'self'` blocks `new Function()` used by PixiJS for WebGL shader compilation. Fix: import `pixi.js/unsafe-eval` which replaces all eval-based methods with CSP-safe polyfills. Dev mode was unaffected because Tauri bypasses CSP in dev. Added gotcha `gotcha-pixi-csp-unsafe-eval.md`.
- [15:30] (Alek) refactor(office): rimosso tooltip hover sulle duck nell'Office view. Puliti prop `onDuckHover`/`onHover` da tutta la chain (OfficeView → OfficeScene → OfficeRoom → OfficeDuck). Rimossa interfaccia `TooltipData` da officeTypes.ts. Il file `OfficeTooltip.tsx` è ora dead code.
- [16:00] (Alek) fix(office): Office view snappava alla chat 1-2s dopo l'apertura — root cause DEFINITIVA dopo 7 tentativi: PixiJS v8 `EventBoundary.mapPointerUp` NON pulisce `pressTargetsByButton` dopo click normali (solo per `pointerupoutside`). Combinato con `pointerup` ascoltato su `globalThis`, i tracking data stale da sessioni precedenti triggeravano `onRoomClick` spurio. Fix `pointer-events: none` su parent div non funzionava perché PixiJS bypassa DOM pointer-events via `globalThis`. Fix definitivo: `pointerdown` guard ref in `OfficeRoom.tsx` — `onClick` chiama `onRoomClick` solo se un `pointerdown` genuino è stato ricevuto sul canvas container. Defense-in-depth: `specialTabTypes` + tab dedup mantenuti. Brain: `fix-office-view-snaps-back-to-chat`.
- [19:30] (Alek) feat(code-intel): Native Code Intelligence MCP server — 4 tool (code_outline, code_find_definition, code_find_references, code_get_imports) basati su tree-sitter per dare agli agenti navigazione semantica del codebase. Sostituisce Grep/Read ripetitivi che sprecavano ~15K token di contesto. Architettura: Node.js MCP server stdio-based (pattern ide-mcp-server.js) con tree-sitter binding nativi. 7 file creati in `src-tauri/node-sdk/lib/code-intel/`. Registrato automaticamente in stream-claude.js accanto a ide-tools. MVP: TypeScript/JavaScript. Validata e scartata LSP skill esterna (skills.sh — rotta, trust: fail). Scartato Repomix (299K token, inutilizzabile). Tree-sitter + walkdir ibrido = ~80% del valore di un LSP con zero dipendenze esterne.
- [18:30] (Alek) feat(debug): Debug Mode v2 — 4 miglioramenti al sistema di debugging. (1) Brain-First Protocol: `loadBrainHints(cwd)` carica slug da `documentation/bugs/` e `gotchas/`, iniettati nel system prompt con protocollo STEP 0 obbligatorio + esempio concreto wrong/right. (2) Git Context: `loadGitContext(cwd)` auto-inietta `git log --oneline -5` + `git diff --stat` con timeout 3s. (3) Thinking forzato: preset debug cambiato da `thinkingMode: 'auto'` a `'hard'` + migrazione v3 in settingsStore. (4) Skill trimmed: rimosse "Common Rationalizations" e "Quick Reference" (-30 righe), aggiunto Step 1.5 "Check Quack Brain" in Phase 1. Struttura prompt riordinata per recency bias: skill → git → Brain protocol (ultimo = più forte). Aggiornati ENTRAMBI stream-claude.js e stream-daemon.js (dual-path gotcha).
- [21:00] (Alek) feat(tooling): Creato droid `codebase-doctor` + `codebase-cleaner` + installato Knip. (1) `codebase-doctor` (`.claude/agents/`): scansiona dead code, performance, code smells in qualsiasi linguaggio; output `documentation/codebase-health.md` con score A-F. Testato con 3 istanze in parallelo (dead-code, performance, smells). (2) `codebase-cleaner` (`~/.claude/agents/` — globale, multi-linguaggio): legge health report ed esegue cleanup chirurgico (delete dead files, remove debug logs, extract hardcoded values, clean deps). Supporta `dryRun`, `scope`, `priority`. (3) Installato `knip` come devDep — risultati molto piu' precisi del doctor custom: 285 file morti vs 35, 68 unused exports vs 27. Workflow consigliato: Knip (dead code) → doctor (performance/smells) → cleaner (fix). Prima esecuzione cleaner: rimossi ~85 righe debug (`[MCP DEBUG]`, `[DORMANT DEBUG]`, `[Memory Observer DEBUG]`) da `claudeSDK.ts` e `App.tsx`. Health score attuale: D (App.tsx 12,993 righe = 40x il limite).
- [22:00] (Alek) fix(remote): Chat input nascosto quando agente è busy — su mobile, aprendo una sessione con agente in_progress, l'input veniva mostrato comunque. Fix: wrappato `chat-input-bar` in condizionale `isAgentBusy(s.agentId)` — se busy, l'input non viene renderizzato. Quando l'agente torna idle, il WebSocket aggiorna lo stato e il re-render mostra l'input. File: `src-tauri/static/app.js`.
- [23:00] (Alek) fix(code-intel): Server MCP non appariva nell'UI agente nonostante server funzionante (ListTools OK nel debug log). Root cause: `options.mcpServers` programmatico in stream-claude.js è inaffidabile quando combinato con `settingSources: ['project']`. Il SDK carica i server da `.mcp.json` indipendentemente. Fix: aggiunto `code-intel` a `.mcp.json` nella root del progetto — il SDK lo carica automaticamente via settingSources. Gotcha critico documentato: `gotcha-mcp-registration-settings-sources.md`. Aggiunti anche colore cyan (#06b6d4) e icona custom `< />` per code-intel in ToolWidgets.tsx, StreamMessage.tsx, ToolCallMinimal.tsx.
- [23:30] (Alek) docs(mcp): Creata guida utente MCP `documentation/guide/mcp/overview.md` — spiega cosa sono i server MCP, i 6 built-in (brain, kanban, semantic-search, ide-tools, memory-prompt-hook, code-intel), come vedere i server attivi nel pannello sidebar, come aggiungere server custom via `.mcp.json`, server globali in `~/.quack/mcp/.mcp.json`, e troubleshooting comune.
