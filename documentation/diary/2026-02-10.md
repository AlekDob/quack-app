---
type: diary
date: 2026-02-10
---
# 2026-02-10

- Fixed Store item description truncation: removed description div from `StoreItemCard.tsx` grid view (still in detail modal)
- Critical Windows fix: replaced all 21 hardcoded `/` path concatenations in `useMarketplace.ts` with `join()` from `@tauri-apps/api/path`. Windows `homeDir()` returns backslash paths, mixing with `/` breaks filesystem operations. Unlocked Quack Store for Windows users
- Fixed agent bundle install routing: `installResource()` only handles individual resources, agent bundles need `installAgentBundle()`. Added routing logic in `QuackStoreDrawer.handleInstall()` with clear error message when no project is open
