---
type: diary
project: quack-app
date: 2026-04-03
---
- [10:00] (Alek) feat: Add 5th permission mode "Ask" (🛡️ amber #f59e0b). Same power as Build (Opus 4.6, medium effort) but SDK `default` permissionMode — asks user permission for each tool use. No skill injection, no banner. 11 files modified: PermissionMode type, AgentModePresets interface, CustomPermissionSelect, ChatSettingsMenu, AgentModesSettings, settingsStore (v6→v7 migration), ChatView Shift+Tab cycle, App.tsx preset cast, claude_cli.rs SDK mapping, claudeSDK.ts direct path mapping, pattern doc updated. Key insight: Ask vs Chat distinction is power level (Opus/medium vs Sonnet/low) + skill injection (none vs chat-interaction skill).
- [10:15] (Auto) Sessione: 4 file letti (~23.0k tok), 1 letture ripetute, ~15.4k tok risparmiati | Brain: 9 gotcha warnings
- [10:20] (Auto) Sessione: 7 file letti (~197.3k tok), 5 letture ripetute, ~704.6k tok risparmiati | Brain: 5 AST hits, 62 gotcha warnings
- [10:32] (Auto) Sessione: 3 file letti (~173.8k tok), 2 letture ripetute, ~178.9k tok risparmiati | Brain: 2 AST hits, 30 gotcha warnings, 14 breadcrumbs
- [11:00] (Alek) fix: Ask mode non chiedeva permesso — due root cause. (1) canUseTool in stream-daemon.js auto-approvava tutti i tool. Fix: aggiunto sistema tool_permission_request (Rust→daemon→frontend). (2) **La vera root cause**: `allowedTools` nel daemon includeva Write/Edit/Bash che il SDK auto-approva al punto 4 della catena di valutazione, PRIMA che `canUseTool` venga mai chiamato. Documentazione SDK conferma: Hooks → Deny rules → Permission mode → **Allow rules** → canUseTool. Fix: in Ask mode, `resolvedAllowedTools` usa solo tool read-only (Read, Glob, Grep, AskUserQuestion, ExitPlanMode, TodoWrite) così Write/Edit/Bash cadono nel canUseTool per approvazione utente.
- [11:48] (Auto) Sessione: 13 file letti (~273.1k tok), 7 letture ripetute, ~1.3M tok risparmiati | Brain: 8 AST hits, 99 gotcha warnings
- [12:30] (Alek) fix: Ask mode sidebar dot stayed yellow instead of purple awaiting state. Root cause: `tool-permission-request` handler set chatStore directly via `useChatStore.getState().setPendingQuestion()`, bypassing `pendingQuestionIdsMap` React state. The sync useEffect (line 1245) then immediately cleared it because the key wasn't in `pendingQuestionIdsMap`. Fix: route through `setPendingQuestionIdsMap` (same pattern as AskUserQuestion and PlanApproval handlers). This cascades to both sidebar dot (purple `?`) and Kanban board ("Awaiting Input" badge). Also fixed `respondToToolPermission` to clean up via same path.
