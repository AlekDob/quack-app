# 2026-07-11

- [UI] (Alek) **Live turn status shimmer** — `Planning next moves…`, `Loading model…`, `Running tools…`, `Generating…`, and in-progress tool headers use `.ai-live-shimmer` (gradient text sweep, 2.2s) so the status dock reads at a glance; planning row bumped to 12.5px/500 + `.ai-spinner-live`. Pill variant peaks on `--primary-fg`; stale suffix unchanged (`.ai-stale-shimmer`). Docs: `022`, `006`. Files: `TurnStreamStatus.tsx`, `App.css`.
- [Fix] (Alek) **Cursor CLI + OpenCode bridge gaps** — Composer stream-json uses native `tool_call`/`thinking` events (not Claude `stream_event`); new `cursorStreamJson.ts` maps them to chat chips + `--stream-partial-output`. Image attachments enabled for Cursor CLI (paths in prompt) and OpenCode (`FilePart` + `modalities.input` vision gate). OpenCode tool SSE uses `state.status`/`state.output`. Docs: `026`, `028`, `016`.
- [Fix] (Alek) **Reasoning UI dedupe + Brain-style chip** — double grey Reasoning panels removed when `blocks[]` exists; new `ReasoningTurnChip` (cloud icon, word count, preview, expand). `splitThinking()` tag fix + dedupe. Docs: `056`, `006`.
- [Feature] (Alek) **Drag file from explorer → composer cite** — pointer-based DnD (no HTML5; Tauri swallows it): drag a file row from `FileTree` onto the chat composer inserts `@relativePath` at the cursor and queues it in workspace context. Docs: `055`, updates `041`/`034`/`022`.
- [Feature] (Alek) **Platform pin in model picker** — agentic chats lock the composer model list to the starting CLI (CC / Cursor / OpenCode); Cursor-style banner + confirm before cross-platform switch. `pinnedProviderId` on `ChatSession`. Docs: `057`, `044`, `025`.
- [UX] (Alek) **Platform pin simplified** — removed "Change platform…" unlock and inline confirm; pinned picker is hard-filtered with a one-line banner + **New chat** to switch CLI. Docs: `057`.
- [Feature] (Alek) **Claude Code dynamic model catalog** — `claude_code_list_models` parses `/model` for aliases; background probes resolve Sonnet 5 / Opus 4.8 / Fable 5 labels (1h cache); instant fallbacks via `claudeCodePickerModels()`; dedupe duplicate display names. Docs: `059`, `014`, `031`.
- [Perf] (Alek) **Instant model picker** — disk snapshot `lcp.modelDiscovery.v1` + `listFastModels()` skip CC spawn on cold path; popover opens with full `.is-hydrating` skeleton until live CLI catalogs finish; chip shows `displayName` not raw id; active provider group reordered first when unpinned. Docs: `025`, `031`.
- [Fix] (Alek) **Usage drawer empty on resumed CC sessions** — `claude_session_drawer_stats` reads JSONL for context + billing when stream-json absent; `sessionDiskHydrate.ts`; polls every 12s. Docs: `023`.
- [Feature] (Alek) **Brain self-improvement** — Jack `[Brain save]` proposals with amber `BrainSaveChip`; richer pre-turn inject reads full `.md` for thin snippets. Docs: `054`.
- [UI] (Alek) **Markdown callout copy** — blockquotes wrap in `.md-callout-wrap` with copy icon row below. Docs: `049`.
- [Perf] (Alek) **Workspace switch** — gate CC usage polls (JSONL disk hydrate, plan limits, auth probe), Ollama retry loop, rules probe, and model-discovery refresh to the foreground workspace only (`activeId === wsId`); immediate catch-up poll on switch-back. Streaming persist + multitask streams unchanged; `setWorkspaceRoot` only from active panel. `AIChatPanel.tsx`.
- [Perf] (Alek) **Many open tabs / workspace switch** — `useWorkspaceHeavyMount`: tear down sidebar, Monaco, and tab portals on backgrounded projects (300ms grace); defer editor mount 2×rAF so tab chrome paints first; mount subagent/compose-review portals only for the active tab. AI side panel + terminal containers unchanged (multitask + xterm stability). `useWorkspaceHeavyMount.ts`, `WorkspaceShell.tsx`.
