---
type: diary
project: quack-app
date: 2026-03-03
---

- [18:00] (Alek) fix(ui): expand chevron icon now visible on ALL truncated user messages, not just the sticky (last) one. Copy button remains sticky-only. Changed condition from `isLastUserMessage && isUser` to `isUser && (isLastUserMessage || isMessageTruncated)` in ChatMessage.tsx header actions.
- [22:30] (Alek) fix(automation): sessioni create da automation/cron apparivano "Untitled" — race condition tra `store.reload()` e `store.save()` nel Tauri Store. `sessionWriteLock.shouldSkipReload()` era definito ma MAI chiamato. Fix a 3 livelli: (1) wire up del lock in `loadAgentSessions`, (2) defense-in-depth in `updateSession`, (3) warning in `saveAgentSessions`. Estratto `sessionWriteLock` in modulo separato per evitare dipendenza circolare. Brain: `fix-automation-session-title-missing`.
- [23:30] (Alek) fix(session): sessione si reinizializzava da zero dopo Stop + nuovo messaggio. `claudeSessionId` veniva salvato nel session store SOLO dopo completamento con successo — se l'utente stoppava prima, il prossimo messaggio creava una sessione nuova. Fix: salvataggio immediato del `claudeSessionId` dall'evento `system/init` in `handleClaudeEvent`. Brain: `fix-session-reset-after-stop`.
