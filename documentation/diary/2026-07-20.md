# 2026-07-20

## Agent Mode context panels (Terminal)

Shipped **`084-agent-context-panels`**: Cursor-style `+` add-view menu on the Agent Mode right column. Changes/Files stay; each project terminal is a closable top tab. Active terminal mounts existing `TerminalCore` (project-scoped via `WorkspaceData.terminals`). Browser row is disabled for now.

Follow-up: dropped the nested “N Terminals” rail (tabs + `+` only). Status bar panel icon / Ctrl+J / Ctrl+` / New Terminal route through `agentContextNav` in Agent Mode.

Claude Sign in (`terminal.claude_login`): in Agent Mode, after spawning the Claude login PTY, selects that terminal tab in the right column so `/login` is interactive (was only opening the IDE bottom panel).

Files: `AgentContextColumn.tsx`, `AgentAddViewMenu.tsx`, `AgentTerminalPanel.tsx`, `agentContextNav.ts`, `actions.ts`, `StatusBar.tsx`, `AgentModeShell.tsx`, `App.css`, features `084` + `052`, CLAUDE.md KB.

## Agent Mode Plan tab (061 / 084 / 088)

When ExitPlanMode publishes a buy-in in **Agent Mode**, an on-demand **Plan** tab
appears in the right context column (Changes / Files / **Plan** / Terminals) with
the full markdown via `AgentPlanPane`.

| Piece | Role |
|---|---|
| `presentPlanReady` | Single router — Agent → Plan tab; IDE → FeatureDocDrawer / `plan:` |
| `focusAgentPlan` | Selects `"plan"` in `agentContextNav` |
| `planBuyInStore` | Drives tab show/hide (+ cwd fallback if session id not cached yet) |
| `onNativePlanReady` | Still merges linked feature to disk; **skips** drawer in Agent Mode |
| `PlanBuyInCard` | Unchanged composer CTA (Pass / Keep) |

Tab hides (fallback → Changes) when Pass or Keep clears buy-in. No new feature
number — extends `061` (plan surfaces) + `084` (context column) + `088` (buy-in).

Files: `agentContextNav.ts`, `AgentContextColumn.tsx`, `AgentModeShell.tsx`,
`PlanPane.tsx`, `quackPlanHarness.ts`, `planBuyInStore.ts`, `AIChatPanel.tsx`,
`App.css`, features `061` / `084` / `088`, CLAUDE.md KB.

## Agent Mode ↔ IDE toggle perf (085)

Created feature **`085-agent-ide-mode-toggle`**: documents the full shell swap (`App.tsx` AgentModeShell XOR WorkspaceShells), adds `[agent-mode-switch]` instrumentation (`switchPerf.ts`), and a trigger-file sim (`agentModeSwitchSim.ts`).

Live sim (8 open projects, many terminals): Agent→IDE settles ~**558ms** dominated by **terminal reattach cluster (~505ms)** + **chat hydrate (~455ms)**; `editors ready` at ~22ms is a false done signal. IDE→Agent ~**671ms** (filetree + terminal + chat hydrate).

**User console (cold, 102-msg chat):** Agent→IDE ~**4474ms** — `session loaded` loadMs **2661** (`force: true` disk) + 9× terminal attach ~4440ms. Pattern “first slow → then fast → idle slow again” = OS page cache around forced disk reloads. Fix: hydrate keeps rich RAM across remount (`085`); empty new chats covered in `087`. Terminal lazy-mount still deferred.

Cross-docs: `043` (conditional force), `076` (lazy hydrate), `086` (Perf Audit timeline).

## Perf Audit window (086)

Opt-in companion window (`?audit=1`): Quack/WebKit CPU·RAM + switch / new-chat / agent-mode / resume timeline. StatusBar Audit chip, Settings → Diagnostics, Ctrl+Alt+P. Shares StatusBar `process_stats` poll. **Copy JSON** exports `quack-perf-audit` snapshots (prefers `detail.elapsedMs` so MODE rows are not the misleading “since toggle” clock). See `086-perf-audit-window.md`.

## New chat performance (087)

Perf Audit samples drove three stacked fixes — documented in **`087-new-chat-perf.md`**:

1. **Disk miss ~3.3s** — `force` on empty dropped RAM and called `chat_store_load` for a missing file. Fix: `addAIChat` seeds empty body; hydrate never force-drops empty.
2. **Hydrate done ~6.9s after 14ms load** — empty UI in `startTransition` queued behind mount work. Fix: sync paint + immediate `finishHydrated` when `msgs.length === 0`.
3. **Panel painted ~1.5s** — extension probe, model catalog, CC `/` scan, skills/agents/presets on the critical path. Fix: `afterFirstPaint` deferral.

Also: resume heal only if hidden ≥2s (`024`) so alt-tab flicker stops flooding the audit timeline.

## Drop Pinky MCP for this project

Removed project `.mcp.json` (`pinky` → `pinky-mcp`, the ~1.3 GB process under Claude Code) and `.claude/rules/use-pinky-brain.md` + CLAUDE.md `@` include. Quack Brain UI/code stays optional via Store; Brain → Setup still rewrites `.mcp.json` if you opt in. Knowledge = `documentation/features/` + diary. Note in `054`.

## Fix: session mix + Editor context leak in history

**Bug doc:** `documentation/bugs/001-session-mix-editor-context-leak.md`

Two linked bugs (not Plan mode):

1. **Mix** — disk-hydrate `guessClaudeSessionId` linked Quack chats to the wrong
   Claude Code JSONL by `turn_count` alone (`list.find` = first match). Persisted
   wrong `providerSessionIds` → `--resume` + thin-row recovery pulled another chat.
2. **Leak** — CLI re-import (recovery / ⟲ Sessions) showed `[Editor context]` /
   QUACK EDITOR / Agent identity as user bubbles. Send path already kept bare
   text in Quack state; JSONL had the wire `ccPrefix`.

**Fix:**

| Area | Change |
|---|---|
| Identity | Removed turn-count auto-guess; sid only from stream-json or ⟲ Sessions |
| Sanitize | `stripEditorContextPrefix` + `sanitizeUserMessageContent` in `cleanStaleToolMessages` |
| Paths | Recovery `toChatMessages` + `onResume` → `applyLoadedMessages` |
| Tests | `src/chatTextUtils.test.ts` |
| Docs | Features `044` (identity safety section), `001`, `023`, `058`; bug `001` |

**Trade-off:** usage ring JSONL hydrate skips until a real sid exists (`023`).
Wrong links already on disk: re-link via ⟲ Sessions; bubbles heal on reopen.


## Plan mode → Pass the ball to Milo

Feature **`088-plan-milo-handoff`**: Cursor-style buy-in after `ExitPlanMode`.

| Piece | Behavior |
|---|---|
| CTA | In-stream `PlanBuyInCard` (Milo avatar) — not the permission card |
| Pass the ball | Milo + Agent + allow ExitPlanMode + auto-send implement prompt |
| Keep discussing / composer send | deny — stay Plan with Jack |
| Preview | `featureId` → FeatureDocDrawer; else `plan:` tab |
| Isolation | `permModeStore`: Plan not on `byCwd`; unknown session ≠ sibling mode |
| Prompt | ExitPlanMode only when composer is Plan |

Also updated: `015`, `061`, `068`, CLAUDE.md KB.

Files: `planBuyInStore.ts`, `PlanBuyInCard.tsx`, `permModeStore.ts` (+ test), `ClaudePermissionOverlay.tsx`, `AIChatPanel.tsx`, `brainPrompt.ts`, `App.css`.

## Fix: Agent↔IDE title loss + truncated transcript

**Bug doc:** `documentation/bugs/002-agent-ide-title-transcript-loss.md`

Round-trip **Agent → IDE → Agent** wiped hub titles to **Untitled** and could
thin the on-disk / displayed transcript.

| Cause | Fix |
|---|---|
| `setAgentMode` flipped layout with no flush+await (unlike project switch) | `flushAllChatPersist` + `awaitChatDiskFlushes` before notify |
| `selectedByWs` `useState` on `AgentModeShell` wiped on remount | Module `agentModeSelection.ts` + IDE `activeAiChatId` seed; `focusAIChat` on select |
| `deriveTitle` → `"Untitled"` + `setAIChatTitle` on thin remount | Skip auto-title when empty/Untitled; `preferSessionTitle` in `preferRicherSession` |

**Docs:** `085` (Durability + title), `043`, `001`, CLAUDE.md KB.  
**Tests:** `chatStoreCache.test.ts` (Untitled title prefer).

Files: `agentMode.ts`, `agentModeSelection.ts`, `AgentModeShell.tsx`, `AIChatPanel.tsx`,
`chatStoreCache.ts`, `PaneNode.tsx`, `WelcomeModal.tsx`, `agentModeSwitchSim.ts`.

## Plan buy-in: show CTA even when ExitPlanMode fails

User still saw identical UX — ExitPlanMode errored "not enabled", so the permission-hook path never published `planBuyInStore`. Fix: publish buy-in from the `ExitPlanMode` **tool_call** args (and end-of-turn fallback); move `PlanBuyInCard` above the composer shell; transcript row shows "Plan ready" instead of a red error. Docs `088`.

## Fix: new Agent Mode session ignored Team preset knobs

New chats after **087** seeded an empty RAM body, so hydrate always took the `found` branch and
skipped `applyDefaultPreset`. Result: pill showed Milo (initial state) while model/mode stayed
last-used **Sonnet** / **Auto** instead of Team **Opus** / **Agent**.

Fix: empty hydrate without a persisted model applies `presetIdForEmptyHydrate` via `applyPreset`;
seed `presetId: DEFAULT_PRESET_ID` in `addAIChat`; retry knobs when the agentic CLI becomes
available (`presetKnobsPendingRef`). Vitest: `aiChatPresetApply.test.ts`. Docs `062` + `087`.

## Fix: subagent chips lost in Cursor-compact stream (004 / 082)

After 082, `CompactBlocks` dumped `Task`/`Agent` into `ActionBatchSummary` as
`"Ran N actions"` / generic `<> Subagent` — avatar chip + drawer click gone even
though `ToolCallRow` still had the special-case. Fix: `isSubagentDispatch` flushes
the batch and mounts duck-avatar `ToolCallRow` again. Vitest:
`chatToolRender.test.ts`. Docs `004`, `006`, `082` + CLAUDE.md KB.

## Fix: mid-turn agent switch reattributed the streaming avatar (062)

Switching Jack↔Milo (SubagentPill) while a turn was still streaming flipped the
in-progress bubble's face to the newly picked agent — previous narration looked
like it belonged to whoever was selected now. Composer correctly updates for the
**next** message; the live bubble must keep the agent frozen at send.

Fix: `turnAgentId` + `streamingBubbleAgentId` (`chatTurnAgent.ts`); commit /
replay / stream checkpoints use the frozen id. Vitest: `chatTurnAgent.test.ts`.
Docs `062`.

## Fix: chat-session switch mixed Jack/Milo avatars (062)

Same family of bugs when hopping between two sessions that use different agents:
assistant rows without `agentId` always rendered as Jack, and remounting a DONE
chat seeded the default Milo pill for a frame. Fix: `resolveMessageAgentId` /
`backfillAssistantAgentIds` / `sessionAgentFromStored` on hydrate + render; RAM
cache seed for preset and messages on mount; send-path refs synced on
hydrate/`openSession`. Docs `062`, bug `003`.

## Fix: Pass-the-ball stamped Jack while Milo spoke (062 / 088)

"Sono Milo…" under a Jack avatar after Pass the ball: `applyPreset("builder")`
then `send` ran **before** React re-rendered, so `agentAtSend` / `[Agent identity]`
still saw Jack while the user expected Milo. Fix: validate preset, then sync
`presetIdRef` + model/effort/mode/thinking refs inside `applyPreset`;
`sendUserText` reads those refs for identity, stamp, and `chatStream` knobs.
Bug doc `003-agent-identity-mismatch.md`. Docs `062` / `088`.
