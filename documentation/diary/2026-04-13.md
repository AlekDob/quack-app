---
type: diary
project: quack-app
date: 2026-04-13
---

- [18:00] (Alek) feat(vercel-sdk): implement agentic tool use for non-Anthropic models — fileRead, fileWrite, listDirectory, searchFiles tools with Zod schemas. generateText + maxSteps(10) for automatic tool loop. Security: safePath with trailing-sep check, regex validation, file size caps. Code review: 8.2/10 PASSED after 2 rounds.
- [20:00] (Alek) fix(vercel-sdk): 3 bugs found during testing. (1) `const isVercelProvider` declared twice in stream-daemon.js crashed daemon startup — all providers broken. (2) `parameters` must be `inputSchema` in Vercel AI SDK v6 tool() — OpenAI rejects with `type: "None"`. (3) `stopWhen: stepCountIs()` replaced with `maxSteps: 10` for broader provider compat. Added agentic->chat fallback, vlog for visible Rust logging, daemon-diag.log for debugging.
- [21:00] (Alek) fix(vercel-sdk): 5 more bugs found in extended testing session. (4) `maxSteps` does NOT exist in v6, reverted to `stopWhen: stepCountIs(10)` — was causing single-step (tool call but no synthesis). (5) CLAUDE.md agent rules ("explain before acting") override tool behavior — truncated system prompt, tool-first instructions at CRITICAL priority. (6) Zero conversation history — added in-memory session store in daemon (vercelSessions Map, max 20 msgs). (7) `call.args` is undefined in v6 — must use `call.input`. (8) Missing tool_result events — frontend showed perpetual loading spinners, added tool_use+tool_result pairs. Also added MiniMax M2.5 + GLM 4.7 providers from Meow, tool name mapping (fileWrite->Write, fileRead->Read) for EditSummaryBar compat.
- [10:39] (Auto) Sessione completata: sessione "Vercel ai sdk", 404 messaggi, 70h 23m
- [10:40] (Auto) Sessione completata: sessione "Ehy gen", 6 messaggi, 66h 11m
