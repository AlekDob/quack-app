## Mermaid preview — zoom, sizing, and fit-to-viewport

**Problem (reported on Virgilio `.mmd` sequence diagrams):**
- Diagram rendered tiny in the top-left at “100%” — illegible.
- Trackpad pinch zoom felt sluggish / hard to control.
- After zooming even slightly (~103%), the diagram appeared to vanish (blank viewport).

**Root causes:**
1. Mermaid `render()` emits SVG with `width="100%"`. Inside `.mermaid-preview-stage` (shrink-to-fit `inline-block`), the SVG collapsed to a few pixels.
2. Old `syncStageBox()` set the stage layout box to `base × scale` **and** applied `transform: scale(scale)` on the same node — double-scaling. Scroll extents no longer matched visual size; `scrollForZoom` panned content off-screen.
3. Pinch factor `exp(-deltaY * 0.002)` was too gentle for macOS pixel deltas.

**Fix:**
- `mermaidZoom.ts`: `normalizeMermaidSvg`, `readMermaidSvgSize`, `syncMermaidScrollSizer`, `fitMermaidScale`; pinch tuned to `0.006`.
- `MermaidPreview.tsx`: spacer `.mermaid-preview-sizer` for scroll box; stage transform-only; `afterSvgPaint()` normalizes + fits on first render.
- `App.css`: sizer + absolute stage layout.
- Feature doc `042-mermaid-preview.md` updated with DOM layout diagram and gotchas.

## Explorer — filter fix + tree drag to editor

**Filter false positives:** Fuzzy match on full relative paths let unrelated queries match (e.g. `Schools` on `apps/client/.../trailing-slash.md` by stitching letters across `/`). Fixed: **name-only** matching in `fileTreeWalk.ts`; `normalizeFilterQuery` strips wrapping quotes. Toolbar chevron buttons: `padding: 0` on `.tree-toolbar-btn` (global `button` padding had misaligned icons).

**Tree → editor split:** File explorer drag now uses `dragState` + `tabDropTarget.ts` (shared with tab drag). Drop on pane edges / tab bar / drawer opens via `store.openFileAt`; composer cite unchanged. Docs: `034-explorer-tree.md`, `055-file-composer-drag.md` refreshed.

## Context usage popover — plan limits restored

When the Cursor-style context breakdown popover replaced `SessionUsageDrawer`, Claude Code OAuth plan limits (5hr / weekly / extra) moved to the Usage tab only. Restored inline **Plan limits** section in `SessionUsagePopover` with filling bars — data already polled every 30s into `sessionUsage.limits` / `.extra`. Shared `fmtLimitResetsIn` helper in `sessionUsageLocal.ts`.

## Smooth assistant streaming (Claude Code / agentic)

**Problem:** Agent replies appeared in jerky word blocks — especially Claude Code with interleaved tools. Each token delta re-parsed the entire message through `renderMarkdown()` and forced a full React commit.

**Fix (Cursor-style):**
- `src/streamPaint.ts` — `createStreamPainter` coalesces `setStreaming` / `setStreamingBlocks` to one commit per animation frame.
- `src/components/StreamingPlainText.tsx` — live tail is plain `pre-wrap` + blinking caret; markdown only after turn commit.
- `CompactBlocks` — stable text blocks (before a tool) still use `MarkdownPreview`; only the **last** growing block uses plain text while `streaming`.
- Attach-replay path (`claude_code_attach`) uses the same painter.

Doc: `069-smooth-streaming.md`; cross-refs in `006`, `049`, `014`, `CLAUDE.md`. `npm run build` clean.

## Works drawer polish + composer link + doc open

**Works catalog full width:** Removed `max-width: 720px` on `.works-items-catalog` / `.works-features-catalog` — list, modules, and stories views use the full main column (`065`).

**Description inline markdown:** Agents write `**bold**`, `` `paths` ``, links in `W-NNN.md` bodies. Drawer now previews inline markdown via `WorkBlockInlineText` + `renderInlineMarkdown` (`049`); click to edit source, blur to preview. Skill `quack-works` v9.

**Drawer UX:** `WorkModulePicker` (searchable, clear module), status/priority chip pickers, larger title, optional empty `moduleId`. `WorksDocRefsSection` grouped refs.

**Composer:** `ComposerWorkLinkPanel` — link existing story/work from Work chip menu (`068`). `quackPlanHarness.linkWorkToChat` / `linkStoryToChat`.

**Doc open:** `workspaceDocOpen.ts` — resolve bare feature filenames, `fs.exists` gate, warning toast / feature drawer empty state instead of OS errors (`070`). Wired in chat file links, docs chip, Works refs, `FeatureDocDrawer`.

**Context popover:** Plan limit bars restored inline in `SessionUsagePopover` (`023`).

Docs: `065`, `068`, `070`, `049`, `054`, `023`; skills `quack-works` v9.

## Honest model labels (alias vs resolved)

**Problem:** Composer chip flipped between `Sonnet` and `Sonnet 5` when CC label probes refreshed — same alias, two display names.

**Fix (Cursor-style honesty):**
- `src/modelDisplay.ts` — `composerChipLabel` (chip/picker = alias `sonnet`), `formatResolvedModel` (post-turn = billed model `Sonnet 5`).
- `ModelPickerRow`, `modelLabel`, `UsageChip`, `UsagePanel`, usage-strip tooltip wired through shared formatters.
- `FALLBACK_MODELS` display names aligned to aliases.

Doc: `071-honest-model-labels.md`; cross-refs in `025`, `059`, `023`.

## Agent Customizations — Skills fuzzy search

**Problem:** Dozens of project + user skills made the Skills tab hard to scan; intro path showed literal `&lt;name&gt;` because JSX does not decode HTML entities in text nodes.

**Fix:**
- `SkillsPane.tsx` — fuzzy search bar (`.mcp-search` inside `.cust-pane-search`) filters by folder name and scope via `fuzzyMatch` / `normalizeFilterQuery`; empty state *No skills match your search.*
- Intro `<code>` path uses a JS string so `<name>` renders correctly.
- `App.css` — `.cust-pane-search` padding between header and `.cust-list`.

Doc: `036-agent-customizations.md` (Skills tab section + `last_verified` 2026-07-13).

## Composer queue images + user bubble thumbs + inline slash (follow-up mode)

**Queue + images:** Follow-up queue was text-only — images during an active turn showed a blocking toast. Extended to `QueuedComposerMessage { text, images? }` in `composerQueue.ts`; persist/drain/multitask paths carry disk paths; `ComposerQueue` shows thumb previews.

**User message thumbnails:** Image thumbs render in `UserMessageImageStrip` **above** the sticky user bar (52px), not inside the compact clamp. `rehydrateMessageImages` on session load; hide boilerplate `"See the attached images."` when thumbs are visible.

**Less compact user bar:** Increased default padding/font; collapse threshold `160px` / `>500` chars; compact clamp `92px` (~4 lines). Images no longer force tall collapse.

**`/` and `@` during active turn:** Removed `streaming !== null` gates on slash + mention menus. Added `parseSlash(input, cursor)` for mid-message commands (`clearSlashSegment` / `replaceSlashSegment`). `onSelect` re-syncs parser state.

Docs: `039`, `030`, `016`, `008`, `022`, `041`. Files: `composerQueue.ts`, `composerDraft.ts`, `aiBus.ts`, `imageAttach.ts`, `UserMessageBar.tsx`, `ComposerQueue.tsx`, `AIChatPanel.tsx`, `useUserBarSticky.ts`, `App.css`.

## Pinky Brain — inject gates + `#` composer citations + mention chips

**Problem:** Pre-turn auto-inject treated every user message as a semantic search query — status updates like *"I'm on localhost:1422, restarted the server"* pulled irrelevant gotchas. Separate chip strip above the composer wasted vertical space.

**Fix:**
- Auto-inject default **off** (`lcp.brain.inject.<wsId>`).
- Configurable gates in Brain tab (`brainGates.ts`): score threshold, intent heuristic, optional thread-aware query.
- Composer `#` popover (`BrainMentionSuggestions`) — debounced Pinky search; pick queues path as **in-composer chip** (brain icon, `--info-bg`).
- Explicit inject via `fetchBrainContextForPaths` bypasses gates.
- Unified `ComposerMentionChips` for brain, `@` files, `@` agents, leading `/` skills — Cursor-style pills inside `.ai-input-row` (072).
- Jack prompt: explicit cites = read those paths; auto-hits = suggestions only.

Docs: `054-pinky-brain-integration.md`, `072-composer-mention-chips.md`. Files: `brainGates.ts`, `brainMention.ts`, `brainInject.ts`, `useBrainMentionSearch.ts`, `BrainInjectGates.tsx`, `BrainMentionSuggestions.tsx`, `ComposerMentionChips.tsx`, `AIChatPanel.tsx`, `composerDraft.ts`, `brainPrompt.ts`, `BrainSearchResults.tsx`.

- [Fix] (Alek) **Jack — explicit planning gate** — Stories/plan drawer no longer open on fresh chat. Removed `ComposerWorkBar` auto-`enterPlanning` when CC perm mode is `plan`. `worksChatAutoLink.ts`: auto-link work items to active chat; **skip draft stories** unless `linkedChats` is set. Jack prompt (`brainPrompt.ts`) + `quack-works` v10: no story at conversation start. Docs: `005`, `054`, `068`.

## User message bar — liquid glass + image card deck

**Problem:** Opaque grey user bubbles felt heavy; 52px image thumbs above the bar added vertical noise and were clipped when sticky at the top.

**Fix (Cursor / Apple liquid-glass pass):**
- `.ai-user-bar` → translucent gradient (`--user-bar-glass-from/to`) + `backdrop-filter: blur(medium)` + top-edge highlight; theme-specific `--user-bar-border` + `--user-bar-shadow` (light = composer-style `0 1px 2px`).
- `.ai-msg-user` sticky wrapper → `background: transparent` so blur reads the stream.
- `UserMessageImageStrip` → **`UserMessageImageDeck`**: 32×32px thumbs absolutely on the bar right (`bottom: 40px`), stacked at rest (`--fan-index` CSS vars), fan left on hover — no extra bar height.
- `UserMessageBarInner` → `.ai-user-bar-aside` groups deck + actions.

Docs: `030-user-message-bar.md` (full surface + deck spec), `016-image-attachments.md`, `003-design-system.md` (tokens). Files: `UserMessageBar.tsx`, `App.css`.

- [Feature] (Alek) **Composer permission-mode chip** — `ComposerPermMode.tsx`: Cursor-style semantic tint + icon per mode (Ask/Plan/Auto-edit/Auto/Agent); menu portaled to `document.body` (fixes overlap under sidebar). Renamed UI **Bypass → Agent** (`bypassPermissions` unchanged); slash `/mode agent` + aliases `holo`/`yolo`/`bypass`. Catalog in `permModes.ts`. Team-tab preset edits hot-reload via `subscribePresetSettings`. Docs: `015`, `022`, `062`.

## Built-in presets — Lia, shipped defaults, Team→chat sync

**Problem:** Team-tab edits to Jack/Milo/Nora/Vera (mode, effort, etc.) appeared to “not save” — overrides wrote to `lcp.presets.v1` but the live chat session kept old `ccEffort`/`ccPermMode`, and reopening the Team drawer could show stale values until a full refresh.

**Fix:**
- `resolveActivePresetDef` (`AIChatPanel.tsx`) — built-ins + Jack always merge overrides from `effectivePresetDefinition` at apply time (not a stale `presetChoices` cache).
- `subscribePresetSettings` — bumps picker tick **and** silently re-runs `applyPreset` for the active session (Jack when `presetId === null`).
- `WhiteboardOrganigramma` — subscribes so Jack root + preset chips re-render after Save.
- `setPresetOverrides` returns `boolean`; `AgentCreateDrawer` toasts on storage failure.

**Shipped defaults refresh** (`builtins.ts`, `instructions.ts`):
- Jack: reasoning / high / structured / plan / thinking on.
- Milo: balanced / medium / concise / Agent (`bypassPermissions`).
- Nora & Vera: balanced / medium / auto; **pinned feminine avatars** (`duck16`, `duck28`).
- **Lia · Companion** (`companion`) — new 4th built-in: balanced / low / terse-review / auto / `duck22`; dialogue-focused instruction block.
- `PresetId` union + `PRESET_ORDER` + Tab-cycle include Lia.

Doc: `062-presets.md` (defaults table, avatar pins, sync gotchas). Files: `builtins.ts`, `types.ts`, `instructions.ts`, `settings.ts`, `AIChatPanel.tsx`, `WhiteboardOrganigramma.tsx`, `AgentCreateDrawer.tsx`, `WhiteboardPresets.tsx`.

## Agent Hub — Archived section (preview + search)

**Problem:** Archived chats vanished from the hub entirely — no way to reopen old sessions without hunting workspace state.

**Fix (speed-first):**
- `AIChatsRail.tsx` — **Archived** collapsible group below Done (expanded hub only): latest **10** by `createdAt` desc; inline search filters title + project name (cap 30).
- Click row → unarchive (`setAIChatLifecycle active`) + `focusAIChat` (tab reopen).
- Context menu on archived rows → **Unarchive**; no `hydrateChatDiff` / live status for archived list (`useMemo` on `loaded`).
- `hubPrefs.ts` — first-run default collapses `"archived"` alongside `"done"`.
- CSS: `.agent-hub-archived-search`, `.agent-hub-dot.archived`, muted archived titles.

Docs: `009-agent-hub.md` (Archived section), `001-ai-session-library.md` (`doneAt`/`archivedAt` fields). Files: `AIChatsRail.tsx`, `hubPrefs.ts`, `App.css`.

## AskUserQuestion dock — stuck spinner + Cursor-style card

**Problem:** Question tool showed a perpetual "Question ⟳" row above the composer — `parseAskQuestions` returned empty when streamed `tool_call.arguments` were `{}`/partial, so the dock never rendered clickable options.

**Fix:**
- `askQuestionStore.ts` — cache full `tool_input` from the PreToolUse hook (`ClaudePermissionOverlay` publishes on `AskUserQuestion` deny-redirect).
- `chatToolRender.tsx` — `coerceToolArgs`, lenient option aliases (`name`/`value`), `mergeAskQuestionArgs`, `isAskUserQuestionTool`; fallback card with "Reply in message box" instead of infinite spinner.
- `AIChatPanel.tsx` — `dockedAskCall` merges hook snapshot + call args; clears cache on answer/dismiss.
- `App.css` — neutral Cursor-style dock (no orange left bar; hover rows, monochrome indicators).

Docs: **`073-ask-user-question-dock.md`** (new); cross-refs in `006`, `015`, `022`, `CLAUDE.md`.

## Plan harness — Build handoff to Milo + Plan explore auto-allow

**Problem:** Jack in CC Plan mode flooded permission cards on every Task/subagent call. "Allow all" flipped to Auto and Jack could skip formalizing the plan. "Approve & start" on `ExitPlanMode` had Jack implement instead of handing off to Milo.

**Fix (Cursor-style):**
- `ClaudePermissionOverlay` — `PLAN_EXPLORE_TOOLS` auto-allow in Plan mode (`Task`/`Agent`/`TaskCreate`/`TaskUpdate`/`TaskList`/`TodoWrite`). `ExitPlanMode` card: **Keep discussing** (deny) vs **Build** (handoff + allow). No Allow all on plan card.
- `quackPlanHarness.handoffStoryToBuilder` — approve draft story, `createWorkFromStory`, link `W-NNN`.
- `AIChatPanel` — `onPlanBuild` → handoff + `applyPreset("builder")` + `bypassPermissions`.
- `StoryPlanDrawer` — **Build** in panel head when `planning`.
- `ComposerWorkBar` — **Build with Milo** menu item.

Docs: `015-claude-permission-mode.md`, `061-plan-mode-tab.md`, `068-quack-plan-harness.md`.

## Team subagent click — black screen fix

**Problem:** Clicking a subagent card on the Team tab (e.g. `code-explorer`) blanked the main editor — black pane until full app reload. Tab bar sometimes showed `code-explorer.md` as active with empty content.

**Root cause:** `WhiteboardOrganigramma` used `openFile()` then `moveTabToDrawer()`. `openFile` always appends the file to the active pane and sets it **active**, hiding Team. For one or more frames the file host did not paint (`WorkspaceShell` gates pane file portals on `editorsReady`; preview-only markdown in the drawer path had the same gate in `TabContentHost`).

**Fix:**
- `store.openFileInDrawer(wsId, path)` — `bufferFileIfNeeded` + `moveTabToDrawer`; main pane active tab unchanged.
- `WhiteboardOrganigramma.AgentNode` — `writeEditorMdView("preview")` + `openFileInDrawer`.
- `fileComposerDrag` — drawer drop uses `openFileInDrawer` (same bug class).
- `TabContentHost` — markdown files in the drawer render without waiting for `editorsReady`.

Docs: `018-whiteboard-organigramma.md`, `063-surface-view-prefs.md`, `055-file-composer-drag.md`. Files: `store.ts`, `WhiteboardOrganigramma.tsx`, `TabContentHost.tsx`, `fileComposerDrag.ts`.

## Smooth streaming — char-by-char typewriter (v4)

**Problem (follow-up):** After rAF `streamPaint`, Claude Code replies still felt like **word blocks**; caret was a detached grey bar with harsh `steps()` blink.

**Fix:**
- `typewriterReveal.ts` — `charsToReveal`: max **2 chars/frame**, ~54 chars/s, gentle catch-up when buffer runs ahead.
- `useTypewriterReveal.ts` — rAF loop; reads `targetRef` (effect deps only `active`).
- `StreamingPlainText.tsx` — inline caret inside `.ai-stream-plain-text`.
- `App.css` — 1.5px `currentColor` caret, soft opacity pulse.

Rejected: Vercel `ai`/`smoothStream` (server-only); word-based `Intl.Segmenter` catch-up (still chunky).

Docs: `069-smooth-streaming.md` (iteration table + Vercel cross-ref); `006`, `049`, `CLAUDE.md`.

