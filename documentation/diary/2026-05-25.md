---
type: diary
project: quack-app
date: 2026-05-25
---
- [HH:MM] (Alek) Refactor tab Pend. del ChangesPanel: ora mostra tutto il working tree (`git_status_summary`) in due sezioni collassabili `Unstaged (N)` / `Staged (N)`, replicando il pattern del Git drawer. Prima il tab era vuoto se non c'erano modifiche di sessione. Tab Comm. invariato (file della sessione già committati). Aggiunto `onUnstage` a `FileRow`, nuovo `handleUnstageRel` nello hook, badge Pend. = `unstaged + staged`. Feature doc: `documentation/features/067-pending-tab-staged-unstaged-sections.md`.
- [09:16] (Auto) Sessione: 7 file letti (~12.3k tok), 1 letture ripetute, ~2.7k tok risparmiati | Brain: 13 gotcha warnings, 33 breadcrumbs
- [10:51] (Auto) Sessione: 3 file letti (~22.0k tok) | Brain: 14 gotcha warnings, 41 breadcrumbs
- [11:02] (Auto) Sessione: 19 file letti (~248.5k tok), 5 letture ripetute, ~571.6k tok risparmiati | Brain: 4 AST hits, 63 gotcha warnings
- [HH:MM] (Alek) Quack Remote PWA: rifatta come mini Task Hub mobile-first. Backend: aggiunto `SessionLiveStateMap` + 4 nuovi `WsEvent` (`SessionStreaming`, `PendingQuestion`, `MessageAdded`, `SessionUpdated`), 3 Tauri command `notify_session_streaming/_pending_question/_message`, endpoint `GET /api/project-colors`, `SessionSummary` arricchito (`updatedAt`, `lastMessageRole`, `lastMessageStatus`, `isStreaming`, `pendingQuestionCount`, `projectColor`). Desktop: nuovo hook `useRemoteLiveStateSync.ts` che osserva `chatStore` (loading/pending/last-msg) con debounce 150ms e invoca i `notify_*` solo se la remote API è abilitata. PWA (`src-tauri/static/`): tab `Task Hub` diventa home (sostituisce `Sessions`), 4 sezioni priorità con colori `#a855f7/#22c55e/#f59e0b`, project chip colorato, FAB `+` 3-step (project → agent → prompt) che chiama `/api/execute`, nuovi WS handlers patchano `state.sessions` in place + fallback polling 5s dopo 10s di WS down. Tutto integrato senza breaking change sugli endpoint esistenti. File: `remote_ws.rs`, `remote_api.rs`, `lib.rs`, `src/hooks/useRemoteLiveStateSync.ts`, `App.tsx`, `static/app.js`, `static/style.css`.
- [11:17] (Auto) Sessione: 8 file letti (~234.0k tok), 3 letture ripetute, ~106.5k tok risparmiati | Brain: 1 AST hits, 126 gotcha warnings, 69 breadcrumbs
- [11:30] (Auto) Sessione: 1 file letti (~5.0k tok) | Brain: 5 gotcha warnings
