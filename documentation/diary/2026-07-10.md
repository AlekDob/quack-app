---
type: diary
project: codetta
date: 2026-07-10
---
- [Feature] (Alek) New AI chat tabs now open an anchored "Name this session" popover immediately (hub New chat, Agent Mode empty state, pane empty state, ⌘⌃N). `addNewAIChat` centralizes create+focus+prompt; `NewChatNamePopover` locks the title via `renameAIChat` on Start; Skip keeps the auto `AI Chat N` default. Branch/multitask paths unchanged (branch keeps derived title; multitask uses default anchor). `addNewAIChat.ts`, `newChatNamePrompt.ts`, `NewChatNamePopover.tsx`.
- [Feature] (Alek) Right-click on a project icon (ActivityBar + Agent Mode rail) now shows **New chat here** at the top of the workspace popover — switches to that project if needed, creates a chat tab, and opens the session-name prompt anchored on the icon. `WorkspaceColorPopover.tsx`, `ActivityBar.tsx`, `AgentModeShell.tsx`.
- [Feature] (Alek) Agent Hub **Done** section header has a `…` menu with **Reopen all** and **Archive all** bulk actions across every done session in the hub. `AIChatsRail.tsx`.
- [Feature] (Alek) **Composer context bar** — Cursor-style path + git branch inside the composer pill (`050-composer-context-bar.md`). Portaled menus (`ComposerCtxMenu`), project switch / Open folder → new chat + name prompt, `GitBranchPicker` shared with Source Control, `displayTildePath`. Files: `ComposerContextBar.tsx`, `WorkspacePathPicker.tsx`, `GitBranchPicker.tsx`, `composerCtxMenu.tsx`, `AIChatPanel.tsx`, `SourceControlPanel.tsx`, `pathUtils.ts`, `App.css`.
- [UI] (Alek) **Copyable markdown code blocks** — fenced blocks in chat/preview/drawers render as a rounded pill with a copy icon row underneath (Cursor-style), replacing the hover-revealed top-right Copy button. Single-line shell commands (`bash`/`sh`/empty lang) get light token coloring (`--tool-bash` / `--tool-search`). Feature doc `049-markdown-renderer.md`; cross-refs in `003`, `006`, `027`, `CLAUDE.md`. Files: `markdown.ts`, `MarkdownPreview.tsx`, `App.css`.
- [UI] (Alek) **Title bar project-color gradient** — ambient wash on the topbar when a workspace has a color (`.topbar.has-ws-color::before`, theme-tuned `--topbar-ws-tint-*`); same in dev and production. `TopBar.tsx`, `workspaceColors.ts`, `App.css`.
- [Feature] (Alek) **Claude Code guided sign-in** (`052-claude-code-login-ux.md`) — `claude_auth_status` probes Keychain/credentials without the rate-limited usage API; shimmer banner above composer; `terminal.claude_login` opens 440px terminal on `/login`; PTY watcher shows success toast and auto-closes tab; Model browser CLI vs signed-in; chat stderr rewrite. Files: `claude_usage.rs`, `claudeAuthStatus.ts`, `claudeLoginTerminal.ts`, `ClaudeLoginBanner.tsx`, `actions.ts`, `AIChatPanel.tsx`, `ModelBrowser.tsx`, `claudeCode.ts`, `App.css`.
- [Feature] (Alek) **Agent commit dock** — Cursor-style pill above the composer when the agent runs `git commit` via Bash: short hash, message, relative time, push badge (`Pushed` / `Local` / `↑N`). Live detection on `tool_result`, hydrate from transcript, `git status` ahead/upstream poll. `051-agent-commit-dock.md`; `AgentCommitDock.tsx`, `agentCommitDetect.ts`, `agentCommitStore.ts`, `AIChatPanel.tsx`, `App.css`.
- [Feature] (Alek) **Composer voice dictation** — Cursor-style mic in the composer: recording row (waveform, timer, ✕/✓) replaces the textarea; native macOS `SFSpeechRecognizer` via `speech` crate (`dictation.rs`), Web Speech API fallback on Windows/WebView2. `052-composer-voice-dictation.md`; `ComposerMic.tsx`, `dictation.ts`, `AIChatPanel.tsx`, `Info.plist`, `App.css`.
- [Feature] (Alek) **Composer git actions** — Cursor-style changed-files list + **Changes +N −M** + **Commit & Push** split button inside the composer pill (`053-composer-git-actions.md`); `git_diff_stat` backend, stage-all commit flows, branch/commit/push menu, composer draft prefills commit message. `ComposerGitActions.tsx`, `composerGitOps.ts`, `git.rs`, `ipc.ts`, `AIChatPanel.tsx`, `App.css`.
- [Fix] (Alek) **Foreign Claude sessions unusable (permission spam)** — PreToolUse hook in workspace `settings.local.json` used `permissionDecision:'ask'` when `CODETTA_PERM_HOOK` was absent, forcing a hook-level prompt on every gated tool in terminal Claude and Claude Desktop (ignoring `permissions.allow` and `bypassPermissions`). Foreign sessions now emit `allow` (no-op); only Quack-spawned sessions POST to the localhost server. Patched `claude_code.rs` + Virgilio hook; doc `015` updated.
- [Fix] (Alek) **Context ring inflated on Claude Code** — `result.usage` summed cache reads across tool-loop API calls (32% vs CC `/context` ~6%). Now use last API snapshot from `message_start`/`message_delta` (`contextTokensFromApiUsage`). Docs: `023`, gotcha `cc-context-ring-result-usage.md`.
- [Fix] (Alek) **Context vs plan conflated in hero** — ring/drawer hero showed OAuth plan 5hr % (46%) when context snapshot was missing, while CC `/context` showed 22%. Hero/ring now context-only; plan limits stay in their section. JSONL fallback `claude_session_context_usage` polls when stream absent (terminal CC / attach).
- [Fix] (Alek) **Stop/Esc scoped per session during multitask** — with multiple mounted `AIChatPanel`s (background agents), Esc cancelled every in-flight turn; Stop click was already per-panel. `chatVisible` prop from `AIChatHost` / `AgentChatHost` gates the window Esc listener. Docs: `022` § Stop, `046`, `001`. Files: `AIChatPanel.tsx`, `WorkspaceShell.tsx`, `AgentModeShell.tsx`.
- [UI] (Alek) **Sticky user bar compact** — tall stuck prompts collapse to ~68px (24px thumbs + ~2 lines); `UNSTICK_GAP_PX` hysteresis on sentinel; no `scrollHeight` read while compact (expand loop fix). `030-user-message-bar.md`; `useUserBarSticky.ts`, `UserMessageBar.tsx`, `App.css`.
- [UI] (Alek) **Turn stream staleness** — `Still working (Ns)` shimmer → `Unusually slow` + inline Stop at 30s; `StaleSuffix` in `TurnStreamStatus.tsx`.
- [UI] (Alek) **Copyable code blocks** — pill + icon row underneath; shell one-liner token colors refined. `049-markdown-renderer.md`; `markdown.ts`, `chatToolRender.tsx`, `App.css`.
- [Feature] (Alek) **Composer dictation hardening** — macOS native path retries, Web Speech lifecycle, meter smoothing. `052-composer-voice-dictation.md` cross-ref; `dictation.ts`, `ComposerMic.tsx`.
- [Fix] (Alek) **White Monaco editor in dark app mode** — custom `quack-*` themes were registered in `onMount` (after Monaco applied `theme`), so dark mode fell back to built-in `vs` (white). `EditorPane` + `SimpleMonacoEditor` now use `beforeMount={ensureEditorColorThemes}` like `DiffView`; `applyEditorColorTheme` queues until Monaco registers. See `033-editor-color-themes.md` § Monaco mount lifecycle. `editorColorThemes.ts`, `EditorPane.tsx`, `SimpleMonacoEditor.tsx`.
- [Feature] (Alek) **Pinky Brain integration** — full L2+L3: Rust `pinky.rs` bridge (search, setup, stats, telemetry, global migration), Brain tab `brain:<wsId>` (activity bar), pre-turn hybrid inject into chat (`brainInject.ts`, CC-safe `ccTurnContext`), `BrainTurnChip` with savings vs Grep+Read, animated dashboard (`BrainCharts`, `useCountUp`, zero deps), Cursor-style search (shimmer skeleton, term highlight, path on hover), responsive container queries on `.brain-panel-inner`, `openBrainDoc` for split-pane file open. `054-pinky-brain-integration.md`.
- [Fix] (Alek) **Pinky setup froze entire app** — `pinky_setup` / `reindex` ran as sync Tauri commands on the main thread; first `pinky reindex` loads the embed model (~60s) and blocked all UI. All `pinky_*` IPC now `async` + `spawn_blocking`; setup shows non-blocking busy copy. `pinky.rs`, `BrainPanel.tsx`, `054`.
- [Fix] (Alek) **Pinky init stdin hang** — `pinky init` prompted interactively for CC hooks (`[S/N]`) and blocked headless spawns. Quack setup now passes `--no-hooks` (hooks owned by Quack CC bridge). `pinky.rs`, `054`.
- [Fix] (Alek) **Pinky reindex silent / looks empty** — first reindex takes ~60s while `brain.db` is missing; UI showed 0 entries with no loader. `busyKind` (`setup`|`reindex`) drives info toast, header shimmer, skeleton rows, dashed “No index yet” callout, success toast on finish. `BrainPanel.tsx`, `App.css`, `054`.
