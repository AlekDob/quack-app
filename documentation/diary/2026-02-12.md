---
type: diary
date: 2026-02-12
---
# 2026-02-12

- Fixed TeammateWidget drill-down: removed isStarting from isClickable condition, preserved sessionId on stop events
- Diagnosed team persistence race condition: loadActiveTeam called before agents ready, fixed with reactive useEffect on agentCount
- Fixed IDEOnboarding infinite render loop (Maximum update depth exceeded): bare `useIDEStore()` destructuring + store updates in effect. Fixed with individual selectors and `hasLoadedRef` guard
- Added `.quack/teams/` to gitignore, removed 5 cached team JSON files
- Fixed TeammateWidget sessionId reactivity: changed from `getState()` (static) to reactive selector
- Fixed Quack Store agent-bundle installation: implemented `onAgentBundleInstalled` callback chain creating proper terminal in sidebar
- Fixed Stamina Context Receipt Messages: 0 bug (three independent root causes): 1) Rust null deserialization for cache tokens, 2) missing chatTokensMap update in trackUsage, 3) StaminaBarBorder double-counting overhead
