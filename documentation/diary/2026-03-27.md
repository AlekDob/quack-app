---
type: diary
project: quack-app
date: 2026-03-27
---

- [16:00] (Alek) LSP research session — verified Claude Code's native LSP tool (goToDefinition, findReferences, hover, documentSymbol, workspaceSymbol, goToImplementation, callHierarchy). Confirmed Quack already has tree-sitter-based code-intel MCP server (4 tools) + IDE context integration that pulls LSP diagnostics from external IDEs.

- [16:30] (Alek) HTML Visualizer improvements — DEFAULT_HEIGHT changed from 300px to 650px for better out-of-box visibility. Fixed anchor navigation bug in sandboxed srcdoc iframes: `<a href="#id">` links were navigating away from content, now intercepted with `scrollIntoView()` in the injected AUTO_RESIZE_SCRIPT. Updated pattern doc to reflect that JS works in sandbox (`allow-scripts`), correcting previous "no JS" documentation.

- [17:00] (Alek) Skill code-navigation created/updated at `~/.claude/skills/code-navigation/` — provides background knowledge for intelligent code navigation using both MCP code-intel tools (tree-sitter) and native LSP tool. Available globally for code-explorer and code-reviewer agents.

- [19:00] (Alek) chore: bump `@anthropic-ai/claude-agent-sdk` 0.2.81→0.2.85. Key changes in 4 versions: `reloadPlugins()` method, `taskBudget` for API-side token budget awareness, `enableChannel()` for SDK-driven MCP activation, `seed_read_state` control subtype for Edit after Read removed from context, `session_state_changed` events now opt-in, fix for PreToolUse hooks with `permissionDecision: "ask"`. Updated skill references (SKILL.md, rules, template) from stale v0.2.47 to v0.2.85.
