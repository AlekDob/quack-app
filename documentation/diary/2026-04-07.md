---
type: diary
project: quack-app
date: 2026-04-07
---
- [10:00] (Alek) Split View tab drag between groups: implemented onTabSplit/onCloseSplit via drag handlers on tab-bar-group-left/right divs. Chat tab locked to left group. Fixed isDraggingTab not resetting on drop (overlay stayed visible blocking interactions). Fixed split not closing when switching agent/terminal (useEffect on activeId). Fixed tab color in right group (forced #fff with !important). Removed blue borders from split-active tab.
- [11:00] (Alek) Sidebar drag-to-split: SplitDropZone now accepts 4 sidebar MIME types (quack-file, quack-skill, quack-rule, quack-command). Added draggable to RuleItem and CommandItem. App.tsx: new isDraggingSidebar state, resolveTabFromSidebarDrop creates tabs on the fly, handleContentDragOver detects sidebar drag via dataTransfer.types. Drop zone shows for sidebar drags even when split is already active (allows replacing pane content). Click on split tab no longer switches left pane.
- [10:28] (Auto) Sessione: 4 file letti (~46.1k tok), 1 letture ripetute, ~102.3k tok risparmiati
- [10:30] (Auto) Sessione: 1 file letti (~34.1k tok)
- [12:00] (Alek) fix(snippets): tag expansion non funzionava dopo creazione snippet nel modal. Root cause: useSnippets() crea stato locale indipendente per ogni componente — SnippetModal e ChatInput avevano liste separate. Fix: refreshSnippets() al close del modal. Creata feature doc 050-prompt-snippets e bug doc fix-snippet-modal-stale-tags.
