---
type: diary
project: codetta
date: 2026-07-15
---
- [12:35] (Alek) **Perché Quack consuma le sessioni Claude Code più in fretta del desktop** — indagine + A/B controllato. CAUSA: i prompt iniettati da Quack ("read 5+ relevant files", "Be thorough and substantive" in `jackSystemPrompt`/inside-Quack block) spingono un tool-loop molto più profondo per turno; ogni giro rifattura l'intero contesto (~200k). Dati reali: 27.7 vs 5.4 chiamate API/turno utente (~5×). A/B stesso task/modello: bare $0.46 (24 call) → quack-prompt $1.26 (42 call) → quack+effort $0.71 (30 call), cioè 1.5–2.7×. SCAGIONATI: cache (~96-97% hit in entrambi), peso del testo iniettato (~43 tok mediani, 3% dei turni), effort flag. Fix proposto (non applicato): ammorbidire le direttive di esaustività per l'orchestrator CC + effort opt-in. Dettaglio: `decisions/004-quack-token-consumption.md`.
- [13:30] (Alek) **Fix consumo CC — ridisegno prompt per-agente + pilot retrieval**. Nuovo `quackAgentCorePrompt` (persona-aware: Nora parla da Nora, non più "You are Jack") sostituisce `jackSystemPrompt`; blocco EFFICIENCY ("match effort, locate before reading, reuse, stop when done") al posto di "read 5+ files"; via "Be thorough and substantive". Comportamento di ruolo spostato/affinato nei preset (`instructions.ts`): Milo ladder Ponytail, Nora bound 2-ipotesi, Vera solo-diff, Lia zero-tool; Jack nuovo blocco Planner (`builtins.ts`, con la guida Works). Vera effort medium→low. RE-MISURA A/B (stesso task/modello): vecchio prompt Quack $1.26/42 call → nuovo $0.54/16 call = **−62% costo e chiamate**, ~parità col CLI nudo ($0.46/24). PILOT retrieval: Pinky **non è leva di efficienza** (default OFF, inject passivo, savedTokens euristico) — tienilo per la conoscenza; Graphify grafo cross-language TS+Rust locale in 5s, `query/explain` collassano la discovery ($0), arm D 24→17 call, MA non scende sotto il nudo (costo dominato da cacheRead) e non attraversa il confine Tauri IPC. Raccomando Graphify opt-in, prompt è la leva dominante. Dettaglio: `decisions/004-quack-token-consumption.md`.
- [14:20] (Alek) **Works come leva di efficienza + auto-tracking dagli agenti** (segue il fix prompt). FASE 1a: manifest story/work inviato pieno solo al 1° turno (o quando cambia), poi puntatore compatto — gate `worksManifestGate.ts` (`manifestForTurn`); taglia il costo per-turno che Works aggiungeva. FASE 1b: scope flywheel READ-ONLY (niente scritture nei .md): il manifest ora elenca "Files in scope" derivati dai file già editati nelle sessioni linkate (`collectStoryScopeFiles` + `formatScopeSection`) → più lavori una story, più punta ai file giusti. FASE 2: auto-tracking — `worksAgentDirectives.ts` parsa a fine turno i direttivi emessi dall'agente (`[Works link] S-NNN`, `[Works new-story]`, `[Works new-work]`, `[Works update]`) e applica via API esistenti (createStory/updateStory/linkStoryToChat); protocollo nel `quackClaudeCodeEditorPrompt(worksEnabled)` con TEMPLATE efficienza (Scope/Entry points/Decisions/Acceptance = la "memory card" che fa saltare la ri-esplorazione); indice open-stories iniettato UNA volta su chat slegata; ogni azione via toast (+ board = vista d'insieme). GOTCHA risolto: baseline per-chat (`lastWorksChatRef`) per non ri-applicare i direttivi dello storico allo switch/reload (creava story duplicate). Niente delete via direttivi. MISURA A/B (arm E, story linkata): il manifest fa leggere il feature-doc diretto (3 Read) ma il costo a n=1 è dominato dalla varianza dei subagent; leva prompt resta dominante. Dettaglio: `decisions/004-quack-token-consumption.md`. — bug nel ponte permessi CC↔Quack (`015-claude-permission-mode.md`). CAUSA: `ensure_pretooluse_hook` in `claude_code.rs` fa dedupe delle proprie entry in `.claude/settings.local.json` guardando solo il marcatore `_codetta: true`; una entry legacy scritta da una build precedente (senza marcatore) sopravviveva per sempre accanto a quella fresca. Quella legacy, se `CODETTA_PERM_HOOK` non è nel processo hook, risponde sempre `'ask'` — e Claude Code combina più hook `PreToolUse` prendendo la decisione più restrittiva, quindi quell'`'ask'` vinceva sempre, anche con la UI su Bypass (il frontend/`ClaudePermissionOverlay` non veniva mai interpellato). FIX: il dedupe (`claude_code.rs:399-419`) ora riconosce anche le entry legacy tramite l'impronta univoca del comando (`/permission?token=`), non solo il marcatore; ripulita a mano anche la entry stale nel `.claude/settings.local.json` di questo progetto. Serve riavviare/ricompilare l'app Tauri perché il fix si applichi ai prossimi spawn.
- [16:19] (Alek) **Fix regressione "Quack va lentissimo" — `startTransition` che affamava lo stream**. Un agente (OpenCode) aveva avvolto gli update del painter di streaming (`setStreaming`/`setStreamingBlocks` e i tool-call) in `startTransition` dentro `AIChatPanel.tsx`, per "tenere reattivo il composer". Ma quegli update SONO il contenuto principale e sono già coalizzati a 1 paint/frame da `createStreamPainter` (rAF): renderli a bassa priorità li fa morire di fame — ogni frame la transition in corso viene abortita da quella del frame dopo, React su un componente da ~7000 righe committa raramente → stream a scatti/bloccato e CPU al massimo. FIX: rimossi i due `startTransition` (torna urgente) + import. TENUTE le altre modifiche del batch, che sono ottimizzazioni valide e verificate: `toolResultsById`/`userTurnByIdx` memoizzati su `messages` (indici equivalenti al vecchio calcolo da `display`), `MarkdownPreview`/`StreamingPlainText` in `memo()`, `openChatFile` in `useCallback`. Il fix Rust dedup-hook in `claude_code.rs` è a sé (permessi) e non c'entra con la velocità. LEZIONE: `startTransition` è la leva sbagliata per aggiornamenti ad alta frequenza che sono la UI primaria; il composer va isolato con una sottostruttura memoizzata, non deprioritizzando lo stream.
