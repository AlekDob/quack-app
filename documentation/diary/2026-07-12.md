- [Fix] (Alek) **Agent Hub hover expand** — collapsed rail now grows to 240px in-flow on hover (not a broken 44px overlay); mouse handlers on the full panel so you can click chats while peeking; chevron still pins open.
- [Feature] (Alek) **Editor tab drawer** — drag any tab to the far-right 56px strip of the editor area to open it in a resizable right drawer (not an inline split). Width persists in `layout.editorDrawer`; drag header back into a pane to dock; `TabContentHost` portals file/AI/plan/etc. into the drawer body. — Plane-inspired tickets: `.codetta/works/` persistence (`works_store.rs`), Works tab (`works:{wsId}`), list + kanban views, issue drawer, `ComposerWorkBar` (hotfix/plan/blank), Plan mode draft on enter + approve on `ExitPlanMode`, `workItemId` on chat descriptor + context inject on send. Doc: `054-works-layer.md`.
- [Feature] (Alek) **Works layer v3 — Plane views + drawers** — Left sidebar is a **views collection** (All, In progress, Todo, Backlog, Completed, Cancelled, Modules) with count badges; `sidebarView` in `viewPrefs`. **Modules** view shows `WorksFeaturesCatalog` (Brain search bar + `.brain-hit-row` list) in the main panel, not in the rail. **Work drawer** redesigned Cursor-style: single surface, label→value properties grid, icon actions, fade-up animations. **Feature doc drawer**: strip frontmatter/duplicate H1 (`featureDocPreview.ts`), dedupe title (`featureDisplayTitle` / `featureFileLabel`), Open in editor closes drawer. Context menu on work rows; timeline week nav + bar drag/resize. Doc: `054-works-layer.md`, skill `quack-works`.
- [Feature] (Alek) **Preset system** (`062`) — backend-agnostic working modes (Milo/Nora/Vera + user-created) that shape the CURRENT session, explicitly NOT subagents (no isolated context, no Task spawn). Pure-domain module `src/presets/` separates shipped defaults from user overrides (`lcp.presets.v1`) from the resolved `EffectivePresetConfig`; model defaults are an abstract `ModelTier`, never a hardcoded model name, so each backend maps it to its own concrete model and degrades gracefully. Configurable + creatable from the organigramma: `AgentCreateDrawer` (Cursor-style right slide-over, create AND edit — `updatePreset()` rewrites the same file in place so a renamed custom preset keeps its slug/id) + `AvatarPicker` (35-duck grid + native upload, durable via new `save_persistent_image` Rust command since the chat-attachment pipeline is temp-dir-only). Removed the organigramma's skill-linking DnD (chips + free pool) — pure clutter, unrelated to presets. Merged the composer's two agent pickers into one `SubagentPill`: "primary agents" (Jack + presets) on top, real delegable subagents below a divider — picking a preset there calls the same `applyPreset` as the (now-deleted) standalone `PresetPopover`. Security note: custom presets are workspace `.md` files (`.codetta/presets/`, same trust tier as `loadWorkspaceRules`) — their appended instructions are now explicitly labeled `[Preset "X" — from this workspace's .codetta/presets/, not verified by Quack]` in the system prompt so they can't silently pass as an authoritative Quack directive (basic prompt-injection hygiene; the file only takes effect if the user deliberately selects it, never auto-applied on opening a workspace). Files: `src/presets/*`, `src/components/{AgentCreateDrawer,AvatarPicker,SubagentPill,WhiteboardPresets,WhiteboardOrganigramma}.tsx`, `src/subagents.ts` (exported `DUCK_COUNT`), `src-tauri/src/fs_ops.rs`, `App.css`.
- [Feature] (Alek) **Preset system — built-ins now editable too** — Milo/Nora/Vera have no backing file, so "editable" for them means an override layer, not a file write. New `effectivePresetDefinition(def, overrides?)` (`resolvePresetConfig.ts`) is the one merge point: folds label/role/avatar/modelTier/effort/thinking/outputStyle/instructions overrides onto the shipped definition, used by BOTH resolution (`resolvePresetConfigFor`) and every display site (composer picker, organigramma card) so an edit shows up everywhere consistently. `AgentCreateDrawer` now branches on `editing.source`: custom → `updatePreset(path)` (unchanged), builtin → `setPresetOverrides(id, {...})`, plus a "Reset to default" footer button (`clearPresetOverrides`). `AIChatPanel` subscribes to `subscribePresetSettings` so an edit made from the organigramma reflects in the composer picker without remounting the chat. Verified end-to-end with a runtime script: rename+retier+re-instruct Milo → `resolvePresetConfigFor` correctly resolved the new tier to `claude-code:opus`, then reset cleared it back to shipped defaults. Files: `src/presets/{types,resolvePresetConfig,index}.ts`, `src/components/{AgentCreateDrawer,WhiteboardPresets,AIChatPanel,WhiteboardPane}.tsx`, `App.css`.
- [Fix] (Alek) **Preset composer picker — 2 more fixes** — (1) removed the "Subagents" section from `SubagentPill`'s dropdown entirely per Alek: it's a technical catalog dump, not a primary-agent choice; delegation still works via `@name` typed in the composer (`MentionSuggestions`, unaffected). Dropdown now lists only Jack + presets. (2) "switching preset only changes effort, not model" — root cause: builder/debugger/reviewer ALL shipped with `modelTier: "balanced"`, a data gap left over from removing the old "planner" (the only preset that used `reasoning`/Opus). Rebalanced: debugger (Nora) → `reasoning` (root-cause work benefits most from the stronger model), builder/reviewer stay `balanced`. Verified with a runtime script: builder/reviewer → `claude-code:sonnet`, debugger → `claude-code:opus`. Files: `src/components/SubagentPill.tsx`, `src/presets/builtins.ts`, `App.css`.
- [Feature] (Alek) **Tab/Shift+Tab composer shortcuts** — `Tab` (no modifiers, composer focused) cycles the active primary agent (Jack → Milo → Nora → Vera → back to Jack, calling the existing `applyPreset`); `Shift+Tab` cycles the Claude Code permission mode (Ask → Plan → Auto-edit → Auto → Bypass). Both placed in the textarea's `onKeyDown` AFTER the @-mention and slash-command blocks so autocomplete keeps first claim on Tab when either popover is open. Extracted the mode list into a shared `PERM_MODE_OPTIONS` constant (was duplicated inline in the `.ai-mode-menu` JSX) so the menu, the mode button's label, and the new Shift+Tab cycle all read one source of truth. Composer hint row now advertises "Tab agent" / "Shift+Tab mode". File: `src/components/AIChatPanel.tsx`.
