- [Feature] (Alek) **Works ↔ Brain doc refs** — Manifest inject (`worksTurnContext.ts`): paths + feature outline + acceptance, no 1200-char body dump. `worksBrainRefs.ts` resolves primary/related/extra; frontmatter `refs:` on work/story; drawer Documentation section + composer `N docs` chip; inject depth pointers/outline/pinky. Files: `featureDocOutline.ts`, `WorksDocRefsSection.tsx`, `ComposerWorkBar.tsx`.
- [Refactor] (Alek) **Two bundled skills** — Merged `feature-creator` into `quack-works` v6 (Part A: feature docs/modules; Part B/C: stories/tickets/cycles). `quack-brain` v6 = search/save only. Removed `src/bundledSkills/feature-creator.md`.
- [Feature] (Alek) **feature-creator ↔ Works integration** — `feature-creator` app-bundled in `src/bundledSkills/` (step 7: module sync, stories S-NNN, tickets W-NNN). `quack-works` / `quack-brain` v5 PM loop starts with `/feature-creator`. Docs `054`, `066`, `documentation/skills/feature-creator/`.
- [Feature] (Alek) **App-bundled Works skills** — `quack-works` + `quack-brain` ship in `src/bundledSkills/` (v4: cycles, stories, list grouping, frontmatter examples). `ensureAppBundledSkills` syncs to `<ws>/.claude/skills/` on workspace open + Works hydrate; slash menu `source: app`. Canonical markdown mirrored in `documentation/skills/`.
- [Feature] (Alek) **Works cycles + stories (`066`)** — Storage moved to git-trackable `works/` at workspace root (`worksDir.ts` migrates `.quack/works`). **Cycles:** auto ISO weeks, Plane-style dashboard (progress bar + SVG burndown + priority list). **Stories:** `works/stories/S-NNN.md`, `StoryDrawer`, spawn backlog `W-NNN` with `parentId`; cycle/story pickers in `WorkItemDrawer`. Snapshot v3. Activity bar check-square → Works (replaces TODO sidebar).
- [UX] (Alek) **Context usage popover (Cursor-style)** — Replaced the composer **Context & Usage slide-over drawer** with a **popover** anchored above the context ring (same dismiss pattern as `EffortPopover`: backdrop, Esc, liquid glass). Popover shows hero `% Full`, token total, **segmented bar**, and per-category rows: system prompt, tool defs, rules, skills, MCP, subagents, conversation (`contextBreakdown.ts` + `claude_context_assets` / `loadWorkspaceRules` / `claude_mcp_list`; conversation = live API remainder). Estimated rows prefixed `~`. Plan limits, This chat billing, and Quack spend moved to Usage dashboard (footer link). Removed `SessionUsageDrawer.tsx`; types → `sessionUsageLocal.ts`. Docs: `023`, gotcha `context-breakdown-estimates`, `022`/`CLAUDE.md` cross-refs.
- [UX] (Alek) **Editor tab drawer → full-height overlay** — Works/Brain/Team drawer is now `position: fixed` (portal), full app height below topbar, default width 680px, z-index 900; resize no longer pushes editor or Agent Hub.
- [Feature] (Alek) **Surface view prefs** (`063`) — Works / Brain / Team open in **side drawer** or **editor tab**; Works defaults to drawer; Settings → Views segmented control; `surfaceViewPrefs.ts`, `openSingletonSurface` in `store.ts`; activity bar highlights drawer-hosted tabs.
- [Feature] (Alek) **Works v4 — markdown work files** — Each ticket is a real `.md` at `.quack/works/items/W-NNN.md` (YAML frontmatter + body); `snapshot.json` is a slim index (`version: 2`). Agents Read/Write work files like feature docs; v1 `descriptionBlocks` migrate on hydrate; orphan `.md` files auto-import; `worksWatch.ts` reloads on `fsBus`. Work drawer → Notion block editor; context inject includes file path. Files: `workItemMd.ts`, `worksItemFiles.ts`, `worksWatch.ts`, `quackDir.ts`; docs `054`, skills `quack-works` / `quack-brain`.
- [Feature] (Alek) **Agent Hub drawer + chat tab switch perf** (`064`) — collapsed hub: 44px icon chips + hover **overlay drawer** (z-index 80, no editor resize); pinned chevron still 240px in-flow. Chat switch: skip veil same-project, scoped workspace flush, 280ms cap + `onHydrated` early dismiss. Pane tabs: visibility stacking (`ai-tab-host` / `file-tab-host`), Monaco stays mounted hidden. Docs: `009`, `001`, `043`, `064`.
- [Feature] (Alek) **Editor tab drawer** — drag any tab to the far-right 56px strip of the editor area to open it in a resizable right drawer (not an inline split). Width persists in `layout.editorDrawer`; drag header back into a pane to dock; `TabContentHost` portals file/AI/plan/etc. into the drawer body. — Plane-inspired tickets: `.codetta/works/` persistence (`works_store.rs`), Works tab (`works:{wsId}`), list + kanban views, issue drawer, `ComposerWorkBar` (hotfix/plan/blank), Plan mode draft on enter + approve on `ExitPlanMode`, `workItemId` on chat descriptor + context inject on send. Doc: `054-works-layer.md`.
- [Feature] (Alek) **Works layer v3 — Plane views + drawers** — Left sidebar is a **views collection** (All, In progress, Todo, Backlog, Completed, Cancelled, Modules) with count badges; `sidebarView` in `viewPrefs`. **Modules** view shows `WorksFeaturesCatalog` (Brain search bar + `.brain-hit-row` list) in the main panel, not in the rail. **Work drawer** redesigned Cursor-style: single surface, label→value properties grid, icon actions, fade-up animations. **Feature doc drawer**: strip frontmatter/duplicate H1 (`featureDocPreview.ts`), dedupe title (`featureDisplayTitle` / `featureFileLabel`), Open in editor closes drawer. Context menu on work rows; timeline week nav + bar drag/resize. Doc: `054-works-layer.md`, skill `quack-works`.
- [Feature] (Alek) **Preset system** (`062`) — backend-agnostic working modes (Milo/Nora/Vera + user-created) that shape the CURRENT session, explicitly NOT subagents (no isolated context, no Task spawn). Pure-domain module `src/presets/` separates shipped defaults from user overrides (`lcp.presets.v1`) from the resolved `EffectivePresetConfig`; model defaults are an abstract `ModelTier`, never a hardcoded model name, so each backend maps it to its own concrete model and degrades gracefully. Configurable + creatable from the organigramma: `AgentCreateDrawer` (Cursor-style right slide-over, create AND edit — `updatePreset()` rewrites the same file in place so a renamed custom preset keeps its slug/id) + `AvatarPicker` (35-duck grid + native upload, durable via new `save_persistent_image` Rust command since the chat-attachment pipeline is temp-dir-only). Removed the organigramma's skill-linking DnD (chips + free pool) — pure clutter, unrelated to presets. Merged the composer's two agent pickers into one `SubagentPill`: "primary agents" (Jack + presets) on top, real delegable subagents below a divider — picking a preset there calls the same `applyPreset` as the (now-deleted) standalone `PresetPopover`. Security note: custom presets are workspace `.md` files (`.codetta/presets/`, same trust tier as `loadWorkspaceRules`) — their appended instructions are now explicitly labeled `[Preset "X" — from this workspace's .codetta/presets/, not verified by Quack]` in the system prompt so they can't silently pass as an authoritative Quack directive (basic prompt-injection hygiene; the file only takes effect if the user deliberately selects it, never auto-applied on opening a workspace). Files: `src/presets/*`, `src/components/{AgentCreateDrawer,AvatarPicker,SubagentPill,WhiteboardPresets,WhiteboardOrganigramma}.tsx`, `src/subagents.ts` (exported `DUCK_COUNT`), `src-tauri/src/fs_ops.rs`, `App.css`.
- [Feature] (Alek) **Preset system — built-ins now editable too** — Milo/Nora/Vera have no backing file, so "editable" for them means an override layer, not a file write. New `effectivePresetDefinition(def, overrides?)` (`resolvePresetConfig.ts`) is the one merge point: folds label/role/avatar/modelTier/effort/thinking/outputStyle/instructions overrides onto the shipped definition, used by BOTH resolution (`resolvePresetConfigFor`) and every display site (composer picker, organigramma card) so an edit shows up everywhere consistently. `AgentCreateDrawer` now branches on `editing.source`: custom → `updatePreset(path)` (unchanged), builtin → `setPresetOverrides(id, {...})`, plus a "Reset to default" footer button (`clearPresetOverrides`). `AIChatPanel` subscribes to `subscribePresetSettings` so an edit made from the organigramma reflects in the composer picker without remounting the chat. Verified end-to-end with a runtime script: rename+retier+re-instruct Milo → `resolvePresetConfigFor` correctly resolved the new tier to `claude-code:opus`, then reset cleared it back to shipped defaults. Files: `src/presets/{types,resolvePresetConfig,index}.ts`, `src/components/{AgentCreateDrawer,WhiteboardPresets,AIChatPanel,WhiteboardPane}.tsx`, `App.css`.
- [Fix] (Alek) **Preset composer picker — 2 more fixes** — (1) removed the "Subagents" section from `SubagentPill`'s dropdown entirely per Alek: it's a technical catalog dump, not a primary-agent choice; delegation still works via `@name` typed in the composer (`MentionSuggestions`, unaffected). Dropdown now lists only Jack + presets. (2) "switching preset only changes effort, not model" — root cause: builder/debugger/reviewer ALL shipped with `modelTier: "balanced"`, a data gap left over from removing the old "planner" (the only preset that used `reasoning`/Opus). Rebalanced: debugger (Nora) → `reasoning` (root-cause work benefits most from the stronger model), builder/reviewer stay `balanced`. Verified with a runtime script: builder/reviewer → `claude-code:sonnet`, debugger → `claude-code:opus`. Files: `src/components/SubagentPill.tsx`, `src/presets/builtins.ts`, `App.css`.
- [Feature] (Alek) **Tab/Shift+Tab composer shortcuts** — `Tab` (no modifiers, composer focused) cycles the active primary agent (Jack → Milo → Nora → Vera → back to Jack, calling the existing `applyPreset`); `Shift+Tab` cycles the Claude Code permission mode (Ask → Plan → Auto-edit → Auto → Bypass). Both placed in the textarea's `onKeyDown` AFTER the @-mention and slash-command blocks so autocomplete keeps first claim on Tab when either popover is open. Extracted the mode list into a shared `PERM_MODE_OPTIONS` constant (was duplicated inline in the `.ai-mode-menu` JSX) so the menu, the mode button's label, and the new Shift+Tab cycle all read one source of truth. Composer hint row now advertises "Tab agent" / "Shift+Tab mode". File: `src/components/AIChatPanel.tsx`.
- [Feature] (Alek) **Preset mode + Jack is now an agent too** — added `permMode` (Claude Code permission mode: Ask/Plan/Auto-edit/Auto/Bypass) as a full preset field: `PresetDefaults.permMode` + `UserPresetOverrides.permMode`, merged by `effectivePresetDefinition`, resolved into `EffectivePresetConfig.permMode`, editable via a new "Mode" segmented control in `AgentCreateDrawer` (shared `PERM_MODE_OPTIONS` now lives in `src/presets/permModes.ts`, imported by both the drawer and the composer's mode menu/Shift+Tab cycle — one source of truth). Bigger insight: Jack himself is now editable the exact same way. He was never a `PresetDefinition` (he's the root, `presetId === null`) — added `getJackDefinition()`/`JACK_PRESET_ID` in `builtins.ts`, a synthetic definition with no backing file, so "editing Jack" persists to the override store exactly like Milo/Nora/Vera. Lifted the edit-drawer state up from `WhiteboardPresetGroup` into `WhiteboardOrganigramma` so Jack's root card (now clickable) and every preset card share one `AgentCreateDrawer` instance. `applyPreset` unified to resolve either a real preset OR Jack's definition when `id` is null. New chats now silently apply Jack's saved mode/model/effort via `applyJackDefaultsIfConfigured()` — guarded on `getPresetOverrides("jack")` being non-empty, so nothing changes for users who've never touched Jack (no regression to the pre-preset effort/mode defaults). Verified end-to-end: setting Jack to Plan mode + reasoning tier + high effort resolves to `claude-code:opus` + `permMode:"plan"`; reset clears it. Files: `src/presets/{types,builtins,resolvePresetConfig,permModes,createPreset,loadCustomPresets,index}.ts`, `src/components/{AgentCreateDrawer,WhiteboardOrganigramma,WhiteboardPresets,AIChatPanel}.tsx`, `App.css`.
- [UX] (Alek) **Organigramma → Team, full width, new icon** — renamed the tab everywhere user-facing (activity bar label/tooltip, command palette "Open Team", tab label in `PaneNode`/`EditorTabDrawer`/`store.ts`, sub-tab pill, in-tab buttons/legend) while leaving internal identifiers (`WhiteboardPane`, `wb:` tab kind, `WhiteboardOrganigramma.tsx`) untouched — no reason to churn file/type names for a label change. Added a new `"users"` icon to `Icon.tsx` (two-person outline, same stroke-2/24x24 convention as the rest) and swapped every `Icon name="whiteboard"` site to it. Fixed the actual full-width complaint: `.whiteboard-org` had `max-width: 920px; margin: 0 auto`, centering the org-chart in a narrow column regardless of pane width — dropped both, now `width: 100%`. Files: `src/activityBarViews.ts`, `src/actions.ts`, `src/store.ts`, `src/components/{PaneNode,EditorTabDrawer,WhiteboardPane,Icon}.tsx`, `App.css`.
- [Fix] (Alek) **Chat header now shows the active preset, not hardcoded Jack** — every assistant message rendered `<img src="/jack.jpeg">` + "Jack" unconditionally, even after switching to Milo/Nora/Vera in the composer, since nothing snapshotted which agent was active when a message was sent. Added `ChatMessage.agentId?: string | null` (display-only, `src/ai.ts`) set to the live `presetId` at both assistant-message commit sites (`AIChatPanel.tsx`: the unified streaming-loop commit and the process-replay finalize) plus the in-progress streaming bubble, so a message keeps showing its sender's identity even if the user switches agents mid-conversation. New `msgIdentityFor(agentId)` resolves `null`/`undefined` → Jack (`effectivePresetDefinition(getJackDefinition())`), else looks up `presetChoices` for label/role/avatar — same resolution the composer picker already uses. File: `src/ai.ts`, `src/components/AIChatPanel.tsx`.
- [Feature] (Alek) **Per-backend tier→model mapping in Settings (fixes OpenCode/Cursor CLI preset switch)** — root cause of "cambio preset ma il modello non cambia" on OpenCode/Cursor CLI: `capabilities.ts`'s `modelForTier` for both backends pointed every tier (reasoning/balanced/fast) at the same `:default` sentinel, since their model catalogs are discovered live and can't be hardcoded like Claude Code's opus/sonnet/haiku. Rather than hardcode guesses, added a user-configurable override layer: `src/presets/tierModelOverrides.ts` (new store, same map+pub/sub pattern as `presets/settings.ts`, key `lcp.tierModelMap.v1`, shape `{ [backendId]: { reasoning?, balanced?, fast?: ModelId } }`). `resolvePresetConfigFor`'s `resolveModel` now checks it between the explicit per-preset model pin and the static capability default: `ov.model ?? getTierModelOverride(backend, tier) ?? caps.modelForTier[tier]`. New `TierModelSettings` component (Agent Customizations → Providers tab) renders one card per dynamic-catalog backend (Cursor CLI, OpenCode) with 3 selects populated by that backend's own `listModels()` (reuses the catalog Quack already discovers — no new discovery code), writing straight to the override store. Files: `src/presets/{tierModelOverrides,resolvePresetConfig,index}.ts`, `src/components/{TierModelSettings,CustomizationsModal}.tsx`, `App.css`.
- [Fix] (Alek) **Preset model tiers spostato in Settings** — Alek si aspettava il nuovo mapping tier→modello nel modale generico "Settings" (`SettingsModal.tsx`, il TOC con Appearance/Views/Editor/…), non nell'"Agent Customizations" separato (aperto dalla Agent Hub espansa). Rimosso da `CustomizationsModal.tsx`, aggiunta una `Section title="Preset model tiers"` dedicata in `SettingsModal.tsx` subito dopo "AI Providers (Bring Your Own Key)" — stesso componente `TierModelSettings`, ora raggiungibile dal TOC di Settings. File: `src/components/{SettingsModal,CustomizationsModal}.tsx`.
- [Fix] (Alek) **Model picker loader stuck off on first open** — `warmLiveCliCatalogs` bailed silently (`if (!snap) return`) when no disk-cached model-discovery snapshot existed yet (first launch, or after cache expiry), so it never started the slow Claude Code CLI catalog probe and never set `ccCliInflight` — meaning `catalogWarming` stayed false and the picker showed no skeleton/spinner while quietly waiting. Now it awaits `ensureModelDiscovery({force:false})` first before deciding which CLIs to warm. File: `src/modelDiscoveryStore.ts`.
- [Fix] (Alek) **New-chat first message felt frozen, no loader** — `sendUserText` built the whole system-prompt preamble (workspace rules, Pinky Brain extension check, Works context hydration) BEFORE calling `setStreaming("")`, so on a brand-new chat's first turn — the slowest case, everything cold — nothing rendered for a couple seconds: no user bubble, no "Planning next moves…" spinner, no `ai-status-dock`. Added an early `flushSync(() => setStreaming(""))` right after the hard-cap check so the turn-status dock lights up immediately; the user message bubble still lands once the preamble finishes assembling. File: `src/components/AIChatPanel.tsx`.
- [Docs] (Alek) **Agent Tasks checklist documented (`067`)** — new feature doc for the Cursor-style collapsible task checklist (`AgentTasks` in `AgentModeShell.tsx`), sourced from `aiTaskStore.ts` pub/sub store (published by `AIChatPanel.tsx` from TodoWrite/TaskCreate stream events). Determined it is a new structural component, not a fold-in to `009-agent-hub.md` (that doc covers the separate `AIChatsRail` cross-project hub). CLAUDE.md Knowledge Base updated with the new entry.
