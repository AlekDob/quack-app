- [UX] (Alek) **Quack Store → editor tab** — moved from cramped sidebar section to full `store:<wsId>` tab (like Brain/Usage); activity bar opens tab directly. Files: `store.ts`, `WorkspaceShell`, `QuackStorePanel`, `App.css`.
- [Feature] (Alek) **Quack Brain + Quack Store** — extension model: Quack Store tab (detect + hybrid `cargo`/`pip` install), Quack Brain hub with dynamic Knowledge/Skills segments, `quack_modules.rs` bridge, SkillOpt-Sleep dry-run/adopt, gated chat inject/save chips + `SkillProposalChip`. Docs: `059`, update `054`. Files: `quackStore/`, `QuackStorePanel`, `BrainKnowledgePanel`, `BrainSkillPanel`, `quack_modules.rs`.
- [UX] (Alek) **Store row layout** — replaced broken 3-col grid (text floated right) with flex `[icon | body | actions]`; single subtitle line; fixed-width actions column for aligned Install/Open + Docs.
- [Fix] (Alek) **Skill Trainer install** — macOS PEP 668 blocked silent `pip install --user`; Rust now tries `pipx` first, then pip with `--break-system-packages`; install errors persist in-row (phase no longer resets to idle when `busyId` clears); post-install re-detect with explicit message if CLI missing. Files: `quack_modules.rs`, `QuackStorePanel`, `StoreExtensionRow`.
- [UX] (Alek) **Activity bar overflow + customize** (`060`) — view-icons are dynamic: height-driven visible count (`useActivityBarViewHeight` + `computeBarIconLayout`), `…` overflow popover (viewport-flipped via `useActivityBarPopoverPosition`), always-on customize grip, two-zone drag reorder (`lcp.activityBar.*`). Sidebar vs tab kinds: horizontal separator + left trace vs bottom pip. AI chat removed from bar. Files: `activityBarViews.ts`, `activityBarPrefs.ts`, `ActivityBarViewIcons.tsx`, `ActivityBarMorePopover.tsx`, `ActivityBarCustomizePopover.tsx`, `ActivityBarIconButton.tsx`, `usePointerListReorder.ts`, `useActivityBarViewHeight.ts`, `useActivityBarPopoverPosition.ts`, `ActivityBar.tsx`, `App.css`.
