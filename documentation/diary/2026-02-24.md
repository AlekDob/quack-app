---
type: diary
project: quack-app
date: 2026-02-24
---

- [18:00] (Alek) Analisi per aggiungere icona "Reveal in Finder" all'ImagePreviewWidget (ToolWidgets.tsx:756). Scoperto che RevealInFinderButton.tsx esiste già con supporto cross-platform (iconOnly mode). Piano: importare nel widget header, CSS minimal con margin-left: auto. Da verificare se filePath è sempre assoluto. ~15 righe di modifica, nessun nuovo file.
- [18:15] (Alek) Implementata icona "Reveal in Finder" nell'ImagePreviewWidget. Riusato `RevealInFinderButton` con `iconOnly` + classe custom `image-preview-finder-btn`. Posizionata top-right con `margin-left: auto` nel flex header. Styling: opacity 0.4 → 0.9 on hover, bg trasparente con hover highlight. File modificati: `ToolWidgets.tsx` (import + JSX), `ToolWidgets.css` (2 regole CSS).
- [21:30] (Alek) Fix bug piani duplicati: il SDK rimaneva in `permissionMode: 'plan'` per tutta la sessione, e dopo la prima approvazione di ExitPlanMode il SDK ri-entrava in plan mode causando un secondo widget "Awaiting Approval". Fix: aggiunto flag `planAlreadyApproved` in `stream-claude.js` — dopo la prima approvazione, le chiamate successive a ExitPlanMode vengono auto-approvate. Se il piano viene rifiutato, il flag resta false e l'agente può risubmettere. File: `src-tauri/node-sdk/stream-claude.js`.
- [22:00] (Alek) Fix notifiche mancanti per AskUserQuestion e PlanApproval: i listener globali in App.tsx usavano solo `sendNotification` (nativa OS, invisibile quando l'app è in focus su macOS) e avevano closure stale di `agentChats`. Fix: aggiunto `showProjectToast` (in-app toast warning, 8s) a entrambi i listener + usato `terminalsRef` invece di `agentChats` per info agente sempre aggiornate + aggiunto `id` alle notifiche native. File: `App.tsx`.
- [23:00] (Alek) Implementata feature Automation: sistema di cron job per sessioni agente programmate. Architettura: Rust scheduler (tokio 30s loop + crate `cron` per parsing) emette tick event, frontend controlla jobs e lancia sessioni. Storage su Tauri Store JSON (`quack-automations.json`). UI: icona orologio nella toolbar (Cmd+J), vista tab con timeline jobs + storico esecuzioni, form modale con preset cron (ogni giorno, lun-ven, settimanale, mensile) + custom raw. 12 file creati, 6 file modificati. TypeScript strict + Rust compilano senza errori.
- [23:45] (Alek) Piano architetturale per Quack Remote API + Mobile Dashboard. Scoperto che axum server esiste già su :6768 con endpoint session/create, session/message, session/start-chat. Piano: estendere con REST API completa (/api/agents, /api/sessions, /api/jobs, /api/execute), WebSocket real-time su /ws, mobile web dashboard servita direttamente da axum, auth Bearer token, binding 0.0.0.0 per accesso LAN. 5 fasi, ~17h stimate. Salvato in `documentation/decisions/decision-quack-remote-api-mobile-dashboard.md`.
- [00:30] (Alek) Implementata Phase 1 della Remote API. 3 nuovi moduli Rust: `remote_auth.rs` (Bearer token state + generation, no middleware), `remote_config.rs` (config via Tauri Store `quack-remote.json`, comandi get/set/regenerate + local IP detection), `remote_api.rs` (9 endpoint REST: status, agents CRUD, sessions CRUD, jobs list/fire/toggle, execute fire-and-forget con inline auth check). Integrato in `lib.rs`: binding condizionale 0.0.0.0 vs 127.0.0.1, `nest("/api", api_router)` per mount routes. Zero nuove dipendenze Cargo.
- [13:45] (Alek) Testing Phase 1 Remote API completato con successo. Tutti i 6 endpoint testati via curl su porta 6769 (dev build separato dall'app installata su 6768). Risultati: GET /api/status 200 (version, uptime, agent count), GET /api/agents 200 (23 agenti), GET /api/agents/:id 200 (dettaglio completo), GET /api/sessions 200, GET /api/jobs 200 (2 automation jobs), senza token 401 ("Invalid or missing Bearer token"). Auth inline funziona correttamente. Phase 1 COMPLETA.
- [14:45] (Alek) Implementata UI Settings per Remote API. Nuova categoria "Remote API" nella sidebar Settings (tra Notifications e Appearance). Componente `RemoteApiSettings.tsx` con 4 sezioni: Enable Remote Access (toggle + port display), Authentication (masked token + copy + regenerate), Connection (API base URL + local IP + status badge LAN/localhost), Available Endpoints (reference visuale con metodo GET/POST colorato + path + descrizione). Riusati tutti i controlli esistenti (SectionHeader, SettingsRow, IOSSwitch, ios-button). CSS aggiunto a UnifiedSettings.css per token display, URL display, status badge, endpoint list. Icona globe SVG. TypeScript + Rust compilano OK.
