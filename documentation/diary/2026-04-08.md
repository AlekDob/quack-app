---
type: diary
project: quack-app
date: 2026-04-08
---
- [09:00] (Alek) Added kanban tab type to split view supported types — was missing from the split pane renderer in App.tsx, causing "Questo tipo di tab non supporta la split view" fallback. Updated feature doc 049.
- [10:09] (Auto) Sessione: 2 file letti (~163.9k tok), 2 letture ripetute, ~489.1k tok risparmiati | Brain: 4 AST hits, 25 gotcha warnings, 13 breadcrumbs
- [11:30] (Alek) Typography custom font size: added 'C' (Custom) preset with numeric stepper (10-22px). M preset rebased to 13px body (was 14px), S to 11px. Added safeCustomSize() to guard NaN from stale persisted state (gotcha documented). Migration v10 ensures customFontSize field exists. Updated typography.ts, settingsStore, TypographySettings UI, index.css defaults.
- [10:13] (Auto) Sessione: 3 file letti (~8.2k tok), 1 letture ripetute, ~1.9k tok risparmiati | Brain: 8 gotcha warnings, 3 breadcrumbs
- [10:20] (Auto) Sessione: 4 file letti (~183.4k tok), 2 letture ripetute, ~37.6k tok risparmiati | Brain: 1 AST hits, 25 gotcha warnings, 13 breadcrumbs
- [10:25] (Alek) Kanban split pane horizontal scroll — when in split view, columns were too narrow. Added `isSplitPane` prop to KanbanTabView, `.split-pane` CSS class overrides `overflow-x: hidden` on `.kanban-view`, sets `min-width: 900px` on columns+header so board scrolls horizontally when pane is smaller than 900px.
- [10:45] (Alek) Kanban column restructuring — removed In Progress sub-groups (Ready/Working/Cold). Cold tasks now auto-promoted to TODO. Ready tasks auto-promoted to Human Review. In Progress is now flat (only Working tasks). Human Review has 2 groups: AWAITING INPUT (orange, SDK question pending) and READY (green, agent finished). Classification logic moved from KanbanColumn to KanbanView (useMemo). Updated CSS, feature doc 044.
- [12:04] (Auto) Sessione: 1 file letti (~1.2k tok)
- [12:30] (Alek) Fix sidebar intra-group project sorting: projects inside a group couldn't be drag-reordered because they weren't in a SortableContext. Added nested SortableContext per group + intra-group detection in handleRepoDragEnd that reorders both group.projects (Rust backend via updateGroup) and local repositoryOrder. Updated feature doc 043.
- [13:02] (Auto) Sessione: 2 file letti (~88.3k tok), 2 letture ripetute, ~196.7k tok risparmiati | Brain: 18 gotcha warnings
- [14:00] (Alek) Added "Brain" accordion section to SidePanelAccordion — new section after File Explorer shows a FileExplorer rooted at `{rootPath}/documentation`. Lazy-loads on first expand, badge shows .md+.mmd file count via search_files_recursive. Reuses shared tree cache and onLoadChildren/onOpenFile from App.tsx (zero changes to App.tsx). Color: #e879f9 fuchsia. Updated feature doc 035.
- [15:00] (Alek) Brain sort by modification date — added `modified_at: Option<u64>` to Rust `DirectoryEntry` (fs.rs), `modified_at?: number` to TS types, `sortBy` prop to FileExplorer ('name'|'modified'), `sortEntries()` with dirs-first + newest-first. Brain section passes `sortBy="modified"`. Updated feature docs 033, 035. Created pattern `pattern-brain-accordion-section.md`.
- [14:40] (Auto) Sessione: 7 file letti (~201.6k tok), 4 letture ripetute, ~1.3M tok risparmiati | Brain: 8 AST hits, 45 gotcha warnings
- [14:47] (Auto) Sessione: 1 file letti (~3.3k tok) | Brain: 5 gotcha warnings
- [14:53] (Auto) Sessione: 2 file letti (~4.1k tok), 1 letture ripetute, ~5.9k tok risparmiati | Brain: 4 gotcha warnings
- [14:55] (Auto) Sessione: 2 file letti (~1.9k tok)
- [15:22] (Auto) Sessione: 1 file letti (~3.4k tok), 1 letture ripetute, ~10.2k tok risparmiati | Brain: 20 gotcha warnings
- [15:30] (Alek) DiffViewer split view — added side-by-side mode with toggle button in header. Left col shows removed/old lines, right col shows added/new lines, unchanged lines mirrored on both sides. buildSplitRows() aligns removed/added pairs. Toggle icon uses split-pane SVG, persists per-instance via useState. CSS grid 50/50 with separator border.
- [16:00] (Alek) DiffViewer fullscreen mode — added expand button (second toggle in header) that opens diff in a fullscreen overlay via createPortal. Overlay has backdrop-blur, 1200px max-width container, no max-height limit so full diff is visible. Font bumped to 12px in fullscreen. Escape key + click-outside to close. Extracted shared DiffContent component to avoid duplicating split/unified rendering logic. Both split and fullscreen modes work together.
- [16:00] (Alek) Markdown/Mermaid preview system — Preview/Editor toggle for .md/.mdx/.mmd files in both CodeEditorView and SplitCodeEditor. Button text flips between "Preview" and "Editor", outline hidden in preview mode. Preview state preserved across file switches. Added `toggleEditorPreview` (Cmd+Shift+P) and `editorSave` (Cmd+S) to ShortcutActionId with KeyboardShortcutTooltip on buttons. Fixed `.editor-btn-preview` padding to match other buttons.
- [16:15] (Alek) Clickable file path chips in MarkdownText — inline code matching file extensions gets `data-filepath` + `md-file-link` class with click handler dispatching `quack:open-file` CustomEvent. App.tsx listener resolves relative paths via `explorerRoot/src/` then `explorerRoot/` and opens in code editor tab. Works in both editor preview and chat messages.
- [16:30] (Alek) Mermaid (.mmd) preview — `isMermaid` detection in CodeEditorView/EditorContent/SplitCodeEditor. Mermaid code blocks in MarkdownText rendered as lazy-loaded MermaidDiagram. MermaidDiagram supports zoom (pinch/Ctrl+scroll/buttons, 0.2x-3.0x) and pan (space+drag when focused, middle-click drag) with `will-change: transform` for GPU acceleration. Created feature doc 052-markdown-mermaid-preview.md. Updated feature docs 024, 049.
- [16:45] (Alek) Drag-to-chat drop zone — SplitDropZone now shows a third "@ Chat" zone at the bottom when dragging sidebar items (files/skills/rules). Dropping a file there calls `handleMentionFile` to insert `@file:path` in chat input. Previously the split overlay intercepted all drops, preventing files from reaching ChatInput. Added `onChatDrop` + `showChatZone` props to SplitDropZone. Chat zone has green accent color to distinguish from blue split zones. Updated feature docs 049, 051.
- [17:00] (Alek) Folder drag-and-drop — removed `draggable={!isDirectory}` guard and `if (is_dir) return` from FileExplorer.handleDragStart. Folders now draggable to split panes and chat zone. Drag payload includes `isDir` flag. Updated feature doc 033.
- [16:01] (Auto) Sessione: 2 file letti (~2.3k tok), 1 letture ripetute, ~1.2k tok risparmiati
- [16:44] (Auto) Sessione: 3 file letti (~7.1k tok), 1 letture ripetute, ~1.9k tok risparmiati | Brain: 1 gotcha warnings
- [17:15] (Alek) Fix SplitCodeEditor hooks order crash — `useShortcutsStore(formatShortcut)` was called after `if (isLoading) return` early return, violating React Rules of Hooks. Moved hook to top with others. Bug doc: fix-split-code-editor-hooks-order.md.
- [16:55] (Auto) Sessione: 1 file letti (~3.4k tok), 1 letture ripetute, ~6.8k tok risparmiati | Brain: 15 gotcha warnings
- [17:14] (Auto) Sessione: 5 file letti (~6.6k tok)
