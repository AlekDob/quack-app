---
type: diary
project: quack-app
date: 2026-03-12
---

- [18:30] (Alek) Fix PlanWidget CSS: rimosso `@media (prefers-color-scheme: dark)` che dipendeva dall'OS e non dall'app (Quack e' sempre dark). Header ora visibile anche con macOS in light mode, sfondo widget piu' prominente, colori allineati al design system.
- [20:00] (Alek) Remote Teams API Phase 1: creato `remote_api_teams.rs` con 4 endpoint REST (`POST/GET /api/teams`, `GET/DELETE /api/teams/:id`). I team sono orchestrati via Remote API — ogni membro = sessione separata lanciata con staggered `remote-execute` events (500ms delay). Auto-sync degli status nel GET. Storage in Tauri Store `quack-remote-teams.json`. Zero modifiche a daemon/App.tsx.
- [22:00] (Alek) Remote Teams Phase 2 — Frontend integration: creato `RemoteTeamWidget.tsx` (avatar cliccabili, status dot, task preview), integrato in `ChatView.tsx` sopra MessageList per il lead agent. Aggiunto `team-created` Tauri event nel backend Rust per notificare il frontend. Esteso `teamStore.ts` con `RemoteTeamData` e `setRemoteTeam`. Aggiunto listener `team-created` in `App.tsx`. Aggiornata skill `quack-remote` con polling pattern + timeout per orchestratori AI.
