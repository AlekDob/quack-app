---
type: diary
date: 2026-02-13
---
# 2026-02-13

- Fixed stamina bar correctly: `input_tokens` with prompt caching = only non-cached tokens. Real context fill = `input_tokens + cache_read + cache_creation`. Also use assistant message usage (per-step) not result event (cumulative). See bugs/fix-stamina-messages-zero-modelusage-fallback
- Fixed auto-compact percentage discrepancy: removed AUTO_COMPACT_COST from totalContextUsage calculation (was inflating by 45k). Now matches CLI `/context` output
- Fixed stamina reset on app restart: persist tokens to sessionStore on result events, restore to chatTokensMap at bootstrap
- Implemented custom IDE support via native file picker: users can add any editor without app update. Rust extracts name/icon from Info.plist, stores in `~/.quack/custom-ides.json` (see patterns/custom-ide-support-via-native-file-picker)
