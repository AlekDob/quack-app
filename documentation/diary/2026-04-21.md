---
type: diary
project: quack-app
date: 2026-04-21
---
- [HH:MM] (Alek) Refactor Effort parameter — model-aware defaults + daemon clamp. Sostituiti 5 hardcoded `effort: options?.effort || 'medium'` in `src/App.tsx` con `|| defaultEffortForModel(model)` (righe 2935, 3695, 3885, 4644, 5170, 5180, 5193 — tutti con breadcrumb `Brain: task-effort-model-aware-refactor`). Aggiunto clamp in `stream-daemon.js` che degrada `xhigh → high` quando il modello non è Opus 4.7 (con warning), rendendo finalmente vero il commento in `types.ts`. Aggiornato default fallback model `opus46 → opus47` nei path coerenti. Impact: utenti Opus 4.7 con settings non migrate ora ricevono `xhigh` (raccomandazione Anthropic) invece di `medium`; utenti su Haiku/Sonnet 4.5 con preset `xhigh` non lo passano più all'API. Follow-up pendente: valutare deprecazione thinkingMode come UI separata (slider unico effort) — richiede UX review.
