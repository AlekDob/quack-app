---
type: diary
project: quack-desktop
date: 2026-07-05
---
- [09:45] (Alek) **Composer effort — default, persistence, reminders, shortcuts.** (1) Removed CLI "default" from the slider; app default is **medium**; always passes `--effort` explicitly. Persisted in `lcp.claudeCode.effort` (like perm mode). (2) Replaced text pill with signal-strength meter (5 bars + chevron); popover portaled with fixed coords. (3) Compose reminder: first keystroke (empty→non-empty) pulses the meter + shows animated floating label (`Medium`, etc.) above the bars; `Ctrl+1–5` quick-switch with same feedback. (4) Docs: `022-chat-composer.md` (full effort section), `003-design-system.md`, `014-claude-code-bridge.md` (per-turn knobs table). Files: `EffortPopover.tsx`, `AIChatPanel.tsx`, `App.css`.
- [10:15] (Alek) **Per-project context dock + status pill contrast.** (1) Moved editor/`@`-file context from a lone chip above the composer + transcript `Files:` bar into `ContextFilesDock` on the **right** of `.ai-status-dock-row` (`1 file in context`, hover popover with ON/OFF + remove). (2) `workspaceChatContext.ts` — attach toggle persisted per `wsId`, `@` queue in-memory per project; send path gated by `isUnderRoot`. (3) `StatusPill` inverted monochrome (`--primary-bg`/`--primary-fg`). (4) Popover `--radius-md` fix (was invalid `var(--radius)`). Docs: new `037-project-context-dock.md`, updates to `022`, `006`, `CLAUDE.md`. Files: `ContextFilesDock.tsx`, `workspaceChatContext.ts`, `pathUtils.ts`, `AIChatPanel.tsx`, `App.css`.
- [10:45] (Alek) **Conductor-style tool stream + compose review diff tabs.** (1) **Unified `.ai-iarow`** — `InterleavedBlocks` always uses `CompactBlocks` (editor + agent): explore chip (`Explored N files, M searches`), action chips (`4 ran`, etc.). Removed old `.ai-tcall-wrap` pill cloud. (2) **Live ComposeCard** — visible mid-stream (`· editing…`); Undo/Keep gated to turn end. (3) **`ComposeReviewPane`** — click file in recap → `crev:` tab with inline Monaco diff, hunk nav, Undo `⌘N` / Keep `⌘Y`; snapshot-backed before pane. (4) **Agent Mode split** — review pane beside chat (50/50), multi-file mini-tabs. Docs: new `038-compose-review.md`, rewrite `006-chat-tool-render.md`. Files: `composeReview.ts`, `ComposeReviewPane.tsx`, `composeCard.tsx`, `chatToolRender.tsx`, `DiffView.tsx`, `store.ts`, `WorkspaceShell.tsx`, `AgentModeShell.tsx`, `PaneNode.tsx`, `AIChatPanel.tsx`, `App.css`.
- [11:15] (Alek) **Sticky user message bar (Cursor-style).** User prompts now pin at the top of `.ai-messages` while scrolling the assistant reply for that turn. Root fix: group flat messages into `.ai-turn` wrappers (`groupChatTurns` in `chatScroll.ts`); `position: sticky` on `.ai-msg-user` (not `.ai-user-bar`) so the containing block spans prompt + response. Removed broken `ai-msg-user-latest` gate; disabled `.ai-msg` entrance animation on user wrappers (`transform` breaks sticky). `z-index` moved to sticky wrapper. Docs: rewrite `030-user-message-bar.md`, cross-refs in `021`, `022`. Files: `chatScroll.ts`, `AIChatPanel.tsx`, `UserMessageBar.tsx`, `App.css`.
- [11:30] (Alek) **User bar surface contrast.** Replaced `--bg-hi` on `.ai-user-bar` with dedicated `--user-bar-bg` / `--user-bar-border` tokens (12% fg mix on stream in dark, 5% on `--bg-alt` in light) so prompts read clearly above the transcript without heavy shadows. Docs: `030-user-message-bar.md` (surface table), `003-design-system.md` (token list). Files: `App.css`.
- [12:30] (Alek) **Tool stream polish + compose recap + agent subagent panel.** (1) **Solo tool rows** — `CompactBlocks` skips `.ai-iarow` when a run has one tool; `.ai-tcall-standalone` adds vertical margin. (2) **WebFetch drawer** — `isMarkdownDrawer()` renders fetch results as `MarkdownPreview`, not raw `<pre>`. (3) **Subagent in Agent Mode** — click `ai-tcall-subagent` opens `SubagentTranscriptView` in the 50/50 side panel (`focusedAgentSidePanelKey`, inline mount + close); editor layout unchanged (portal). (4) **ComposeCard prominence** — total `+/-` recap pill (`.ai-compose-bar-recap`, live during stream), entrance slide-up, post-turn border pulse, taller bar. Docs: `006`, `038`, `004`. Files: `chatToolRender.tsx`, `composeCard.tsx`, `AgentModeShell.tsx`, `SubagentTranscriptView.tsx`, `store.ts`, `App.css`.
- [14:15] (Alek) **Cursor-style composer queue.** Replaced the purple toast + generic banner with in-pill queue cards (`ComposerQueue.tsx`): visible message preview, `N Queued` badge, `↵ to Send` hint, remove per item. Placeholder while busy → `Send follow-up`. **Start Multitasking** menu: **New chat** (parallel send via `requestAIPrompt` + optional `chatId` on `aiBus`) or **Send now** (abort current turn, drain queue). Stop (Esc) still clears queue. Docs: new `039-composer-queue.md`, updates to `022`, `003`, `CLAUDE.md`. Files: `ComposerQueue.tsx`, `AIChatPanel.tsx`, `aiBus.ts`, `App.css`.
- [14:30] (Alek) **Agent-mode chat switch loader.** Sidebar session picks remount `AIChatPanel` (keyed by chat id) — the old transcript lingered for a beat with no feedback. Added a brief overlay (`Loading chat…` + spinner) in `AgentModeShell` that shows on every switch (not first load), clears via `onHydrated` after messages paint (double `rAF`), min 150ms so it reads intentional. Fade-in on the chat body. Files: `AgentModeShell.tsx`, `AIChatPanel.tsx`, `App.css`.
- [14:35] (Alek) **Chat switch loader — New chat too.** Loader now triggers immediately on `newSession` / `selectSession` click (not only on `activeChatId` effect), including first session from the empty state and cross-workspace picks. Files: `AgentModeShell.tsx`.
- [14:50] (Alek) **Agent-mode chat switch — no remount.** Overlay + `flushSync` approach was invisible and stuttery. Replaced with `AgentChatHost` (one `AIChatPanel` per chat, lazy mount, CSS crossfade) — same pattern as editor `AIChatHost`. Switching is instant for visited chats; streams survive tabbing away. Removed `onHydrated`. Files: `AgentModeShell.tsx`, `AIChatPanel.tsx`, `App.css`.
- [15:10] (Alek) **Chat switch — forced 1s overlay.** User still couldn't perceive the transition. Added a solid full-bleed `Loading chat…` veil (`z-index: 200`, `--bg`) that always shows for **1 second** on every sidebar pick / New chat, hiding panels until the timer ends. Files: `AgentModeShell.tsx`, `App.css`.
- [16:00] (Alek) **Chat switch veil — editor mode + shared module.** Extracted `chatSwitch.ts` (`pulseChatSwitch`, 500ms) + `ChatSwitchVeil` / `useChatSwitching`. Editor hub now pulses on focus/new chat; `AIChatHost` shows the same veil over tabbed chats. Agent mode refactored to the shared module. Files: `chatSwitch.ts`, `ChatSwitchVeil.tsx`, `useChatSwitching.ts`, `AIChatsRail.tsx`, `WorkspaceShell.tsx`, `AgentModeShell.tsx`, `App.css`.
- [15:20] (Alek) **Per-session effort / mode / thinking.** Effort (and permission mode + extended thinking) were global in `localStorage`, so switching chats showed the wrong slider. Now stored on `ChatSession` (`ccEffort`, `ccPermMode`, `ccThinking`), restored on tab/history switch; global keys remain defaults for brand-new chats only. Files: `chatHistory.ts`, `AIChatPanel.tsx`.
- [15:45] (Alek) **Per-session composer draft.** Input text, follow-up queue, attach toggles (`/tree`, terminal), `@` subagent targets, and staged images persist on `ChatSession.composer` and restore when switching sessions (Agent Mode remounts the panel on every pick). Model per session restore fixed (`setSelected(session.model)`). Docs: new `040-per-session-composer-state.md`, updates to `001`, `014`, `015`, `022`, `039`, `CLAUDE.md`. Files: `composerDraft.ts`, `chatHistory.ts`, `imageAttach.ts`, `AIChatPanel.tsx`.
- [15:55] (Alek) **Fix per-session state bleed + lost draft.** Legacy chats without `ccEffort` still read global `localStorage` after another session changed effort; debounce cleanup only `clearTimeout` dropped drafts on fast switch. Fix: `sessionKnobsFrom` → medium/Ask for existing rows without fields; `mergeSessionKnobs`; `flushSessionState` with synchronous cleanup + `useLayoutEffect` unmount flush; `knobsPersistRef`. Docs: expanded `040`. Commit `7e72a1a4`.
- [15:45] (Alek) **@-mention path preview (Cursor-style).** File autocomplete in the composer now shows filename + parent dir in the list, and a side tree preview (`src › components › file.tsx`) with indent guides when a file row is highlighted. Extracted `MentionSuggestions.tsx` + `MentionPathPreview.tsx`. Files: `AIChatPanel.tsx`, `App.css`.
- [15:50] (Alek) **@-mention popover clipping fix.** Reverted the portaled menu (layout broke). Inline popover restored; `.ai-panel.ai-mention-open { overflow: visible }` plus `:has()` on tab/side-panel hosts so the list isn't clipped — no portal, no fixed positioning. Files: `MentionSuggestions.tsx`, `AIChatPanel.tsx`, `App.css`.
- [16:05] (Alek) **Docs: @-mention file preview (041).** New `041-mention-file-preview.md`; cross-refs in `004`, `022`, `CLAUDE.md`. Commit bundles mention components + session usage ring, git accordion panel, and related UI/Rust changes.
- [16:10] (Alek) **OpenCode — stop system-prompt echo.** `deepseek-v4-flash-free` (and similar) repeated the whole `[System]` block in chat because Quack flattened Jack + CLAUDE.md into the user `parts` text. OpenCode's API has a native `system` field — bridge now uses `splitCliPrompt` → `body.system` + user-only `parts`. Agentic providers (OpenCode/Cursor) no longer get Quack's fake `list_files` tool rules. Files: `cliPrompt.ts`, `openCode.ts`, `AIChatPanel.tsx`.
- [16:15] (Alek) **Fix compose-review diff tab in editor mode.** Clicking a file in `ComposeCard` opened a `crev:` tab with Monaco squashed into a narrow vertical strip — `.pane-content` is a flex row and `.compose-review-host` had no width/fill rule (agent mode worked via `.agent-main-review .compose-review-host { flex: 1 }`). Portaled host now uses `position: absolute; inset: 0` like `.ai-tab-host`, `display: none` when inactive, and `diffRef.layout()` on show. Files: `ComposeReviewPane.tsx`, `App.css`.
