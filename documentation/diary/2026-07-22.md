# 2026-07-22

- [09:10] **Provider session terminal icon always opens a new PTY.** `resumeProviderInTerminal` no longer reuses the last tab; Agent Mode selects the new right-column terminal. `providerSessionTerminal.ts`, feature `044`.

- [09:10] (Alek) **Composer project picker → Cursor-like** (`050`). Chip = project **name** (tooltip `~/…`); menu **Recents** MRU (open + closed, cap 10, checkmark on current) + Open folder…. Pick → `setActiveWorkspace` / `openWorkspace` + `addNewAIChat` in target project (no rebind — chats owned by `wsId`). Files: `WorkspacePathPicker.tsx`, `App.css` (`.ai-composer-ctx-path`, `.ai-composer-ctx-check`). Docs: `050`, `022`, `001`, `CLAUDE.md`.

- [08:41] (Alek) **Agent Mode is now the default layout.** Fresh installs / unset `lcp.agentMode` open Agents (not IDE). Explicit IDE choice still persists as `""`. `src/agentMode.ts` + feature `085`.

- [08:45] (Alek) **StatusBar icons Agent Mode–aware.** Explorer/Git → Files/Changes tabs; Ctrl+B toggles Files↔Changes; hide Save/Auto-save/zoom (no editor surface). Terminal/panel already adapted. `StatusBar.tsx`, `agentContextNav.ts`, `actions.ts`, feature `084`.

- [09:48] (Alek) **Chat auto-title via cheap LLM** (feature `089`). If the chat is never named, after the first turn a fast model generates a short title (like Claude Code / Cursor): CC → `claude -p --model haiku`, Cursor → `cursor-agent -p`, direct API providers → accumulated `chat()` with Haiku/gpt-4o-mini/local. New `chatAutoTitle.ts` + trigger effect in `AIChatPanel`. KEY INSIGHT: the persist path recomputed `deriveTitle` on every flush and would clobber the LLM name — added `autoTitled` flag + `preferredTitle()` so locked/auto titles survive flushes; hand rename (`titleLocked`) still wins. Reused existing one-shot primitives (`claude_print_text`/`run_cursor_output`); only added thin `#[tauri::command]` wrappers. `npm test` 163 green, `tsc` + `cargo check` clean.
