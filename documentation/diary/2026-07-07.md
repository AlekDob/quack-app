# 2026-07-07

- [14:30] **Feature 044 — Provider session bridge.** Problem: users could not match Quack chats to Claude Code JSONL sessions or Ghostty `claude --resume` runs (three separate ids). Solution: (1) header chip shows `CC`/`CU`/`OC` + truncated uuid, click copies full id; (2) terminal icon writes `cd '<cwd>' && claude --resume <id>` to bottom PTY; (3) ⟲ Sessions picker shows id prefix, "This chat" badge, linked Quack chat title, per-row terminal icon. Backend unchanged — uses existing `providerSessionIds`, `claude_code_list_sessions`, `claude_code_load_session`. Docs: `features/044-provider-session-bridge.md`, updates to 001/014/agent-provider-patterns/CLAUDE.md.
- [14:45] **Feature 045 — HTML preview (drawer + tab + editor).** Sandboxed iframe preview for agent HTML and workspace `.html` files. Drawer via `ToolResultDrawer` (`variant: browser`); virtual `prev:` tabs stashed in `htmlPreview.ts`; `.html` editor tabs get Edit/Split/Preview like Mermaid. Agent tools `ShowHtmlPreview` / `*html_preview*` open drawer; Write/Edit on `.html` gets a globe preview chip; drawer can "Open in tab".
